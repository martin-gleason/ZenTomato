# Register — ZenPom

**THIS FILE IS HAND-MAINTAINED EXCEPT FOR ONE REGION.** Everything here is written by hand except
the table between the `BEGIN GENERATED: decisions` and `END GENERATED: decisions` markers under
`## Decisions (D)`, which `scripts/gen_status.py` writes from `docs/plans/00-deltas.md` (`D37`,
ratified 2026-09-22 — nothing is maintained twice). `scripts/gen_status.py` also READS this whole
file to write `docs/plans/00-status.md`. It is the register `docs/conventions.md` Axis 2 describes:
one `##` per register, every row carrying a status, every register sorted by ID, every ID appearing
exactly once — and since `C34` those four properties are checked by
`scripts/check_register_rows.py` at pre-commit and in CI rather than asserted here.

**PROVENANCE, CORRECTED 2026-09-23 by `C33`.** This header previously read *"Regenerated by
`scripts/extract_zenpom.py`, which MERGES"*, and described a `Mode`/`TD` merge protocol for an
extractor that **exists in no branch and no commit of this repository** (`docs/chores/C28.md:32`;
`docs/plans/F13.md:663` records the upstream tool as carrying `O`, `D` and `C` only, so it has never
heard of `A`, `M`, `H` or `RR`). The `Mode` and `TD` columns are real and are kept — `TD` holds the
Todoist links the owner's own sync uses — but no run refreshes them and an empty cell asks for
nothing. **This is `docs/conventions.md`'s validate-every-artefact case** — cited by name and not by
its upstream decision number, because that number is an upstream one and this project's delta walk
(`H1`) rightly refuses a `D<n>` with no heading in `docs/plans/00-deltas.md`, which stops at `D41`:
the tables below were checked row by row and the sentence describing where they come from was not
read.

## Owner items (O)

| ID | Title | P | Status | Mode | From | Why | TD |
|---|---|---|---|---|---|------|
| O1 | One real day's export, read beside the Rhodia | P0 | open | @? | F6 | `SPEC.md`'s *Done when* for F6, and the only judgement of "readable without translation". The golden file is a format test, not the criterion. | td:6hPjPhcfpH8Qwqvj |
| O2 | **Migration over an existing install** **CLOSED 2026-08-28** |  | closed |  | F6, F2c | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F6, F2c. The text below is OPEN.md's own, not a rewrite. Build `202608281026` was installed over the running app, not a deleted one, and it carried F2c's seventh `AppSettings` column. The sprint ran to completion with settings and history intact, so lightweight migration works over a real store. This was the load-bearing one: a unit test builds a pre-F2c row, it cannot find one on a phone. |  |
| O3 | A full sprint with the screen locked, uninterrupted | P0 | closed | @verify | F4 | CLOSED 2026-09-09 on 0.9.0 (202608281553). Run at last as the single uninterrupted sprint the spec describes, with O4 and O5 in the same run. The owner: "O3, O4, and O5 were one and succeeded. each passed the test." This closes SPEC.md's Done when for F4 — the standing objection was that the pieces had been seen individually and never as one run. | td:6hPjPhm5fjP2GGpC |
| O4 | Headphones, CarPlay, and an incoming phone call | P0 | closed | @verify | F4 | CLOSED 2026-09-09 on 0.9.0 (202608281553), in the same run as O3 and O5. The row was already marked closed with no evidence recorded; this fills it. NOT ITEMISED: the owner reported the run passing as a whole and the three interruption paths were not confirmed individually, so this records one run rather than three per-path results. |  |
| O5 | A playlist short enough to reach its end, to prove looping | P1 | closed | @verify | F4 | CLOSED 2026-09-09 on 0.9.0 (202608281553), in the same run as O3 and O4. A real playlist reached its end, which is the half no test could reach — the loop had only ever been asserted against a stand-in. |  |
| O6 | **The alarm firing through an active Focus, and through silent mode** **CLOSED 2026-08-28** |  | closed |  | F2 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F2. The text below is OPEN.md's own, not a rewrite. **Both halves now observed.** Through a Focus: confirmed on three builds. Through silent mode: the owner ran the `C20` check on build `202608281026` — *"in silent and DND, the alarm consistently fired as did the sheet."* That is the half `SPEC.md` F2's whole design rests on: AlarmKit was chosen over a notification precisely so an alert would survive a Focus and the ringer switch, and it does. |  |
| O7 | **Sound off** **CLOSED 2026-08-28 — on the phone** |  | closed |  | F2, C20 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F2, C20. The text below is OPEN.md's own, not a rewrite. AlarmKit has no silent case, so this setting rested entirely on a bundled silent file, and **nothing had ever observed a phone staying quiet**. It has now: *"bell sound OFF: no sound from phone."* `Silence.caf` does what the setting rests on. The watch is a separate question — `O33`. |  |
| O8 | VoiceOver on hardware | P0 | open | @verify | F2, F5 | The countdown was coarsened to whole minutes to cut announcement spam; whether iOS re-announces at all is runtime behaviour nobody has listened to. F5 adds the capture buttons' labels, values and announcements. | td:6hPjPhpX9fjRm99C |
| O9 | The merged stop sheet with taps in it, at AX5 | P1 | open | @? | F5 | The single surface D14 was written for. Three chained sheets on one view, and the switched-off confirm button, unexercised at the largest text size. | td:6hPjPhxG7MHrQ8jj |
| O10 | **Apply the nine outstanding spec amendments** **CLOSED 2026-08-27 — the count is zero** |  | closed |  | C6 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by C6. The text below is OPEN.md's own, not a rewrite. `A1`–`A7` were applied 2026-08-24 and `A8`–`A9` on 2026-08-27; the row's "nine outstanding" had been stale since August. `AMENDMENT-BASELINE.txt` holds the authoritative count and `DeltaIntegrityTests` fails if it grows, so this is enforced rather than tracked. `docs/specs/AMENDMENTS-TO-APPLY.md` is now a record of what each amendment changed, not a to-do list. |  |
| O11 | **F7's gate: D2 into `SPEC.md`** **CLOSED — D2 was applied 2026-08-24** |  | closed |  | F7 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F7. The text below is OPEN.md's own, not a rewrite. `docs/plans/F7.md` said *"Until D2 is merged into `SPEC.md`, this feature does not exist and no line of it may be written."* It was merged as part of `A2` on 2026-08-24, which is what unblocked F7 — the row simply outlived the fact, alongside `O10`. |  |
| O12 | Three Todoist API facts, against a live token | P1 | open | @verify | F3b | `scripts/check-todoist-facts.sh`. Whether an archived project resolves by id, whether sync reports deletions as tombstones or by silence, and whether old numeric task ids still resolve. TWO OF THE THREE ARE NOW SETTLED — the owner ran it 2026-09-24 against their real account and the output is in `docs/reviews/F13.md`. (1) AN ARCHIVED PROJECT DOES RESOLVE BY ID: 19 archived projects, `GET /projects/{id}` on \"FECP and SDOP\" returned HTTP 200 with `is_archived=True`. So the mirror must read BOTH `/projects` and `/projects/archived` or it loses the name — `/projects/archived` is NOT on the allowlist and reading it is a decision, not a tidy-up. (3) NO ALL-NUMERIC TASK ID IS IN ACTIVE USE: 50 active tasks, 50 alphanumeric, 0 numeric. That does not prove old ids are dead, only that none is live; `--task-id` settles it if a recorded old id turns up. (2) TOMBSTONES ARE STILL OPEN: phase 1 took a token (7 projects, 0 already flagged deleted) and phase 2 needs the create-token-delete sequence the script prints. **AND THE PHASE-2 VERDICT BLOCK HAD NEVER COMPILED** until 2026-09-24, so this fact could not have been settled by running it — see `F13-M10`. | td:6hPjPj3MCrjqcvrC |
| O13 | **Developer Mode on the watch, then provision it** **CLOSED 2026-08-24** |  | closed |  | C8 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by C8. The text below is OPEN.md's own, not a rewrite. Root cause: the new Watch Ultra 3 has never had Developer Mode turned on, so Xcode cannot register it, so no watchOS profile can include it — and the watch app is signed with an iOS profile instead. iOS installs the phone app and silently skips the watch app. Blocks F7's device check. Check with `scripts/check-watch-provisioning.sh` |  |
| O14 | **Does the music row give a real answer now?** **CLOSED 2026-08-27 — yes.** Settings → Music reads **Connected** on the device, in Todoist's vocabulary. Open since the MusicKit-entitlement confusion; the App ID work (`C11`) and `F4c`/`F4d` between them answered it. — *now also readable in Settings → Music, which is the faster check* — *the App ID half is done; see `docs/chores/C11.md`* |  | closed |  | C8, C11 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by C8, C11. The text below is OPEN.md's own, not a rewrite. The App ID is explicit and MusicKit is enabled on it under App Services. The premise that a missing **entitlement** caused `ICError -7013` was wrong — there is no MusicKit entitlement for an iOS app. So whether those errors persist, and whether the subscription check now succeeds, is a device question rather than a settled one. |  |
| O15 | F7's device check — three wrist taps, phone in another room | P0 | open | @verify | F7 | D2's *Done when*, and the only thing that can close F7. Then the harder half: taps made genuinely out of range, arriving later with their **original** timestamps. | td:6hPjPj6Gg79fqFMj |
| O16 | **Branch protection on `main` — it has never existed** **CLOSED 2026-08-24** |  | closed |  | C1 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by C1. The text below is OPEN.md's own, not a rewrite. PR required, CI required, linear history, rebase-only, no force pushes. |  |
| O17 | **Search in the music picker** **CLOSED 2026-08-27 — built and verified on the owner's own library**, which is the only fixture that counted. `D23` ratified, `F4e` shipped. |  | closed |  | F4c | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F4c. The text below is OPEN.md's own, not a rewrite. The other half of the note that produced F4c. A library of a few hundred playlists is unusable without one, but a search field is new functionality rather than a rearrangement, so it wants a delta argued rather than assumed. |  |
| O18 | **No `PrivacyInfo.xcprivacy`** **CLOSED 2026-08-27 — added, and fenced.** Declares no tracking, nothing collected, no required-reason API, and `PrivacyManifestTests` holds it to that: the three arrays must stay empty, and the shipped Swift is searched for the five required-reason API families with comments stripped. Verified present in the built bundle rather than in `project.yml`. Formerly: |  | closed |  | C12 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by C12. The text below is OPEN.md's own, not a rewrite. Verified rather than assumed: no required-reason API is used (the only `UserDefaults` in the tree is a word in a comment) and nothing is collected or sent, so Apple does not require a manifest. Worth adding anyway so "collects nothing" is machine-readable rather than a claim in a README. |  |
| O19 | **The App Store name — "ZenTomato" is taken** **CLOSED 2026-08-25 — the name is `ZenPom`** |  | closed |  | C9, C10 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by C9, C10. The text below is OPEN.md's own, not a rewrite. App Store Connect returned `409 DUPLICATE.DIFFERENT_ACCOUNT` on the first upload attempt: the name is in use by another account. Costs nothing in code — bundle ID and home-screen name are unaffected — but no build can be uploaded until an app record exists, and no record can be created without a name. Effectively permanent once builds ship under it, and it is the name the public GPL repo carries, so it is `C10`'s decision too. |  |
| O20 | No choice of alarm sound (needs a delta — D24 proposed) | P1 | open | @Review | F2 | The owner: *"the default alarm sucks, and will be quite jarring to folks using an app with Zen in the name."* A sound picker is a **seventh** setting, and `PolishFenceTests` pins `AppSettings` at six — a bound that was mutation-tested against an alarm-sound picker specifically. So this cannot be built without ratifying the delta first. | td:6hPjPj968h93gQqC |
| O21 | **Music cannot be restarted during a break** **CLOSED 2026-08-28 — built as `F4f`, verified on the device** |  | closed |  | F4 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F4. The text below is OPEN.md's own, not a rewrite. `D25` ratified and applied to `SPEC.md` line 27. All three of that line's obligations confirmed on hardware: a break starts silent even with music on, the switch turns it on for that break, and it stops at the boundary without being asked. The switch is scoped to the break — during one it shows and sets that block, because the standing intention already read "on" while nothing played, leaving nothing to switch. |  |
| O22 | **A CLA or copyright assignment, before the first outside pull request** **CLOSED 2026-08-27 — `CONTRIBUTING.md` ratified by the owner** |  | closed |  | C10, C18 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by C10, C18. The text below is OPEN.md's own, not a rewrite. Ratified in its pledge-joining form: a contributor licenses their work GPL-3.0-or-later and joins the App Store non-enforcement pledge in `LICENSE-EXCEPTION.md`, keeping their copyright and every other GPL right. In force for every pull request opened after 2026-08-27; the instrument records its own effective date. What remains is enforcement at review, which is in the adversarial reviewer's brief. |  |
| O23 | Progress bars appear full rather than filling | P1 | open | @verify | F5 | Owner's note during sprint 2: *"progress bars should fill up, not just appear full."* Cosmetic, and not looked at yet. | td:6hPjPj9hGXXcM34C |
| O24 | the id names two different closed rows: a block heard end-to-end on a chosen bell, and the dual-licensing report fetchable by SHA | — | closed | — | F2c, C13 | Both closed before this row existed — `F2c` on 2026-08-28, `C13` on 2026-08-27 — and one register row cannot hold two items. `C37` recovered both verbatim as `C-8` and `C-9` in `docs/reviews/OPEN.md`'s `## Closed` table; this row is the pointer, not the record. The collision itself is still open in `## Defect — reused IDs`. | — |
| O25 | docs/chores/C18.md needs splitting so it can leave the licence allowlist | P2 | open | @Review | C18 | C18 holds both the superseded MIT design and the arrangement that shipped, so it must quote a phrase the licence check forbids, so it is exempt from that check — and the stale phrase then sat in its own title for a day. A banner and corrected task rows fix the misreading today. The structural fix is to move the MIT reasoning to `docs/archive/` and take C18 off the allowlist, which is doc surgery the owner should approve rather than the agent do inside a feature branch. | td:6hPjPjFj3JC8GGXC |
| O26 | **The alarm cannot be silenced from inside the app** **BUILT 2026-08-28, unseen on a phone** |  | closed |  | F2, F2c | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F2, F2c. The text below is OPEN.md's own, not a rewrite. `D26`, built as `F2d`. `AlarmScheduling` gains `alertingAlarmID`, `alertingUpdates()` and `stopAlerting(id:)`; the engine gains `silenceAlarm()`, which stops the noise and then runs the same `handleDismiss()` path `DismissBlockIntent` runs. Twenty-two tests across three suites, including a drift test that the button and the system alert produce the same recorded outcome. **Device check is `O29`.** |  |
| O27 | **Every setting is editable while a block runs** **BUILT 2026-08-28** |  | closed |  | F2c, F2e | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F2c, F2e. The text below is OPEN.md's own, not a rewrite. `D27`, built as `F2e`. One `.disabled(isBlockRunning)` on a `Group` holding all three customization sections, with a fence that was shown to fail when a section is moved out of it. Device check is `O30`. |  |
| O28 | **The alert sounds need a preview** **BUILT 2026-08-28** |  | closed |  | F2c, F2e | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F2c, F2e. The text below is OPEN.md's own, not a rewrite. `D28`, built as `F2e`. Selecting a sound plays it, which is iOS's own Settings › Sounds idiom rather than a new control. `Default` cannot be played — it is iOS's sound, not a file ZenPom holds — and the footer says so. Device check is `O30`, and the audio session is the half a simulator cannot answer. |  |
| O29 | The Silence button, on a phone — HALF ANSWERED 2026-08-28 | P0 | open | @verify | F2d | **App in the foreground: passes.** Build `202608281343`, the owner's words: *"with app open, silence button was amazing. all tests fired."* The screen-locked reflection gap found in that run is `D29`, ratified and built. **What remains is the second half**: a block ending with the app closed, dismissed from the system alert, confirming no double advance against the in-app button. The owner has it: *"the back half was tested in a previous build, I'll retest when this version lands."* | td:6hPjPjMCH3QJ3CVj |
| O30 | The locked settings screen and the sound preview, on a phone | P0 | open | @verify | F2e | `F2e`'s *Done when*, unrun. Three checks: the customization rows locked during a block with music and Todoist still live; **each bell previewed with the ringer switch off** — `.playback` is what should make that audible — with the app's own music expected to keep playing, and audio from *another* app allowed to be interrupted, which is the stated trade-off of the single non-mixable session policy this app has always had; and **music stopped and started again after a preview**, which is the one that matters, since the reverted `.mixWithOthers` version would have left the session mixable for the life of the process and broken `F4`'s interruption handling invisibly. Plus a preview left mid-play by leaving the screen and by backgrounding, both of which must go quiet. | td:6hPjPjRMRR7H6CWC |
| O31 | The watch alarm's button says Stop, and ours says Done — PARKED 2026-08-28 | P3 | open | @Review | F2d, F7 | This app sets that label to `"Done"`; the watch app's own source has no Stop button at all. So watchOS is drawing its own alert rather than honouring `AlarmButton`, and the word is not ours to change. The owner's ruling: *"if its a watchos default, keep it. I'll upload a screen shot next time to ris it."* Reopen if the screenshot shows otherwise. | td:6hPjPjVHpGxqW3Rj |
| O32 | A watch-face complication does not exist | P1 | open | @Review | F7 | Requested 2026-08-28: *"make sure zenpom can be installed as a complication on a ultra watch face."* **`SPEC.md` line 58 excludes it by name** — *"widgets beyond the Lock Screen Live Activity"* — and `F7.md` declined it citing that line. The only `WidgetKit` code in the tree is `ZenTomatoActivity`, an `ActivityConfiguration` embedded in the *iOS* app; a complication is a separate watchOS widget extension that does not exist. So this is a build, not a check: `D30`, proposed and not ratified, with the recommendation **not** to take it before the September 13 stop while `D29` — a lost reflection prompt in the ordinary locked-phone case — is outstanding. | td:6hPjPjc6HwRXpcxC |
| O33 | The watch makes a noise when the phone is told not to | P1 | closed | — | F2, F7 | CLOSED 2026-09-24 on build 202609241512 — **the reported defect does not reproduce, and five tests say why rather than one saying that.** The owner ran: (1) watch on silent AND theatre mode, sounds on the watch — no alarm on the watch, bells at the start and end of the break, *\"working as intended\"*; (2) the same at a 5-minute block — same result; (3) watch in the ZenPom Focus mode, phone sound ON — the watch rings; (4) watch in ZenPom Focus, phone sound OFF — **no alarm on the watch**; (5) watch in no Focus mode, phone sound OFF — no alarm and no alert. **Tests 4 and 5 are the ones that close this row:** sound off on the phone is now honoured on the wrist, which is precisely what was reported broken. THE EARLIER HYPOTHESIS IS REFUTED BY TEST 3 — the watch rings when sound is on, so the named sound does resolve on the wrist and the watch target is not missing its audio. That hypothesis was written as a hypothesis and is now closed as wrong, which is what filing it as one was for. **IT DID NOT CLOSE CLEAN:** the owner's judgement on test 4 is that silence is the wrong answer — *\"I think it should be a silent alarm, and a buzzing to let the user know the pom is done\"* — which is a new want and not this defect. `D45` carries it. A second observation from the same sitting, unrelated and unreproduced: music did not resume after the break with *classical concentration* selected. `O51`. ORIGINALLY FILED AS: Reported 2026-08-28 on build `202608281553`: *"focus screen on, bell sound OFF: no sound from phone, but the watch makes a buzz and a slight ringing."* **The phone half closes `O7` positively** — sound off really is silent, which nothing had ever observed — and it retro-explains `C20`: the alarm heard with sound off in an earlier sprint was almost certainly the watch. **Hypothesis with evidence:** the watch app ships no `.caf` at all. `project.yml` gives it `Assets.xcassets` specifically and says why — *"that also holds Silence.caf … which is F2's and has no business on a wrist"* — and `find` over the built `ZenTomatoWatch.app` returns none. If AlarmKit mirrors the alarm and resolves the named sound against the *watch's* bundle, it finds nothing, and a missing named sound is not silence there either. **If so the chosen bell is not honoured on the watch either.** One check, a build not a sprint: add `ZenTomato/Resources` to the watch target, install, run one block with sound off. Silence on both means one line fixes it; a buzzing watch anyway means `sound on/off` cannot be honoured on the wrist from the app bundle, which is spec-level and owes a delta. **Not to be guessed at** — three framework assumptions about this alarm have already been wrong. | td:6hPjPjj3VJ8mjvmC |
| O34 | From the lock screen, slide-to-stop worked | P1 | open | @? | F2d | Same run. The accompanying note — *"but the bell sound was turned on"* — is ambiguous between *the sound setting was on, so it rang* (expected) and *something changed the setting*. Recorded rather than resolved; one word next run says which. | td:6hPjPjqC3rxq24gC |
| O35 | Documentation accuracy left over from F2d/F2e's eight review passes | P2 | closed | — | F2d, F2e | CLOSED 2026-09-24. **Three of the four were already true or already fixed; the work was two edits and no decision, which is why the owner reading this row asked what they were being asked.** See `A20`. (1) The delta-count claim was FIXED by `D41`'s `theIndexStatesTheNumberOfDeltas` — the file now says 45 above 45 headings and a test asserts it, which is the same hole this row recorded. (2) `SilenceAlarmTests.theFlagClearsWhenWatchingEnds`'s doc was not merely stale but BACKWARDS: it gave *\"Start and Stop are both disabled while it is set\"* as the reason the test mattered, and `D26` rejected exactly that arrangement — `TimerScreen.swift`'s note records the first attempt disabling them and *\"producing a screen with three controls and nothing pressable\"*. What shipped swaps the button and disables nothing. Rewritten to give the reason that is true now: `ringingAlarmID` drives `alarmIsRinging`, which makes the primary control become Silence, so a stale `true` is a button offering to silence an alarm that is not ringing. (3) The `Alarm ringing, largest text` preview was the only one of eleven in `TimerScreen.swift` not to state a colour scheme, so its appearance followed the Xcode canvas rather than the preview; it now sets `.preferredColorScheme(.light)` like the rest. NO MUTATION, AND THAT IS NOT AN OMISSION: neither edit is a behaviour, and a mutation on a comment would be theatre. Evidence is `swiftlint --strict` clean and 657 tests passing unchanged — the point being that nothing moved. **Filed rather than fixed, at the owner's explicit direction:** the merge bar for that branch was *no code-behaviour defects*, with accuracy findings recorded and fixed after. The list, from pass eight: `SilenceAlarmTests.theFlagClearsWhenWatchingEnds`'s doc still describes controls being disabled, two design revisions stale; the `Alarm ringing, largest text` preview omits `.preferredColorScheme(.light)` unlike every other AX5 preview in the file; and `docs/plans/00-deltas.md` says *"24 deltas"* above a table of 32 rows — pre-existing on `main`, and `DeltaIntegrityTests` asserts presence rather than the count, so nothing catches it. | td:6hPjPjw7qVxgWPmC |
| O36 | The zenpom Focus runbook, followed once on the phone | P1 | open | @verify | C21 | C21 shipped written but NOT TRIED, and its own header says so. Step 6 is the verification: run one sprint with the Focus on and compare that sprint's external-distraction count against a typical one. The log already counts internal and external separately, so the only real measure of whether the chore was worth writing is free. Menu labels in it are unconfirmed against iOS 26 and corrections go back into the file. | td:6hVP753PjwGrV9jC |
| O37 | The shape screen, on a phone — F8-T3's device check | P1 | open | @verify | D39 | **RELEASED 2026-09-24. The condition it was held on is met: `F8`'s five BLOCKING notes are answered.** It was held because it asked the owner to confirm figures no document justified; the figures are now justified by running the shipped shaper — 120 minutes balanced produces exactly **22·5·22·5·22·5·22 with a 17-minute long break**, which is what this row asks to be read back. The owner's worked example is reconciled rather than ignored: `D43` makes the pom COUNT an input, and the owner ruled 2026-09-24 that break lengths stay OUTPUTS — *honour the poms and grow the breaks* — so a 17-minute long break where 10 was named is the ruled behaviour and not a defect. NOTE FOR THE SITTING: this row's Todoist task was closed 2026-09-24 at the owner's instruction, to be re-added at this gate; it needs re-adding. ORIGINALLY: HELD 2026-09-22 by D39, which halted F8 at T3 and re-gated it: this check asks the owner to confirm figures no document justifies. The owner's own worked example — 3 sprints over 120 minutes with a minimum 5 minute break and a 10 minute long break — cannot be produced by what was built, which yields one sprint of four poms with a 17-minute long break. It is released when F8's five BLOCKING notes are answered at its fresh gate. ORIGINAL CHECK, unchanged: Enter 120 and read back four poms of 22, three short breaks of 5 and a 17-minute long break finishing exactly two hours out. Then 45 (says it is under the minimum), then 10 (says no shape is possible rather than drawing an empty one), then change the preset at 180 where the presets actually differ. Close by confirming AppSettings is untouched. | td:6hVP756GHqxjFXgj |
| O38 | The two sentences on the shape screen are the agent's words, not the owner's | P2 | closed | — | F8 | CLOSED 2026-09-24. Both sentences are the owner's now, cleaned for accuracy rather than for taste. TWO DRAFTS WERE REFUSED ON THE CONTRACT BEFORE THE THIRD LANDED, and the refusals are the value here. (1) *\"Sprints include the Pomodoro (focus) as well as the break\"* contradicted `docs/specs/definitions.md`: the ratified word for a pom plus its break is CYCLE, a sprint is *the set of poms you run before the long break*, and that file exists because the owner's own earlier proposal — that a pom is *\"a full cycle of focus and break\"* — was REFUSED on five unanimous sources. Putting it on screen would have re-argued a settled ruling in the app's voice. The owner's next draft used `cycle` correctly. (2) *\"you can create a more custom cycle in the settings menu\"* would have walked the reader past `Save to settings`, which is ON THIS SCREEN in that state — `saveDetail` is non-nil whenever a shape exists. THE OWNER'S UNDERLYING POINT WAS RIGHT AND IS NOW PROVEN: `SettingsBounds` allows 1–120 minutes a block, below this screen's floors of 10 and 5, so a shorter sprint really is possible — just not through `Fit a sprint`. `theCopysClaimsAreTrueOfTheAppItDescribes` pins both claims against the things they claim about, and `O38-M1` reinstates the settings-menu wording and watches it go red. 658 tests pass. ORIGINALLY FILED AS: "No shape this short / The shortest shape these controls can make is 15 minutes" and "45 minutes fits 3 pomodoros. A full sprint needs 60 minutes." Drafted by the agent and constants in ShapeScreenCopy.swift, so changing them costs nothing. This is the app's voice at the one moment it tells somebody something mildly disappointing, which is where tone matters most. | td:6hVP75CrH6hWPFfj |
| O39 | Does the absorption preset control earn its place? | P2 | open | @Review | F8 | Ratified 2026-09-09 that the three presets ship in order to be TESTED. Measured: at 60 minutes all three return an identical shape, at 120 the spread is two focus minutes, at 180 it is 120 against 150 against 102. Arithmetic cannot say whether three hours feels better at 150 focus minutes or 102 — only use can. Without this row the control survives by default rather than by evidence, which is how surface accumulates. | td:6hVP75HxHXGMGhhC |
| O40 | The App Group capability, in the developer portal | P2 | open | @Review | F17 | **REVIEWED 2026-09-24, AND THE BLOCKING CHAIN IS SHORTER THAN IT WAS: `D30` IS RATIFIED.** The claims check out — no `.entitlements` file exists anywhere in the tree, and no code names an App Group identifier, so nothing is broken today and there is no dangling reference waiting to fail. The mode was `@?`, one of the seven `A20` counted; it is `@Review` now because there is a real decision here. WHAT IS STILL AHEAD OF IT: `F17` is item 14 of 14 in the v1.5 order, its plan says *plan written, awaiting the gate, no code has been written*, and it has not been gated. So the work is not due. AND A LIVE RISK IF IT IS DONE EARLY: adding an App Group capability REGENERATES THE PROVISIONING PROFILE, and that profile is currently working — it carried the TestFlight upload of 0.9.0 build 9 on 2026-09-24. Touching it days before a release, for a feature that has not been gated, risks disturbing something that works to enable something not yet built. THE RECOMMENDATION: do it at `F17`'s gate and not before. ORIGINALLY FILED AS: F17's complication needs one and F18 refuses one, so it is F17-only and F17 is last. The account and the app's current capabilities are provisioned — TestFlight proves that — but there are NO entitlements files in the tree at all and an App Group has never been added to the App ID or pulled into the profile. C2's territory and the owner's; no task or chore row covered it before this one. | td:6hVP75PwVq2v8WRC |
| O41 | D35's one-line amendment into SPEC.md | P0 | closed | @? | F8 | CLOSED 2026-09-15. SPEC.md line 5 now reads "Ends when: v1.5 is released, approved, and sent via TestFlight (D35)" and D35 is in the Amendments applied list, so AMENDMENT-BASELINE.txt's ratchet is back at zero and everyRatifiedSpecAmendmentIsApplied is green. Merged as 65240a7 in PR 41. APPLIED BY THE AGENT AT THE OWNER'S EXPLICIT INSTRUCTION, which is a waiver of the standing never-edit-SPEC.md rule and is recorded in that commit rather than done quietly; the rule stands. Until this landed, CLAUDE.md's v0.1-wins tiebreaker made the dead September 13 date authoritative by construction, six days after v1.5 replaced it. Found by the F8 review. |  |
| O42 | 00-status.md claims a generator that has never existed | P1 | closed | @Review | C28 | The file's header says "GENERATED by scripts/gen_status.py — do not edit by hand" and names a CI check. scripts/gen_status.py is absent from the working tree and from all 350 commits in every branch, and nothing in .github/, scripts/ or .githooks/ references it or 00-status.md. So the project's single status artifact is hand-written prose wearing a generated header — which is why it reported F8 as "no code written" for six days and nothing corrected it. Two honest fixes: write the generator, or strip the false header. The owner picks; the agent should not choose to keep a claim that is not true. CLOSED 2026-09-15 by C28: the generator now exists, and is enforced by make check-status, the pre-commit hook and CI, each proven by breaking it. Found by the F8 review. |  |
| O43 | F8-M7 and F8-M8 each name two different mutations | P2 | open | @Review | F8 | Commit 2fab614 defines F8-M7 as auto-start ON / engine.isRunning -> true and F8-M8 as auto-start OFF / state.kind -> .longBreak. ShapeScreenTests.swift:76 already uses F8-M7 for rendering nil as an empty shape and :111 uses F8-M8 for T1's two budgets. F8.md's table stops at F8-M5. The mutations were genuinely run — the commit records observed values — but neither ID is citable. F8.md:620 declines to open an M register deliberately and with its reasoning stated; the collision is what that choice costs. Same class as the O23/O24/O25 collisions this register already flags. | td:6hcWGxMmhH6fVjWC |
| O44 | OPEN.md and the register are lossy in OPPOSITE directions, and neither is a superset | P1 | closed | — | F8 | CLOSED 2026-09-24 by `D42`, which is the ruling this row asked for and did not have. NOT OVERRULED — RIGHT ON BOTH COUNTS: the prerequisite it named, that the register be complete first, was met by `C33` and `C34`, and the decision was the owner's and has now been made and numbered. Its measurement is what made the migration safe: 16 of OPEN.md's 35 O ids and 83 lines of prose existed nowhere else, so `C37` transcribed the 16 and left the prose outside the generated markers. ITS PROHIBITION WAS LIVE WHILE C37 WAS BUILT, and the artefact showed it — OPEN.md printed "SO: DO NOT GENERATE OPEN.md FROM THE REGISTER" inside a region generated from the register, which is how `C37`'s adversarial review found that no delta existed. FILED SAYING "OPEN.md is five items behind the register". THAT PREMISE WAS WRONG and the fix it implied would have destroyed data. Measured 2026-09-15: 16 of OPEN.md's 35 O-ids are absent from the register entirely (O2, O6, O7, O10, O11, O13, O14, O16, O17, O18, O19, O21, O22, O26, O27, O28 - all closed, all carrying the reasoning for how they closed), plus 83 lines of prose the register has no column for. One of those paragraphs records that a laggy button inflated the distraction tally, that the affected rows are still in the database, and that no capture surface exists to remove them - so any export covering that block over-reports. That is a caveat on O1 and it exists nowhere else. In the other direction the register had 15 rows truncated; C29 repaired them. SO: DO NOT GENERATE OPEN.md FROM THE REGISTER. The decision is the owner's: carry the 16 closed rows and the prose into the register and then generate, or keep OPEN.md as the narrative record and stop calling the register canonical for O rows. |  |
| O45 | Whether v1.5's amendment ledger belongs inside the baseline | P2 | closed | — | C32 | CLOSED 2026-09-24 by `D44`, ratified by the owner: *\"add the amendment ledger of 1.5 to the baseline.\"* `docs/specs/zenpom-v1.5.md` now carries `## Amendments applied` in the same shape and the same words as `SPEC.md`, the ratchet's two `WatchedBaseline` rows are identical, and `V15-AMENDMENTS-APPLIED.md` keeps the per-amendment evidence with its heading renamed so no second list can be read — `C32-M6` is the mutation for a parser that found a *mention* of that heading instead of the heading. THE EDIT WAS APPLIED RATHER THAN HANDED BACK, on the precedent the owner set on 2026-09-11 — *\"why am I re-editing? this feels like an extra step\"* — and the rule is intact: what makes an edit to a baseline legitimate is a ratified decision, not whose hands are on the keyboard. **THE MOVE ALSO EXPOSED A DEAD GUARD, WHICH IS THE MORE USEFUL FINDING: `A21`.** OPENED 2026-09-23 by C32. docs/specs/zenpom-v1.5.md is a ratified baseline and the agent may never edit it, so C32 put its amendment ledger and baseline count in two files BESIDE it — docs/specs/V15-AMENDMENTS-APPLIED.md and docs/specs/V15-AMENDMENT-BASELINE.txt — in the same relationship AMENDMENT-BASELINE.txt already has to SPEC.md. SPEC.md can carry its own '## Amendments applied' list inline because the OWNER edits SPEC.md. The owner may prefer the same shape for v1.5; that is an edit to a ratified baseline and needs a D<n> of its own, exactly as D40 was needed for D33. Only the owner can decide it, which is why it is here and not a proposal in the chore. | td:6hcWGxPxxvHfrVxj |
| O46 | The v1.5 ratchet still cannot see D33, the delta it was built for | P1 | open | @? | C32 | OPENED 2026-09-23 by C32's review. D41's second hole says nothing would ever have gone red for D33; after C32 there is a ledger, a baseline file and a ratchet and D33 STILL does not go red. Two causes. FIXED: D33 declares no document inside its Currently block, so it was judged against SPEC.md - the wrong baseline - until the declaration rule learned to read a delta's preamble, where D33 writes 'Owed because `docs/specs/zenpom-v1.5.md` is a ratified baseline'. NOT FIXABLE BY VERBATIM MATCHING: D33 amends an order TABLE and quotes it as a comma list of ids, every one under the twelve-character fragment floor, so there is no verbatim overlap to find; lowering the floor makes F8 match everywhere. MEASURED, NOT ARGUED: the pre-C31 tree was reconstructed from ac80738 and the finished detector run over it - v1.5 owed set {D31}, 'would this have gone red for D33?' = False. So D33 is in docs/specs/V15-AMENDMENTS-APPLIED.md because a person put it there, and the ledger rather than the instrument is what catches a paraphrasing delta. This is the D18 class docs/specs/AMENDMENT-BASELINE.txt already records. THE OWNER'S TO RULE, AND IT IS A CHOICE OF TWO: accept that a paraphrasing delta is caught by the ledger and not by the instrument, or ask for a different detector. | td:6hcWGxQvfGcvFV7C |
| O47 | A refused dedup read drops the wrist tap, and `Distraction.id` still has no uniqueness constraint | P1 | open | @Review | F7b | OPENED 2026-09-23 by `F7b`. `F7b` fixed the swallowed error: `try? context.fetch` mapped a refused read to "nothing found" and fell through to the insert, so a WatchConnectivity resend could become a second row for one press. The ruling shipped is DROP — nothing is inserted and `Outcome.unreadable` is returned — on the grounds that an over-count is permanent (no capture surface can delete a row, see `O44`'s note on the laggy button) while an under-count is merely absent. The owner may overrule; it is one line. The uniqueness constraint was DECLINED, not forgotten: SwiftData's `.unique` upserts rather than rejects, so a redelivered tap would overwrite an existing row's `note` with `nil` — STATED AS A CLAIM, NOT VERIFIED, because nothing here has run it — and adding a constraint to a shipped model with no `VersionedSchema` risks the store `O1` has never been read from. Taking it needs a `D<n>`, a versioned schema with an explicit de-dup migration, and an install-over-a-real-store check in `O2`'s shape. ALSO UNSETTLED, AND NOT THIS UNIT'S: a lost wrist tap is silent, while a refused tap on the phone gets an amber line — so `O15` cannot tell a tap lost to a refused read from one lost to WatchConnectivity. ADVERSARIAL REVIEW, SECOND PASS: TWO BLOCKING FINDINGS, BOTH REAL, NOTHING REFUTED. (1) The first build reached the refused-read branch through an injected closure `refusableFetch` on the production type, justified on the claim that a real ModelContext cannot be made to throw from fetch on demand. THE CLAIM WAS WRONG AND THE SUITE PAID FOR IT: every refused-read test drove the closure, so `return try context.fetch(descriptor)` - the only line the app runs - was never driven as a throwing call, and the reviewer reinstated the original defect one level deeper (`(try? context.fetch(descriptor)) ?? []`) with all 639 tests still green. THE SEAM IS DELETED. A real ModelContext throws when the open on-disk store's bytes are overwritten - NSCocoaErrorDomain 259, NSSQLiteErrorDomain 26 - which was established by running it, not argued; a container whose Schema omits Distraction was also probed and returns [] rather than throwing. Both tests now point a real context at a corrupted real store, the original bytes are restored and the store reopened through a fresh container so the ROW COUNT is read off the disk rather than recomputed. WatchTapInbox has one stored property again and no mutable production surface, which also closes the non-blocking finding that the seam was internal and injectable module-wide. (2) O47 was never written into docs/reviews/OPEN.md, which conventions-local.md defines as every outstanding item from every review in one table; the justification cited O44, but O44 forbids GENERATING OPEN.md from the register, not adding a row, and C32 wrote O45 and O46 into both files. The row is now in OPEN.md under 'Needs the owner'. ONE NON-BLOCKING FINDING WAS REFUTED, NOT TAKEN: logging the discarded error costs more than nothing - a grep over the app target returns zero hits, so there is no logging facility to add a line to and introducing one is new infrastructure inside a defect fix. The silence is recorded in this row instead. The `.failed` doc comment that claimed it 'is reported rather than swallowed' was corrected - the sole caller drops it. | td:6hcWGxQfmGxh9P8j |
| O48 | Whether the decisions register may carry a second `### Decisions` heading, and whether the status vocabulary is contract | P2 | closed | @? | C34 | RULED AND CLOSED 2026-09-24 by the owner: **language in `docs/conventions.md` is contract, not implementation.** It says *"one `##` per register"* and *"every register, one `##` each"*, so the owner-fields overlay could not be a second `## Decisions` heading however carefully that heading avoided the `(D)` suffix. THE IMPLEMENTATION CHANGED, NOT THE CONVENTION: the overlay is now `### Decisions — owner fields` inside the `D` section, after the generated region, and both `gen_status.parse_tables` and `check_register_rows` treat a `###` as a section boundary so its three-column rows are never checked against the `D` table's five-column header. The owner's wider ruling, recorded because it governs more than this row: the reviewing agent should have held to conventions.md rather than proposing a reading of it, and a change to conventions is reviewed carefully rather than arrived at through implementation. OPENED 2026-09-24 by `C34`'s adversarial review. TWO RULINGS `C34` MADE THAT MAY NOT BE THE AGENT'S TO MAKE, recorded as a field rather than as a paragraph in the chore. (1) `docs/conventions.md` Axis 2 says *"one `##` per register"* and `grep -n '^## Decisions' docs/plans/00-register.md` now returns two — `### Decisions — owner fields` (hand-maintained `P` and `TD`) and `### Decisions (D)` (generated). `D37` ruled the table generated and said nothing about where the owner's two fields live; the overlay's heading carries no `(D)` symbol on purpose, so no register is counted twice and no number is wrong, but the SHAPE is a reading of Axis 2 that the agent chose. The alternatives `C34` refused are in `docs/chores/C34.md` under *What was built · 2*. (2) `gen_status.CLOSED` gained `ratified` and `resolved` and deliberately did NOT gain `held`, so `O37` stays counted open — defensible as a no-change, but it is a vocabulary ruling, and Axis 2 pins only `proposed · ratified · rejected · superseded`. If a pinned vocabulary is contract rather than implementation, `resolved` and `held` owe a `D<n>`. Either answer is one line of code. |  |
| O49 | A wrong JSON type on a mirrored field still fails the whole page | P2 | open | @Review | F13 | `TodoistProjectDTO.color` and `TodoistTaskDTO.priority` are optional, which tolerates a MISSING key and an explicit null — verified — but NOT a wrong type: `"color": 4` or `"priority": "high"` raises `typeMismatch` and the whole page is refused, which presents as an empty picker. Both fields' doc comments claimed the broader tolerance and are corrected. `TodoistTaskDTO.Due` already has a hand-written lenient `init(from:)` for exactly this reason, so the fix is known and small. NOT TAKEN INSIDE `F13`: it is new tolerance on a shipped decode path, and folding that into a review pass is how a decode change arrives without anybody deciding it. Todoist v1 sends both as the documented types today, so this is latent. Found by `F13`'s adversarial review. | td:6hcWGxRFmqCj8MpC |
| O50 | The owner's real account data is committed to the tree | P2 | open | @Review | F13 | Four live Todoist task ids and their titles in `ZenTomatoTests/TodoistPriorityTests.swift`, the priority tally `{1: 9, 2: 11, 3: 22, 4: 8}`, and an archived project's name in this register and in `docs/reviews/F13.md`. It is there for a good reason — the fixture is the owner's own account, which is what makes the priority direction evidence rather than a reading of documentation — and gitleaks is clean because none of it is a credential. But it is personal data in a licensed repository and it was committed without being asked about. The owner's call; removing it costs one edit and weakens the fixture to a synthetic one. Found by `F13`'s adversarial review. | td:6hcWGxcJjHvvRWwj |
| O51 | Music did not resume after the break, with a playlist selected | P2 | open | @verify | F7 | Observed by the owner 2026-09-24 on build 202609241512, during `O33`'s device check and unrelated to it: *"music is not playing after the break. Even though classical concentration is selected. More testing maybe required."* **RECORDED AS AN OBSERVATION, NOT A DEFECT, and the owner said so first** — one sitting, no repeat, and the sitting was about something else, so the state the app was in is not fully known. What would turn it into a defect: run one focus block to its end with a playlist chosen, let the short break run out, and say whether music resumes on the next focus block; then the same with the phone locked. `F4`'s resume path is the suspect and `A18` already records a blocking `playbackState` read on the main thread in that area, so this may not be new. | — |

## Decisions (D)

**GENERATED FROM `docs/plans/00-deltas.md` SINCE `C34`, per `D37` (ratified 2026-09-22).** That file
defined 43 deltas while this table held exactly one row, `D30`, so `docs/plans/00-status.md` printed
`Decisions (D) | 1` on a page CI keeps current. Neither source was wrong about itself and neither was
complete — the second intake path `docs/conventions.md` forbids, already producing a wrong number.
Every delta defined there gets a row here: *a register that omits the largest register in the project
is not a register.* `P` and `TD` come from `### Decisions — owner fields` above; every other cell comes
from the delta's own `## D<n> — <title>` heading and the status line beneath it.

**Everything between the two markers is overwritten by the next `python3 scripts/gen_status.py`.**
A hand edit inside the region fails `make check-status` with a diff naming this file; a deleted marker
makes the generator refuse to run at all and write nothing.

<!-- BEGIN GENERATED: decisions — scripts/gen_status.py writes everything between these markers from docs/plans/00-deltas.md (D37). Do not edit by hand: `make check-status` fails, and the next run reverts it. On a merge conflict inside this region, take either side and re-run `make status` — the rows are emitted sorted and deterministic, so the two resolutions are identical. -->
| ID | Title | P | Status | TD |
|---|---|---|---|---|
| D1 | Minimum iOS 18.0 → 26.0 | — | ratified |  |
| D2 | watchOS companion moves from Phase 2 into v0.1 as F7 | — | ratified |  |
| D3 | F2 alerting: AlarmKit primary, Live Activity promoted to required | — | ratified |  |
| D4 | End-of-pomodoro sequence made explicit | — | ratified |  |
| D5 | Todoist API version (verification result, recorded for the file) | — | ratified |  |
| D6 | ~~Secrets live in `.env`~~ **REJECTED 2026-08-22. Superseded by D6b.** | — | rejected |  |
| D6b | Build settings use Xcode's own `.xcconfig` mechanism | — | ratified |  |
| D7 | Fence exception: the countdown numeral may state a raw point size | — | ratified |  |
| D8 | A launch-screen colour set is added to the asset catalog | — | ratified |  |
| D9 | ~~Shipping invalidates the F3 authentication decision~~ **RESOLVED by D18, 2026-08-23** | — | resolved |  |
| D10 | F2 gains the settings screen | — | ratified |  |
| D11 | Completed tasks are recorded and exported | — | ratified |  |
| D12 | The Live Activity has no controls | — | ratified |  |
| D13 | One exit, and it costs a sentence | — | ratified |  |
| D14 | Stopping mid-block shows one sheet, not two | — | ratified |  |
| D15 | What the Rhodia export contains, and what stays out of the counts | — | ratified |  |
| D16 | Designed so bi-directional sync is *possible* later, without preparing for it now | — | ratified |  |
| D17 | A session plan: several Todoist items, in the order you will work them | — | ratified |  |
| D18 | Todoist authenticates with a personal API token. D9 is resolved. | — | ratified |  |
| D19 | Three decisions taken at the F4 gate | — | ratified |  |
| D20 | A Stop control for music, beside skip | — | ratified |  |
| D21 | A completion records whether the task was recurring | — | ratified |  |
| D21b | A task completed during a sprint does not come back into it | — | ratified |  |
| D22 | A block records which project it was for, and the export labels it live | — | ratified |  |
| D23 | The music picker gets a search field. No spec text changes. | — | ratified |  |
| D24 | The alarm sound can be chosen. `AppSettings` gains a seventh field. | — | ratified |  |
| D25 | Music can be switched on during a break | — | ratified |  |
| D26 | A ringing alarm can be silenced from inside the app | — | ratified |  |
| D27 | Settings are read-only while a block is running | — | ratified |  |
| D28 | The alert sound can be previewed from Settings | — | ratified |  |
| D29 | A locked phone is somebody being there | — | ratified |  |
| D30 | A watch-face complication | P0 | ratified | td:6hPjPm4FcP3gJ4Cj |
| D31 | C22 is struck; the licence question was answered on 2026-08-27 | — | ratified |  |
| D32 | A shape is stored, in one file, outside SwiftData | — | ratified |  |
| D33 | v1.5 admits two more units, and the order is restated | — | ratified |  |
| D34 | The shape store speaks to a medium, to buy sync-readiness now | — | ratified |  |
| D35 | v1.5 ends at TestFlight, and the September 13 date is formally dead | — | ratified |  |
| D36 | Agent findings get a register of their own | — | ratified |  |
| D37 | The register's decisions are generated, not maintained | — | ratified |  |
| D38 | The missing registers open now | — | ratified |  |
| D39 | F8 is halted at T3 and re-gated | — | ratified |  |
| D40 | D33 is applied, and CLAUDE.md stops enumerating the order | — | ratified |  |
| D41 | The amendment ratchet learns both spellings, and gains a second baseline | — | ratified |  |
| D42 | The register is authoritative and `OPEN.md` is a view | — | ratified |  |
| D43 | The shape screen lets you set the number of pomodoros | — | ratified |  |
| D44 | v1.5's amendment ledger moves inside the baseline | — | ratified |  |
| D45 | A silent alarm and a haptic on the watch when the phone's sound is off | — | proposed |  |
| D46 | The watch fires the same controls as the phone | — | proposed |  |
| D47 | ~~The shape store is a single `Codable` value in `UserDefaults`~~ | — | rejected |  |
<!-- END GENERATED: decisions -->

### Decisions — owner fields

**Hand-maintained, and the only hand-maintained part of the decisions register.** `P` and a Todoist
link are the owner's fields; `docs/plans/00-deltas.md` has no column for either, so regenerating the
table below from it would delete them. `scripts/gen_status.py` merges these two cells into the
generated row for each ID named here, and **refuses to run** if this table names a `D<n>` that
`00-deltas.md` does not define — a Todoist id keyed to a decision that does not exist is data about
to be lost in silence, which is `C33`'s dropped-`TD` defect one file over.

**THE HEADING DELIBERATELY DOES NOT END IN `(D)`.** `register_rows()` collects a section only when its
heading ends in a parenthesised symbol, so this table is not counted as a second decisions register and
`D30` is not counted twice. **The count itself is not repeated here**: it is on
`docs/plans/00-status.md`, which is generated, and a number copied into prose beside its own source is
the defect the `## Chores (C)` preamble records two sections down.

| ID | P | TD |
|---|---|---|
| D30 | P0 | td:6hPjPm4FcP3gJ4Cj |

## Chores (C)

**SOME ROWS BELOW CARRY THE LITERAL VALUE `unknown` IN `Status`, AND THIS PARAGRAPH NO LONGER SAYS
WHICH OR HOW MANY.** The set is `UNKNOWN_LEGACY` in `scripts/check_register_rows.py`, which is the
one definition `R5` enforces, and the counts are on `docs/plans/00-status.md`'s `Chores` line, which
is generated. Those rows are what an extractor with no status to copy wrote down, and they are left
for the owner to rule on rather than guessed at: establishing each would mean reading that chore's
own file and git history, which is research and not transcription.

**THE LIST IS GONE RATHER THAN CORRECTED, AND THAT IS THE POINT.** Until `C34`'s own adversarial
review this paragraph read *"EIGHT OF THE 26 ROWS"* and named eight ids while the table held 29 rows
and nine `unknown` — the id it omitted was `C15`, the single row `C36` existed for — so the
register's own statement of `R5`'s exception set contradicted the code that enforces it, with every
gate green. `C33`'s review had already caught the identical defect in this same file as an 80-vs-81
count, and `C34` corrected the paragraph below it by hand while leaving this one stale. A list copied
beside a table is a second intake path and drifts like one; the remedy is the one `D37` applied to
the decisions register one section up — stop maintaining it twice.

**THIS PARAGRAPH READ *"No chore in ZenPom carries a status field. Every row below is `unknown`"*
UNTIL 2026-09-23,** when `C33`'s adversarial review caught it. It was false for 18 of 26 rows — and
the same diff that added `closed`/`open` to `C1`, `C13`, `C15`, `C21`, `C23` and `C24` added them
directly beneath the two lines saying no such row existed, without reading them. **`docs/conventions.md`
Axis 2's mandatory field was PRESENT on every one of those rows; on some of them its VALUE was outside
any pinned vocabulary,** which is a different defect and needs a different check — see the note under
the table. Both sentences are deliberately written without counts: they record what was true on
2026-09-23 and nothing re-reads them.

**`unknown` IS A VALUE HERE AND IT IS ALSO WHAT AN EMPTY CELL BECOMES.** `scripts/gen_status.py:174`
maps an absent `Status` to the string `unknown` as well, so the `Chores` line of
`docs/plans/00-status.md` **cannot distinguish a row that says `unknown` from a row that says
nothing.** Today every such row is the first kind; no chore row has an empty cell. A `C34`
check written to fire on an empty status cell would therefore fire on **zero** of the rows it was
written for. The check these rows need is **enum membership** — `Status` must be one of a pinned
vocabulary, and `unknown` is not in it — which is `C33.md`'s row shape 14.

**THAT PREDICTION WAS RIGHT AND `C34` TOOK THE ADVICE.** `scripts/check_register_rows.py` ships both
rules rather than one: **R4** fires on an empty `Status` cell and fires on **zero** rows of this file
today, so its only evidence is mutation `C34-M8`; **R5** is the enum-membership rule, and `unknown` is
not a member of its vocabulary. **R5's exception set is `UNKNOWN_LEGACY` in that file and is written
nowhere else** — **a set of ids and not a count**, because a count permits the same number of
*different* rows to be swapped in. Each owner ruling deletes an id, so the set can only shrink.
`C34-M9` mutates `C26` to `unknown` to prove the exception is keyed to the id rather than a blanket
allowance, and `C34-M18` deletes an id from the set to prove the set is read at all.

| ID | Title | P | Status | Why | TD |
|---|---|---|---|---|---|
| C1 | Create the GitHub repo, add the GPL-3.0 LICENSE, turn on branch protection for `main` | P0 | closed | Title from `docs/specs/SPEC.md:50`, which names it exactly, so this row is NOT a reconstruction and carries no banner. `docs/plans/00-deltas.md:224` states "**C1** (repo, LICENSE, branch protection) — **done**", and `O16` closed against `C1` on 2026-08-24 with "PR required, CI required, linear history, rebase-only, no force pushes". **THE EVIDENCE IS THOSE TWO SENTENCES AND NOTHING ELSE.** This cell also cited commits 927f8d6, 1c5271b and ee82353 until 2026-09-23, when `C33`'s second review pass read their subjects: *"record the five owner items today generated"*, *"link the five new owner items to Todoist"* and *"the owner's review log for PR 35"* — `C1`-scoped bookkeeping, and not repo creation, not the LICENSE, not branch protection. A `git log --grep` on the scope was presented as corroboration in a row whose own first clause is that it is NOT a reconstruction; the three SHAs are withdrawn rather than reinterpreted. The repository's own creation has no commit to cite, which is the point. The branch-protection half is `H31`, which no command here can check. |  |
| C6 | Correction pass | P1 | closed |  |  |
| C8 | Turn on Developer Mode on the watch, and provision it | P1 | closed |  |  |
| C9 | TestFlight runbook | P1 | unknown | Owner: both. The agent can write the runbook and prepare the build; only the owner can hold the Apple Develope | td:6hPjPm8wMpv6Jc7j |
| C10 | GPL-3.0 and the App Store: a decision, not a task | P1 | unknown | Owner: the owner. It is a licensing decision about their own work. | td:6hPjPm9qhGwvjW7j |
| C11 | An explicit App ID with MusicKit (O14) | P1 | closed |  |  |
| C12 | the configuration that ships is now built by the gate | P1 | unknown | Owner: the agent. No user-visible change. | td:6hPjPmJGQhpvJw5C |
| C13 | 0.9.0 (2), the first TestFlight upload | P1 | closed | RECONSTRUCTED 2026-09-23 by C33 from git log; not contemporaneous. No file, no spec entry. Two commits: `528c9df` *chore(C13): 0.9.0 (2) for the first TestFlight upload* and `c4358c2` *chore(C13): remove a swept-up zip from the tree, and widen the ignore*. Closed because the build shipped and `O3`, `O4` and `O5` were verified on it; corroborated by `docs/reviews/OPEN.md:60`, which cites `C13` as the From for `O24`'s dual-licensing-report ruling. |  |
| C14 | three findings from the first real sprint on ZenPom 0.9.0 (2) | P1 | unknown | Owner: both. The agent triaged; two of the three need the owner's ratification before any code is written. | td:6hPjPmJRwGXHJ9WC |
| C15 | Draft one of the v1.1 verbiage, and a factual check against the app | P2 | unknown | RECONSTRUCTED 2026-09-23 by C33 from git log; not contemporaneous. No file, no spec entry, **one commit only** — `b0627e7` *docs(C15): draft one of the v1.1 verbiage, and a factual check against the app*. **This is the weakest of the six and the status is a reading, not a record:** the commit says "draft one" and a draft landing is the deliverable, so `closed`; if the owner disagrees the honest value is `unknown`, which the generator renders as a question rather than a guess. STATUS CORRECTED TO `unknown` BEFORE MERGE. An earlier draft read `closed`, inferred from a single commit whose own subject says *"draft one"*, while the cell conceded that *"the status is a reading, not a record"*. Eight other chores in this table are left `unknown` rather than guessed; this row was the one exception and the inconsistency was the finding. A status is a field that can be checked, and an inference is not one. |  |
| C16 | the app is killed by the watchdog reading whether music is playing | P1 | unknown | Owner: the agent. A defect, not a feature. No delta. | td:6hPjPmQWJfv9536C |
| C18 | GPL-3.0 for the repository, and an App Store exception for the app | P1 | unknown | Owner: both. The agent drafts the files; only the owner can license their own work. | td:6hPjPmRVXw9jMrPj |
| C19 | the alarm that could not be silenced | P1 | unknown | Owner: the agent. Diagnosis only. No fix is proposed here, because the mechanism has not been established and | td:6hPjPmcWqr8g39Hj |
| C20 | the alarm that sounded with sound off | P1 | unknown | Owner: both. The agent narrows it; one short device test decides it. | td:6hPjPmj2fvvqmWqj |
| C21 | zenpom Focus runbook | P1 | open | Sourced from the ratified v1.5 spec, `docs/specs/zenpom-v1.5.md:77` and :104, and from a live file, `docs/chores/C21.md` — so no reconstruction banner. **OPEN, not closed:** `O36` records that `C21` shipped written but NOT TRIED, and its own header says so. Step 6 is the verification — one sprint with the Focus on, that sprint's external-distraction count read against a typical one. A chore whose own file says it was never tried is not closed. |  |
| C23 | the v1.5 spec, ratified | P0 | closed | RECONSTRUCTED 2026-09-23 by C33 from git log; not contemporaneous. No file. Five commits, all `docs(C23)`: `e58f971` *the v1.5 spec is ratified*, `55dc3bf` *v1.5 rewritten against the gate of 2026-09-09*, `8546c92` *D31 ratified — C22 is struck and F11 is unblocked*, `92c0dbc` *index D31, and correct a count that was stale by nine*, `1d9f853` *the definitions are ratified, and F9-T1 is met*. Closed because its output, `docs/specs/zenpom-v1.5.md`, is in the tree and is a ratified baseline. **`55dc3bf`'s subject was misquoted as *the v1.5 spec rewritten against the gate of 2026-09-09* until 2026-09-23** — the word *spec* is not in it — and is now verbatim; `C33`'s second review pass caught it against this project's standing quote-exactly rule. The other four were checked the same way and are verbatim. |  |
| C24 | plans and deltas for the eight unplanned v1.5 features | P0 | closed | RECONSTRUCTED 2026-09-23 by C33 from git log; not contemporaneous. No file. Five commits, all `docs(C24)`: `d13bfae` *plans for the eight unplanned v1.5 features*, `a6bc966` *one engine seam, a watch-side intent, and Todoist made undoable*, `b2465ed` *propose the shape-store delta and the membership delta*, `dd93d17` and `150b5de` *D32 and D33 ratified, and the capture fence is F14's*. Closed because the plans `F10` and `F14`-`F19` exist in `docs/plans/` and `D32` and `D33` are ratified. |  |
| C26 | ZenTomato is still the app's name where the store looks | P0 | open | **2026-09-24, EVIDENCE THAT NARROWS `C26-T1` WITHOUT ANSWERING IT: an archive uploaded to TestFlight and exported for release testing, with `CFBundleName` still reading ZenTomato.** Version 0.9.0 build 9, identifier `com.martingleason.ZenTomato`, exported as `ZenTomato.ipa`, method `release-testing`, automatic signing, team KH6NBQRZBY — read from the export's own `DistributionSummary.plist` and `ExportOptions.plist`. **WHAT THAT RULES OUT:** upload-time validation does not object to the name, so the rejection was not a binary-validation failure and `C26-T2` is not urgent. **WHAT IT DOES NOT RULE OUT:** App Review and the store listing, which are read by people after upload succeeds — the App Store Connect app name remains the most likely target and is `C26-T3`, the owner's alone. A rejection is still pending; the owner is waiting on it. THE EXPORT ALSO LANDED AT `Config/ZenPom 2026-09-24 17-21-22/`, a 1.7 MB signed `.ipa` one `git add -A` from the repository and beside the git-ignored `Secrets.xcconfig`; `.gitignore` now covers archives and exports, because the same class of file reached `main` twice already (`C13`, and this morning's zip). 2026-09-24: `C26-T2`'s PREREQUISITE IS DONE, and the chore did not see it. `PRODUCT_NAME: ZenPom` renames the bundle on disk from `ZenTomato.app` to `ZenPom.app`, and THREE SCRIPTS NAMED THAT FILE LITERALLY — `install-device.sh`, `check-musickit-entitlement.sh`, `check-watch-provisioning.sh`. The rename would have broken all three, and install-device's failure would have read *the build succeeded but produced no .app bundle*, the exact misleading message `C38` had just been fixed to stop printing. All three discover the bundle now, so the rename is a one-line change instead of a four-file one. Names re-read FROM THE BUILT ARTEFACT, not from project.yml: CFBundleName `ZenTomato`, CFBundleDisplayName `ZenPom`, identifier `com.martingleason.ZenTomato`. `docs/handoffs/xcode-and-the-store.md` carries the plan and the zero-drift handoff for Claude in Xcode, whose first rule is that the `.xcodeproj` is GENERATED and git-ignored — a setting changed in Xcode's inspector is lost at the next `make generate`, which runs as a dependency of test, device, check-release and ci. **`C26-T1` STILL GATES EVERYTHING:** nobody has read what the rejection named, and its four possible answers point at four different places, one of which is a different chore with a data migration in it. ORIGINALLY FILED AS: The App Store rejected ZenTomato. The display name is ALREADY ZenPom on all three targets, so the rejection is not about the home screen and a find-and-replace over 156 files is the wrong response. CFBundleName still expands to the target name, and the App Store Connect record is portal work. DO NOT change the bundle identifier: it is an identity, not a name, and a new one means a new container, which orphans the SwiftData store holding the distraction log — the data O1 needs before O1 has ever been run. docs/chores/C26.md. | td:6hVP9fJ3fh5528rj |
| C27 | the standing documents, after the rescope | P1 | closed | CLOSED 2026-09-15, merged as PR 41. CLAUDE.md, conventions-local.md and the adversarial-reviewer brief now describe v1.5 rather than the v0.1 fence, and D35 reached SPEC.md so the amendment ratchet is back at zero. NOTE: this row was closed once already and the edit was lost - reverted by a git checkout cleaning up mutation C28-M2, which discarded every uncommitted register change rather than the one the mutation made. | td:6hVP9fJ3fh5528rj |
| C28 | the status generator that never existed | P1 | closed | CLOSED 2026-09-15. 00-status.md claimed "GENERATED by scripts/gen_status.py" for the life of the project while that script existed in no branch and no commit, so the page was hand-written prose nothing could correct - it reported F8 as having no code for six days, and 1 decision against 36. The generator now exists and is enforced at three surfaces: make check-status, pre-commit, and CI. Four tests, three mutations run against the shipped code and caught. Closes O42. docs/chores/C28.md. |  |
| C29 | the register lost 15 rows' text to a truncating extractor | P1 | closed | CLOSED 2026-09-15. 15 of 43 register rows had their Why cut mid-word at ~110 chars by the upstream extract_zenpom.py; O33 had lost 91% of its text and O30 63%. Repaired from OPEN.md, which held the full text. MATCHED ON TITLE, NOT ID: the first attempt matched on ID alone and assigned the struck bell-files row to O23, because OPEN.md carries two O23s, two O24s and two O25s - the collision this register already flags. Also corrected O44, whose premise was wrong, and closed C27, whose earlier close had been silently reverted. docs/chores/C29.md. |  |
| C30 | the blockers for v1.5, written down before anything is generated | P1 | closed | CLOSED 2026-09-16. Two parallel adversarial reviews plus a condensing pass: 59 raw claims, 16 distinct sites, every survivor re-verified by running it. Findings, recommendations and alternatives in docs/handoffs/blockersfor1_5.md, plus five candidate learnings for Turing 2.0. SIX DECISIONS AWAIT THE OWNER and are listed there; none is ratified by this chore. Also corrected turing-v2.md section 7, which carried the drifted-five-items premise C29 disproved and is the first document a resuming session reads. |  |
| C31 | the six rulings of 2026-09-22, and D33 applied at last | P0 | closed | CLOSED 2026-09-22. The owner ruled on six decisions from C30's handoff; each is recorded as a delta, D36-D41. D33 — ratified 2026-09-09 and never applied — reached docs/specs/zenpom-v1.5.md, which now states thirteen units over fourteen positions; CLAUDE.md stops enumerating the order and points at the spec. Fourteen lines carrying a stale item-number citation — across ten plan files (F8, F10-F18) and the chore plan docs/chores/C21.md — stopped citing a position at all, because a position in an order is a live relationship and conventions.md says those belong in a register rather than a sentence. FOURTEEN LINES, FIFTEEN NUMBERS: docs/plans/F16.md carries two in one sentence. One citation is deliberately left, in the closed chore plan docs/chores/C27.md, which is a historical record of what was true when it closed. F8 is halted at T3 and re-gated per D39 and O37 is held. THE EDIT TO zenpom-v1.5.md IS A WAIVER OF THE STANDING NEVER-EDIT-A-BASELINE RULE. The authorisation is D40, ratified by the owner 2026-09-22, whose own first clause is that applying D33 "edits a ratified baseline, which is the owner's to authorise and no one else's"; the commit carrying this row states the waiver and cites that ratification, the way 65240a7 did for the D35 edit to SPEC.md. The rule stands — nothing here licenses the next baseline edit. THE FOUR EDITS TO THE BASELINE ARE D33'S AND D40'S TEXT ONLY — the unit count, the `and D33` in the amendment trail, the order table, and D40's own legend sentence — AND NOTHING ELSE: an earlier draft added a parenthetical and a two-sentence legend that neither delta proposes, and they were reverted before commit, because a waiver reaches the ratified text and not the agent's gloss on it. The backfills this unit only RULES ON are C32 (the ratchet), C33 (A/M/H/RR rows) and C34 (generated decisions), each of which now has a row of its own below. |  |
| C32 | the amendment ratchet learns both spellings, and gains a second baseline | P0 | closed | CLOSED 2026-09-23, implementing D41. THE DETECTOR NOW READS BOTH SPELLINGS of the marker — two literals, earliest position wins, not a regex, because the block is sliced from the marker's upper bound and a literal's consumed length is fixed; D31-D34 were NOT edited, which is the ruling. THE SPEC.md OWED SET IS UNCHANGED at {D4, D17} before and after, verified by running, so teaching the comma form is safe at a baseline of zero. A SECOND BASELINE IS WATCHED: docs/specs/zenpom-v1.5.md, with two new artifacts BESIDE it rather than inside it, because the agent may never edit a ratified baseline — docs/specs/V15-AMENDMENT-BASELINE.txt (starting number 0, DERIVED BY RUNNING) and docs/specs/V15-AMENDMENTS-APPLIED.md (D31, D33). Which baseline a delta owes is read from a declaration the deltas already write - the first backticked document name in the first paragraph of the block, and when there is none, the delta's PREAMBLE, where D33 and D35 write theirs. A SECOND ADVERSARIAL REVIEW RETURNED TWO BLOCKING FINDINGS AND BOTH WERE REAL; nothing was refuted. (1) The declaration rule SILENTLY NARROWED the SPEC.md ratchet - an unresolvable declaration returned nil, dropping a delta that names CLAUDE.md while quoting live SPEC.md text, and the suite stayed green because the existing proof asserts the marker SLICE and not the judgement downstream of it. A declaration now only REDIRECTS: unresolvable falls back to SPEC.md as before C32, pinned by anUnwatchedDeclarationIsStillJudgedAgainstSpec and mutation C32-M7. So this row's earlier claim that the D40 luck-dependence was CLOSED is withdrawn - it is narrowed, and closing it needs CLAUDE.md watched, which it is not. (2) The v1.5 ratchet STILL DOES NOT DETECT D33, the delta D41's second hole exists for. Half fixed by the preamble rule (D33 was judged against SPEC.md, the wrong baseline); the other half is not fixable by verbatim matching, because D33 amends an order TABLE and quotes it as a comma list of ids all below the twelve-character fragment floor. MEASURED BY RECONSTRUCTING THE PRE-C31 TREE FROM ac80738 AND RUNNING THE FINISHED DETECTOR OVER IT: 'would this have gone red for D33?' = False. That residual is O46, and it is written into V15-AMENDMENT-BASELINE.txt and V15-AMENDMENTS-APPLIED.md rather than left to be inferred from a green run. Four non-blocking findings were also taken: last-path-component resolution instead of hasSuffix (C32-M9), the comma form's slice asserted as a string and not merely found (C32-M10), the absent-count message rewritten to say the digits and 'deltas' must be ADJACENT, and D32's colon-against-a-table-pipe added to AMENDMENT-BASELINE.txt's quote-exactly warning beside D18. THE ARCHITECT'S PREDICTION THAT v1.5 WOULD OWE NOTHING WAS WRONG: D31 IS owed, on the incidental fragment `docs/plans/parked.md`, so the ledger is load-bearing today and V15-AMENDMENT-BASELINE.txt says so. A COUNT ASSERTION now pins the index sentence to the defined count and FAILS ON ABSENCE. TEN mutations run against the shipped code and caught, C32-M1 to C32-M10 in docs/chores/C32.md, with M1-M6 RE-APPLIED AND RE-RUN after the review's fixes rather than carried forward; C32-M6 was not in the plan and found a real defect — appliedAmendments matched '## Amendments applied' anywhere in the file, so a document mentioning the heading in prose had its list read from the mention; the parser now matches at a line start. 628 tests before, 633 after the build, 635 after the review. docs/chores/C32.md. Opens O45 and O46. |  |
| C33 | the missing registers, backfilled — A, M, H and RR | P0 | open | OPENED 2026-09-22 by D36 and D38, which are the rulings. BUILT 2026-09-23 and NOT YET MERGED, which is why this row is `open`: two adversarial review passes have run, the first returning four blocking and seven non-blocking findings and the second two blocking and five non-blocking, and every finding is addressed in the sections below or refuted in `docs/chores/C33.md`. This row said *"this row is the work and none of it is done"* until the second pass caught it standing beside a generated page that already read *built*. Opens `## Agent items (A)` and carries all eighteen rows A1-A18 across from docs/reviews/OPEN.md:172-189, the twelve closed ones included, because a register of open rows only is a to-do list. Opens `## Mutations (M)` and `## Hooks (H)` WITH FULL BACKFILL — every mutation ID in use and every mechanism that actually enforces something, each traced spec invariant -> mechanism -> owning task; `## Risks (RR)` opens with seed rows, since it has no backlog to recover and a backfill would be invention. THE TWO SPELLINGS ARE THIS CHORE'S FIRST PROBLEM: this row was opened saying the tree holds 23 distinct bare M<n> tokens and 52 distinct feature-scoped ones (F8-M1 … F19-M4), which are D38's figures. CORRECTED 2026-09-23 BY THIS CHORE'S OWN DELIVERABLE, and the correction lives in `## Mutations (M)`'s preamble with the commands: it is 22 bare, not 23 — D38's enumeration counts M34, which is not an allocated ID — and 81 scoped, not 52, because D38's command uses an F-only character class and cannot see C28's three or C32's thirteen, F7b has merged since, three F8 IDs exist only in a commit body, and C33 allocated four of its own. THE 81 IS 74 IN-TREE + 3 COMMIT-BODY + 4 OF THIS CHORE'S OWN, which is the arithmetic in `## Mutations (M)`'s preamble, in `scripts/gen_status.py:76` and in `scripts/tests/run-script-tests.sh:604`; THIS CELL SAID 80 UNTIL 2026-09-23, the figure from before C33-M4 was allocated, and the review caught it contradicting all three. The two figures are left in this row rather than overwritten, because this row records what was believed when the chore opened; the register must not contradict itself in one read, so it says which sentence is current. Reconciling the two spellings is step one. The handoff's companion figure of fifteen enforcement mechanisms is NOT carried into either document: no command has been shown to reproduce fifteen, so counting the mechanisms is the first work of this backfill rather than an input to it. A half-filled section must not be opened before the backfill exists, or the generated page's word goes behind work nobody has done. |  |
| C34 | the register's Decisions section, generated rather than maintained | P0 | open | OPENED 2026-09-22 by D37, which is the ruling; this row is the work. BUILT 2026-09-23 and awaiting the adversarial review, so this row stays open until it merges: the Decisions section is generated from 00-deltas.md between a marker pair, all 43 deltas carry a row, the owner's P and TD survive by merge from a hand-maintained overlay, and scripts/check_register_rows.py enforces nine rules and a floor at pre-commit and in CI. Sixteen mutations were run against the shipped code and each named check was seen to fail; docs/chores/C34.md carries the output. The `held` question this row reserved is RULED: held is outstanding work parked on the owner and stays counted open, pinned by C34-M13, while ratified and resolved join CLOSED, pinned by C34-M12. `## Decisions (D)` in this file holds exactly one row, D30, while docs/plans/00-deltas.md defines 43, so 00-status.md prints "Decisions (D) 1" on a page CI keeps current. The section becomes generated from 00-deltas.md by scripts/gen_status.py — neither hand-maintained nor retired, because a register that omits the largest register in the project is not a register. The refused alternative, so it is not re-proposed: retire the section and point the reader at 00-deltas.md. Mutations proven against the shipped script, as C28 did for the generator itself. ALSO IN SCOPE, found by C31's review: `held` — O37's status since D39 — is not in the generator's CLOSED set, so the page prints it verbatim and counts the row as open, which is correct today but was never a decided vocabulary. Whether `held` earns a bucket of its own is this chore's to rule, and a generator change owes a mutation proven against the shipped script; C31 declined to make it unrecorded. |  |
| C35 | sweep the test suite for the guessed-yield idiom | P2 | open | Owner: the agent. No user-visible change. `MusicCoordinator.awaitPendingPlaybackRead()`'s doc comment says the `await Task.yield()` idiom "already exists in these tests", and `F4g` proved what it costs: a test vacuous on one machine and red on another, from the same cause. F4g fixed the two in MusicSkipVisibilityTests. The rest of the suite is unaudited. Each site needs a helper that awaits the actual task, or a stated reason the hop count is stable. |  |
| C36 | the one row C33 left a cell short | P1 | closed |  CLOSED 2026-09-24. C33 opened four registers and repaired ten malformed rows, and left one behind: C15 carried five fields against a six-column header. THE CAUSE WAS THE FIX. Correcting C15's status from closed to unknown used an rstrip whose argument was a space and a pipe, which stripped the trailing pipe AND the empty TD cell with it, so the row lost its last column. Exactly the class of defect C33 existed to remove, introduced by C33, in the same edit that corrected a different defect in the same row. The column-count audit in C33's own commit reported zero malformed rows because it ran BEFORE that edit. Found by re-auditing main after the merge rather than by trusting the commit. C34's validator must run in CI, not once by hand. AND THIS ROW WAS ITSELF MALFORMED, by the other of the two defect classes it describes: the quoted rstrip argument contained a literal pipe, which split the cell, so the row documenting a malformed-row fix was malformed. REWRITTEN 2026-09-24 in C34's branch, and the first attempt is the lesson: escaping it as a backslash-pipe DID NOT WORK, because gen_status.py splits a row with a naive split on the pipe character and never honours the markdown escape. A cell cannot contain a pipe at all, escaped or not, and C34's validator must say so rather than advising an escape that does nothing. SUPERSEDED BY C34, WHICH FIXED THE CAUSE RATHER THAN DOCUMENTING IT: gen_status.py and check_register_rows.py were splitting rows with two different rules, the generator on a bare pipe and the validator on an unescaped one, so an escaped pipe passed the validator and shifted every later cell one column left on the page. Both now share one tokenizer, an escaped pipe round-trips, and the validator's advice to write it escaped is true. C34-M16 is the mutation that pins it; C34-M6's own row carries an escaped pipe and reads back correctly. THREE UNITS IN A ROW have now left a broken row in the file they were repairing - C33 ten, C36 one, C36's own row one - which is the case for C34's validator stated by events rather than by argument. |  |
| C37 | generate docs/reviews/OPEN.md from the register | P1 | closed |  CLOSED 2026-09-24. OPEN.md's two tables are generated from the register between marker pairs; its 83 lines of prose are outside them and untouched. THE ROW-BY-ROW DIFF IS PUBLISHED AND IT IS A CHECK, NOT A PARAGRAPH: `scripts/open_backfill_diff.py`, in `make checks`, reports 71 rows before and 82 after with 67 kept verbatim, 4 reworded and ruled, and ZERO unaccounted for. FOUR THINGS THE DIFF FOUND THAT THE GATES DID NOT. (1) The first draft of `open_row` clipped the Why at 900 characters — `C29`'s truncating extractor, rebuilt by me inside its own replacement; `O47` lost 2201 characters. `C37-M1`. (2) `OPEN.md` carried TWO `O23`s, two `O24`s and two `O25`s, so the register — one row per id — could keep only one of each. Four rows were orphaned, not three: the register's `O24` had MERGED both titles into one row and its Why was EMPTY, the only empty Why in 298 rows. All four are recovered verbatim as `C-7`–`C-10` in OPEN.md's hand-maintained `## Closed` table; renumbering a collision still needs the owner's ruling and `## Defect — reused IDs` still holds it. (3) `O23`'s `From` was `F2c`, taken from the struck bell row it collided with; the progress-bar item came from `F5`. (4) `--check` guarded three artefacts and its success line named two. THE LAST STEP OF THE MIGRATION D36 AND D37 BEGAN, and the irreversible one: after it, OPEN.md stops being authoritative. PREREQUISITES, none optional. The register must be COMPLETE - C33 opened A, M, H and RR and backfilled them. It must be GUARDED - C34 builds the validator, because nothing in the tree noticed C33's ten malformed rows or the one C36 found afterwards. And THE OWNER MUST READ THE BACKFILLED REGISTER ONCE and confirm it, which no check can do for them. THE GENERATION MUST PUBLISH A ROW-BY-ROW DIFF against the OPEN.md of that day: any row present before and absent after is a backfill defect, not a row to accept. O44 measured the stake - 16 of OPEN.md's 35 O-ids and 83 lines of prose exist nowhere else, including the paragraph recording that the distraction tally over-reports for one block, which is a caveat on O1. NUMBERED C37 BECAUSE C35 WAS TAKEN by F4g's guessed-yield sweep after the migration order in blockersfor1_5.md had already named C35 for this. An ID is an identifier and not an index: the sweep keeps C35, this takes the next free number, and the gap is legal and permanent. |  |
| C38 | the device installer chose a simulator over the phone | P1 | closed |  CLOSED 2026-09-24. Owner: the agent. No user-visible change. `scripts/install-device.sh` picked the first device whose marketing name contained `iPhone` and `break`ed, and `xcrun devicectl list devices` lists simulators alongside phones — so with an iPhone simulator on the Mac it chose the simulator, built for `Debug-iphonesimulator`, and failed with *the build succeeded but produced no .app bundle*. FOUND BY RUNNING IT, when the owner asked for the build on their phone. `connectionProperties.transportType == \"sameMachine\"` is the discriminator; `hardwareProperties.isSimulated` is ABSENT on every entry, phone and simulator alike, so a truthiness test on it selects nothing and a falsiness test selects everything. The two failures now read differently, because *no iPhone* and *only simulators* have different fixes. `C38-M1`, `C38-M2`. | — |

## Agent items (A)

**Opened 2026-09-23 by `C33`, implementing `D36`.** Eighteen findings only the agent can close,
carried across from `docs/reviews/OPEN.md`'s *Needs the agent* table — **all eighteen, the twelve
closed ones included**, because the closed rows carry the reasoning for how each closed and a
register of open rows only is a to-do list. `A19` was opened here.

**This is a COPY, not a move.** `docs/reviews/OPEN.md` still holds these rows and is not edited,
retired or generated by this chore; generating it is `C37`. **It was `C35` when this was written**, and `C35` was taken by `F4g` for the guessed-yield sweep before the generation unit was opened — a forward reference to a number nobody had allocated yet. Corrected rather than left to collide, because `OPEN.md` already carries two `O23`s and this register already has a section for that defect.

**Status is derived, and the derivation is mechanical so it can be checked rather than taken:**
strikethrough on the source row's `#` cell is `closed`, no strikethrough is `open`. That yields
exactly six open — `A1`, `A8`, `A14`, `A16`, `A17`, `A18` — which is the number `D36` states, so the
derivation is self-checking. The strike marker *is* the status and is present on all eighteen rows,
so no row here was statusless and nothing was defaulted. The vocabulary is `open` | `closed` and
nothing else: `gen_status.py`'s closed set is fixed, so a nuanced word would count silently as open.
The nuance is in `Why`.

**`P` IS THE AGENT'S, ASSIGNED 2026-09-23 FROM THE EVIDENCE IN EACH ROW. It is not in the source and
it is not contemporaneous, and the owner may overrule it.** Closed rows are left blank, because a
priority on a closed row is noise. If the owner would rather an agent register carried no priorities
at all, blank all nineteen — the register stays valid, because `P` is not the field
`docs/conventions.md` makes mandatory. **`P` IS THE PINNED ENUM `P0` · `P1` · `P2` AND NOTHING ELSE.**
`A14` shipped `P3`, which `docs/conventions.md` Axis 3 does not define; corrected to `P2` 2026-09-23
after `C33`'s adversarial review. `P2` is the floor, so *seen once, never reproduced* and *real,
bounded, needs a device* share it, and the distinction lives in `Why` where a reader can read it.
**`O31` also carries `P3` and is NOT corrected here:** the `O` register is the owner's, and inventing
a priority change on an owner's row is not this chore's to do. A `C34` validator on the priority enum
will fire on `O31` alone, correctly, and that is the point of it firing.

**The source table is NOT in ID order** — `A1`…`A11`, then `A18`, `A17`, `A16`, `A12`, `A13`, `A15`,
`A14`. It was transcribed in source order with each ID and its text travelling together, sorted to ID
order as a separate mechanical pass, and the `(ID, first 40 characters of Title)` pairs diffed before
and after. The diff is empty. That procedure exists because `C29` matched rows on ID across two
documents and pasted the wrong row's text.

**UPSTREAM, AND NOT DONE HERE.** `D36` clause 3 flags an agent register as a candidate promotion to
`docs/conventions.md` Axis 2 — *"the conventions have no home for agent-owned findings"* is a drift
mechanism other projects will hit. **Flagged, not made:** the vendored copy is never edited (`D24`).

| ID | Title | P | Status | From | From | Why |
|---|---|---|---|---|------|
| A1 | `try?` on the three `StatsQuery` fetches | P1 | open | F6 | F6 | A refused database read renders as a confident "you did nothing", on the one screen whose entire premise is being trusted, and as `No pomodoros in this range.` in a document filed as truth. |
| A2 | `noIdentifiersInOutput` is largely tautological |  | closed | F6 | F6 | CLOSED 2026-08-24 — routed through a real store, verified by mutation. Four of its five assertions cannot fail: the fixture contains no identifiers. The structural guarantee is real; the executed half is decorative. |
| A3 | Main-thread render and disk I/O on every range change |  | closed | F6 | F6 | CLOSED 2026-08-24 by measurement. `refreshExport()` builds the whole document and sweeps the temp directory synchronously, eagerly, whether or not anyone taps Export. |
| A4 | The temp sweep deletes every `ZenTomato-*.md` |  | closed | F6 | F6 | CLOSED 2026-08-24 — per-launch directory. Including one an open share extension may still be reading. |
| A5 | Dead code with load-bearing documentation |  | closed | F6 | F6 | CLOSED 2026-08-24 — `StatsRange.everything` deleted. `StatsRange.everything(endingOn:in:)` and `StatsPeriod.recordedSpan` have no caller outside tests and describe an all-time export the range control does not offer. |
| A6 | `StatsRange.swift:35` states the opposite of the shipped UI |  | closed | F6 | F6 | CLOSED 2026-08-24. It says "no free-form date pickers" against a control whose own header says "Two pickers and a reset." |
| A7 | Markdown metacharacters in Todoist titles are not escaped |  | closed | F6 | F6 | CLOSED 2026-08-24. A task named `**Thesis**` renders as markup in the exported page. |
| A8 | Time-zone change re-attributes historical days — *researched, ruled, pinned* | P2 | open | F6 | F6 | Correct across DST — every boundary goes through `Calendar` — but a device that changes zone makes two exports of the same fortnight disagree. Unspecified and untested. |
| A9 | The performance test asserts `< 1s` against a 16 ms budget |  | closed | F6 | F6 | CLOSED 2026-08-24 — tightened to 250 ms. A 60× regression passes green. |
| A10 | The palette lint rule does not cover `Stats/`, `Export/`, `Sprint/` |  | closed | F6 | F6 | CLOSED 2026-08-24 — and `ZenTomatoWatch/`, `Todoist/`, `Plan/`, `Music/`. Nothing violates it today; the guard has a hole. |
| A11 | The no-writes fence test omits `update` and `comment` |  | closed | F6 | F6 | CLOSED 2026-08-24. Two of the four words `CLAUDE.md`'s hook names. The real gate — the allowlist — is untouched, so the test's comment overstates it. |
| A12 | A task finished on another device is still offered until the next refresh |  | closed | F3 | F3 | CLOSED 2026-08-24 — the mirror forgets it on `.alreadyGone`. `.alreadyGone` enters neither the D21b set nor a cache deletion. Pre-existing; widening D21b's trigger would change its meaning from *completed* to *believed gone*. |
| A13 | `docs/reviews/F6.md` does not follow the F1–F5 template |  | closed | C6 | C6 | CLOSED 2026-08-24. Six logs, two shapes. |
| A14 | The rewind that could not be reproduced | P2 | open | F4 | F4 | Reported once, never seen again, most likely an artefact of the track name being one behind at the time. `docs/reviews/F4.md` carries the diagnostic order and four ranked fixes; **B** is recommended if it recurs. |
| A15 | `MusicSubscription.current` is deprecated in process |  | closed | F4 | F4 | CLOSED 2026-08-24 — not actionable on iOS. The device log carries: *"it has recently become deprecated to request the music subscription status in process; the new supported code path fetches it in itunescloudd, but you need to add `com.apple.security.exception.mach-lookup.global-name com.apple.itunescloud.music-subscription-status-service` to your sandbox"*. Works today. Worth doing before it stops. |
| A16 | The query costs 57 ms for an all-time range | P2 | open | F6b | F6b | Found by measuring for A3 — the cost is the query, not the render. ~0.22 ms per day of range, so the 16 ms budget is crossed at about ten weeks; the default fortnight is 3 ms. **Deliberately not fixed:** `F6-contract.md` pre-decided a `@ModelActor` and warned against taking it pre-emptively, *"since a suspension point is where the last two features found their real bugs"*. Recorded with the number so it can be decided with rather than about. |
| A17 | Why the alarm is silent when the phone is silent | P1 | open | F2 | F2 | Unknown, and **not to be guessed at** — three framework assumptions have already been wrong this week. The configuration itself is idiomatic: `AlarmManager.AlarmConfiguration.timer(...)` with `sound: .default` when the setting is on and `.named("Silence.caf")` when off, and `Silence.caf` is confirmed present in the shipped bundle. Two candidate causes, and one device check separates them — see `docs/chores/C14.md`. |
| A18 | Watchdog kill: `playbackState` is a blocking XPC call on the main thread — *diagnosed, fix designed, not built* | P1 | open | F4 | F4 | `docs/crashes/ZenTomato-2026-08-26-134602.ips`: `0x8BADF00D`, ten seconds of wall clock against **0.053s of app CPU** — blocked, not looping. Stack ends in `xpc_connection_send_message_with_reply_sync` under `MPMusicPlayerController.playbackState`. `F4-contract.md` §8 chose to read it live on purpose and did not price it as IPC. **No test could have caught it**: the stand-in returns instantly, and nothing in the suite distinguishes a property read from a cross-process call. See `docs/chores/C16.md`. |
| A19 | Five `F8` mutation IDs are defined only in commit messages and cannot be validated from the tree | P2 | open | C33 | C33 | OPENED 2026-09-23 by `C33`. **All five** — `F8-M6` and `F8-M10` through `F8-M13` — are **defined in no file in the tree**; the only source for each is a commit body (`66e3e1c` for `F8-M6`, `b938f95` for the other four), and each register cell transcribes that body. CORRECTED 2026-09-23 BEFORE MERGE, and the correction is the instructive part: an earlier draft of this row split the five into *two cited* and *three uncited*, derived by a literal grep. `docs/handoffs/blockersfor1_5.md:270` cites all five in one sentence using RANGE notation — "`F8-M6` and `F8-M10`–`M13` exist **only** in the text of two commits" — which a literal grep for `F8-M11` cannot see. The partition was an artifact of the search, asserted as fact in the row whose entire subject is provenance. It also understated the exposure handed to `C34` as two IDs when it is five, so an exemption list built from the earlier draft would have been short by three. And being named in that handoff is not provenance for a mutation's *definition*: the handoff cites these IDs only to record that they have none (`66e3e1c` and `b938f95`), so they are citations with nothing behind them in the tree: the `D14` failure again. **THE ROWS IN `## Mutations (M)` ARE A TRANSCRIPTION AND NOT A SOURCE, AND THE DISTINCTION IS THE WHOLE ITEM.** This row and those rows state each definition, in a file in the tree, so the earlier wording — *"appear in no file in the tree"*, written inside a file in the tree — said something it did not mean and was corrected 2026-09-23 by `C33`'s second review pass. What is absent is an authored definition a checker can trace to a document; what is present is five cells a person copied out of `git show`, which nothing can verify without leaving the tree. All five have rows in `## Mutations (M)` whose `Source` is a SHA, because an ID in use is in the register or the register is not the register. **This is one item, not five rows of the same complaint.** `C34`'s validator cannot resolve a SHA by reading files and must either accept one as a terminal citation or report these as an enumerated exemption — and an exemption list is how a validator quietly stops validating, so whichever it is should be visible in the output. Git history is NOT rewritten and `docs/plans/F8.md` is not retro-edited: `F8.md:620` declined to open an `M` register deliberately and with its reasoning stated. |
| A20 | Seven open owner items carry no mode, so they sit in the owner's queue asking nothing | P2 | open | C37 | **28% OF THE OWNER'S OPEN QUEUE — 7 of 25 — has `@?` in the `Mode` column**: `O1`, `O9`, `O34`, `O35`, `O40`, `O45`, `O46`. `@?` is not a mode. It is the absence of one, and a row that does not say whether it wants a RULING or a DEVICE CHECK cannot be answered without first reconstructing what it was for. RAISED BY THE OWNER READING `O35` and asking, reasonably, *what is the ask for me to decide?* — there was none. `O35` is four documentation fixes filed under `F2d`'s *no code-behaviour defects* merge bar; one is already fixed by `D41`, and the other two are an agent's edits with no contract question in them. **It should never have been an owner item**, and it has waited a month because it was filed with a question mark instead of an owner. `docs/conventions.md` Axis 2 says every row carries a STATUS, for exactly this reason — *status absent means the reader must go looking* — and says nothing about the mode, so this is the same failure at a field the convention does not yet cover. THE FIX IS TWO PARTS: the agent re-reads the seven and proposes a mode for each, including `chore` for any that are not the owner's at all; the owner confirms. Neither half is hard; nobody has done it because an item with no ask also generates no prompt to fix it. |  |
| A21 | Both `appliedList` declarations were inert — the ratchet never read either file | P2 | closed | C37 | Found 2026-09-24 while `D44` moved v1.5's ledger into its baseline. **Pointing EITHER baseline's `appliedList` at `docs/specs/NO-SUCH-FILE.md` left the whole suite green**, for both `SPEC.md` and `zenpom-v1.5.md`: `appliedAmendments` returns an empty set for an unreadable path, and neither outstanding count happens to depend on suppression today. So the field could name a typo, a moved file or a path that never existed, and nothing would say so — and the move itself was therefore unverifiable by the suite that exists to verify it. Removing `D31` from the list passes too, and so does removing `D33`, which is `O46` from another angle. FIXED: `everyWatchedBaselineDeclaresAReadableAppliedList` asserts each declared file is readable, carries the heading at the start of a line, and parses to at least one id; `bothBaselinesCarryTheirOwnAppliedList` pins `D44`'s ruling. A guard nobody has seen fail is a guard nobody knows works. |  |
| A22 | A plan's prose can contradict the register and nothing notices | P2 | open | C37 | **`F8.md`'s `Ruling E` says the shape-store delta is *"not ratified"*. It was ratified as `D32` on 2026-09-09 — the same day the paragraph was written — and the plan was never updated.** The cost was not theoretical: on 2026-09-24 the agent read that sentence, asked the owner to decide a question `D32` had already decided, got an answer, and wrote `D47` for it. `D47` is rejected as a duplicate. THE CLASS: registers are generated, validated and diffed — `gen_status.py`, `check_register_rows.py`, `open_backfill_diff.py` — and PLANS ARE PROSE THAT NOTHING READS. `DeltaIntegrityTests` checks that a cited `D<n>` exists; nothing checks that a sentence ABOUT a `D<n>` still describes it. A cheap first cut: fail when a plan calls a delta unratified that the register lists as ratified, which is one grep over `docs/plans/*.md` for *not ratified* near a `D` token. It would have caught this one. Not built here — it is a chore, and this row is the finding. |  |

## Mutations (M)

**Opened 2026-09-23 by `C33`, implementing `D38`, with full backfill.** 119 rows: every mutation ID
in use anywhere in this project. **`C33` re-runs none of them** — the register records which were run
and when, rather than implying this chore re-verified them. **`C34` added sixteen rows and every
figure in this preamble was recomputed from the table below afterwards**, not carried forward: `C33`'s
own review caught this paragraph contradicting the table at 80-vs-81, and a second instance would be
worse than the first.

**`Title` is the mutation description.** `D38` proposed the column name `Mutation`;
`scripts/gen_status.py` keys a row's fields off the table's own lowercased headers and reads `title`,
so `Mutation` would render blank everywhere. The divergence is recorded rather than left looking like
a mistake. `P` is empty throughout: a mutation is run or it is not, and a priority on it means nothing.

**`Run` IS THE COLUMN THAT MATTERS, and it is a pinned enum of three values.** It separates the **77
mutations that have been applied to shipped code — 76 `run` and one `escaped` — from the 42 that have
never touched code.** `run` — applied to shipped code, the named test seen to go red, then restored.
`escaped` — applied, and the named test did **not** catch it; this project has exactly one, `F8-M9`,
and flattening it into `run` would be the lie the column exists to prevent. `proposed` — named in a
plan for work that has not been built.

**SO THE APPLIED COUNT IS NOT THE COUNT OF MUTATIONS *SEEN TO FAIL*, AND THIS PARAGRAPH SAID IT WAS UNTIL
2026-09-23,** when `C33`'s adversarial review caught it: the sentence read *"the 61 mutations that have
been seen to fail"* four lines above the sentence pinning `escaped` as the case where the named test
did not catch it, which is the flattening the column exists to prevent, performed silently by the
paragraph that forbids it. **Seen to fail is 76.** `escaped` is one further application and its
own row says the first application escaped. `docs/plans/00-status.md`'s `| Mutations (`M`) | 119 | 42 |
0 | 77 |` is a **`Status`** count, not a `Run` count — `F8-M9`'s `Status` is `closed` because it was
caught after the fixture and the regex were repaired — and reading that closed figure back as a `Run` figure is
how the two got conflated. **Both numbers are derivable from the table below and neither is asserted
here:**

```
python3 - <<'PY'
import re, collections
sec = re.search(r'## Mutations \(M\)\n(.*?)\n## ', open('docs/plans/00-register.md').read(), re.S).group(1)
rows = [l for l in sec.split('\n') if l.startswith('| ') and not l.startswith(('|---', '| ID '))]
# SPLIT ON AN UNESCAPED PIPE. A bare split('|') read the wrong column as soon as one
# row carried an escaped pipe in a code span, which C34-M16's row does.
cells = lambda l: [c.strip() for c in re.split(r'(?<!\\)\|', l.strip())[1:-1]]
run = collections.Counter(cells(l)[4] for l in rows)
print(len(rows), run)          # 119 Counter({'run': 76, 'proposed': 42, 'escaped': 1})
PY
```

**A FOURTH VALUE, `run, no count`, WAS PINNED AND IS WITHDRAWN 2026-09-23 after `C33`'s adversarial
review: zero of the 119 rows used it.** A pinned enum with a dead member is an invitation — the next
writer who has no suite count reaches for the value that sounds like their situation instead of for
`escaped` or a real number. The distinction it was for is already carried by the `Count` column, which
reads `—` on 48 rows and means exactly *run, and no count was recorded at the time*. One fact, one
column.
**A mutation is `run` only where a source records it FAILING.** *"Named now and run before this task
is called done"* (`docs/plans/F8.md:468`) is a promise, and a promise filed as a run makes this
register the false-assurance instrument `docs/conventions.md` describes at length.

**TWO SPELLINGS, AND THEY ARE NOT TWO NAMES FOR ONE POPULATION.** Bare `M1`–`M20`, `M22`, `M23` are
genuine project-global IDs from the `F4` retrofit series and `C12`. The rest are scoped to the unit
that owns them, which is what `docs/conventions.md` asks for. They coexist. **`M21` and `M24`–`M33`
are gaps: legal, permanent, and never closed by renumbering** — `docs/conventions.md` Axis 1's
retrofit paragraph, *"an identifier, not an index"*. **It read (`D17`) until `C34`'s review:** `D17`
is *"A session plan: several Todoist items, in the order you will work them"* and says nothing about
renumbering, so the citation had nothing behind it — the `D14` class of defect one register over, and
visible for the first time because `## Decisions (D)` is now generated and can be read against.

**THE COUNTS WERE RE-DERIVED AND `D38`'s HAVE MOVED.**
`grep -rhoE '\b[FC][0-9]+[a-z]*-M[0-9]+[a-z]*' . --exclude-dir=.git | sort -u | wc -l` returns 74
scoped IDs present in the tree at `bc4e0f2`; `D38`'s own `F`-only command returns 58 against the 52 it
recorded, the difference being `F7b`, and it cannot see `C28`'s three or `C32`'s thirteen at all. Add
three `F8` IDs that exist only in a commit body and four of `C33`'s own, and the scoped population was
81; with the 22 bare that was **103**. `C34` added sixteen scoped rows of its own, so the population is
now **97 scoped and 22 bare — 119**, recomputed from the table below rather than carried forward. (`C33-M4` was allocated after the adversarial review, against the
one further line of `gen_status.py` that review asked for; 102 is the figure before it.)

**`M34` IS NOT AN ALLOCATED ID AND HAS NO ROW HERE.** Its only two appearances in the tree are denials
of its own existence: `docs/plans/F15.md:445` — *"The draft claimed `M34` was the highest in the
register; it is not"*, the number having come from a Todoist task id on the `O23` row — and `D38`'s
own enumeration line, which took the draft's word. **`D38`'s `(M1–M20, M22, M23, M34)` overcounts by
one. The delta's text is not edited; the correction lives here.**

**FIVE IDS ARE DEFINED ONLY IN A COMMIT BODY** — `F8-M6`, `F8-M10`, `F8-M11`, `F8-M12`, `F8-M13` — and
each says so in its own cell. The class is filed once as `A19` rather than as five rows of the same
complaint. **The cells are a TRANSCRIPTION of that commit body and not a source**: the definition is
stated here, in a file, and is still traceable only by leaving the tree, which is the distinction
`A19` is about.

**AND THREE OF THOSE ROWS — `F8-M11`, `F8-M12`, `F8-M13` — NAME NO TEST AT ALL.** Their
`Test that catches it` reads `not named in the source`, because the commit body records a count of
tests going red and no test name. `docs/conventions.md` defines a mutation as *"named ways to break the
code, each paired with the test that must catch it"*, so a row that is `run` with no citable test is
**a second, distinct row shape from `A19`'s** — the source is unreachable in `A19`'s case and simply
absent in this one — and a `C34` check on `A19`'s shape passes all three of these. It is
`docs/chores/C33.md`'s row shape 15, added 2026-09-23 after `C33`'s second review pass found it missing
from the fourteen. Not repaired here: inventing a test name for a mutation run three weeks ago in
another unit would be the reconstruction this register refuses.

**`F8-M7` AND `F8-M8` EACH NAME TWO DIFFERENT MUTATIONS.** That is `O43`, it is the owner's to rule
on, and this chore writes **one row per ID** with the collision stated. No disambiguating suffix is
allocated, because `F8-M7a` would be an identifier nothing cites.

**SORTING. THE SOURCE IS IN ID ORDER, AND THE ORDER IS A STATED KEY.** `docs/conventions.md`,
Axis 2: *every register is sorted by ID*, for a writer's reason — *allocating the next number must
not require scanning forty unsorted rows.* The key is the ID parsed as
`(prefix, number, retrofit letter, scoped prefix, scoped number, scoped letter)`, which gives
`C28` · `C32` · `C33` · `F7b` · `F8` · `F10` · `F14`–`F19` · `M1`–`M23`, and it puts the highest
`F8` mutation immediately above `F10-M1` instead of eighty rows away. **An earlier draft of this
section sorted the source to match `gen_status.py`'s output**, which is backwards:
`docs/conventions.md` says *"re-sorting for reading is the generated status page's job, not the
source's."* The page's own `sort_key` does not parse a scoped id and degrades to `(unit_id, 0, "")`,
so the page prints an order of its own. That divergence is the page's business and no longer the
source's. The 103 rows were re-sorted with no content change, proved by diffing the
`(ID, first 40 characters of Title)` pairs before and after — identical, as `C29`'s procedure
requires.

| ID | Title | P | Status | Run | Test that catches it | Count | Source |
|---|---|---|---|---|---|---|---|
| C28-M1 | Append a hand-written line to `00-status.md` |  | closed | run | `make check-status` — exits 1 | — | docs/chores/C28.md:104 |
| C28-M2 | Flip `O43` from `open` to `closed` in the register and do not regenerate |  | closed | run | `make check-status` — exits 1 from a verified-green baseline | — | docs/chores/C28.md:105 |
| C28-M3 | Delete `scripts/gen_status.py` |  | closed | run | `make check-status` — an absent generator fails the gate rather than skipping it | — | docs/chores/C28.md:106 |
| C32-M1 | Delete the comma spelling from `currentlyMarkers`, restoring the colon-only matcher |  | closed | run | `bothCurrentlySpellingsAreRecognised` | caught, 5 issues | docs/chores/C32.md:199-212 |
| C32-M2 | Point the second `WatchedBaseline`'s `document` at `SPEC.md`, so the ratchet stops reading v1.5 while still looking configured for two baselines |  | closed | run | `aCommaFormDeltaQuotingTheV15SpecIsSeen` | caught | docs/chores/C32.md:199-212 |
| C32-M3 | Append a ratified fixture delta quoting live v1.5 text, with its index row and the count sentence corrected to 44 |  | closed | run | `everyRatifiedV15AmendmentIsApplied` | caught — 1 outstanding against a baseline of 0 | docs/chores/C32.md:199-212 |
| C32-M4 | Change the index sentence from *43 deltas* to *42 deltas* |  | closed | run | `theIndexStatesTheNumberOfDeltas` | caught | docs/chores/C32.md:199-212 |
| C32-M4b | Delete the count sentence outright |  | closed | run | `theIndexStatesTheNumberOfDeltas` — the absent-sentence branch | caught | docs/chores/C32.md:203 |
| C32-M5 | Slice the block from `hit.lowerBound`, including the marker |  | closed | run | `theColonFormBlockIsByteIdentical` | caught, 16 issues | docs/chores/C32.md:199-212 |
| C32-M6 | Remove `D31` from `V15-AMENDMENTS-APPLIED.md` |  | closed | run | `everyRatifiedV15AmendmentIsApplied` | caught on the second run at 635 — the FIRST RUN ESCAPED at 633, and that escape is the defect C32 fixed | docs/chores/C32.md:199-212 |
| C32-M7 | Restore the silent drop — `owedBaseline` returns `nil` when the declaration resolves to nothing watched |  | closed | run | `anUnwatchedDeclarationIsStillJudgedAgainstSpec` | caught | docs/chores/C32.md:199-212 |
| C32-M8 | Delete the preamble scan from `declaredBaseline(for:)` |  | closed | run | `theBaselineADeltaDeclaresInItsPreambleIsRead` | caught | docs/chores/C32.md:199-212 |
| C32-M9 | Resolve a declared name by `hasSuffix` instead of the last path component |  | closed | run | `theBaselineADeltaDeclaresInItsPreambleIsRead` | caught | docs/chores/C32.md:199-212 |
| C32-M10 | Move the comma inside the marker literal, `"**Currently**,"`, so the comma form's consumed length changes and the colon form's does not |  | closed | run | `bothCurrentlySpellingsAreRecognised` | caught, 4 issues | docs/chores/C32.md:199-212 |
| C32-M11 | Restore the block-name short-circuit — `declaredBaseline` returns `watchedBaseline(named:)` as soon as the block yields any document-shaped name |  | closed | run | `anUnwatchedBlockNameDoesNotVetoThePreamble` | caught at 637 / 1 issue | docs/chores/C32.md:199-212 |
| C32-M12 | Swap the two entries of `watchedBaselines`, so index 0 is v1.5 |  | closed | run | `theWatchedBaselinesAreInTheOrderEveryCallerAssumes` | caught at 637 / 3 issues | docs/chores/C32.md:199-212 |
| C33-M1 | Delete one scoped `M` row from the register and do not regenerate |  | closed | run | `make check-status` — the `Mutations (M)` row count falls and `--check` exits 1 | caught | docs/chores/C33.md |
| C33-M2 | Revert `gen_status.py`'s `ROW_ID` to the bare-only `[A-Z]{1,2}\d+` |  | closed | run | `statusPageCountsAScopedMutationId` | caught at 20 script tests — the M count fell from 2 to 1 | docs/chores/C33.md |
| C33-M3 | Widen `ROW_ID` to `[A-Z-]+\d*.*`, so it counts anything |  | closed | run | `statusPageCountsAScopedMutationId` — the junk-id half | caught at 20 script tests — the M count rose from 2 to 3 | docs/chores/C33.md |
| C33-M4 | Remove `("A", "Agent items")` from `gen_status.py`'s `KNOWN_REGISTERS` |  | closed | run | `statusPageNamesTheAgentRegister` — the generated register line reads ``A (`A`)`` instead of ``Agent items (`A`)``, the register `D36` opened printing its own symbol as its name | caught at 20/21 | docs/chores/C33.md · added 2026-09-23 after the adversarial review |
| C34-M1 | Delete the BEGIN marker comment from `## Decisions (D)`, leaving the table and the END marker |  | closed | run | `markerlessRegisterIsRefused`; `make check-status` | caught — WRITE mode exits 1 with *0 BEGIN markers and 1 END markers*, and the register was byte-identical afterwards (md5 compared); `--check` exits 1 with the same line. The write-mode half is the one that matters: a generator that silently appends when it cannot find its region is `O42` rebuilt one layer in | docs/chores/C34.md |
| C34-M2 | Hand-edit `D35`'s Status inside the generated region from `ratified` to `proposed` |  | closed | run | `handEditInsideTheRegionFailsCheck` | caught — `make check-status` exits 1 and the FAIL line names **docs/plans/00-register.md**, with a unified diff showing the one row. Today's pre-`C34` message named no file, because there was only one artefact to name | docs/chores/C34.md |
| C34-M3 | In `### Decisions — owner fields`, change the overlay id from `D30` to the first number above this project's last defined delta, which `00-deltas.md` does not define |  | closed | run | `overlayNamingAnUndefinedDeltaIsRefused` | caught — exit 1 naming that id, register byte-identical afterwards. A Todoist id keyed to a decision that does not exist is `C33`'s dropped-`TD` defect one file over, so the loss is refused rather than performed quietly | docs/chores/C34.md |
| C34-M4 | Delete the whole `### Decisions — owner fields` section |  | closed | run | `decisionsRegionCarriesTheOwnerOverlay` covers the positive direction | caught — `make check-status` exits 1; the diff shows `D30` regenerating from `P0` and `td:6hPjPm4FcP3gJ4Cj` to `—` and an empty cell. The overlay is load-bearing, not decorative | docs/chores/C34.md |
| C34-M5 | Append a surplus seventh cell to the `C26` chore row, as `C33`'s seven rows carried |  | closed | run | `registerValidatorCatchesASurplusCell` | caught — R1: *7 cells where the section header has 6*. **`make check-status` stays GREEN under this mutation** (exit 0), which is the argument for the validator existing stated as a result rather than as a claim | docs/chores/C34.md |
| C34-M6 | Put a raw pipe inside a backticked cell of the `H1` row |  | closed | run | `registerValidatorCatchesAnUnescapedPipe` | caught — R1 (11 cells against 10) and R2, the message naming the unescaped pipe in a `code` span and the escape that fixes it. The register holds no escaped pipe to un-escape, so the mutation INSERTS a raw one: `C33`'s defect reached from the other side | docs/chores/C34.md |
| C34-M7 | Duplicate the `A7` row in `## Agent items (A)`, changing nothing else |  | closed | run | `registerValidatorCatchesADuplicateId` | caught — R3 names `A7` and **both** line numbers, 251 and 250 | docs/chores/C34.md |
| C34-M8 | Blank the Status cell of the `RR1` row, keeping the cell count intact |  | closed | run | `registerValidatorCatchesAnEmptyStatus` | caught — R4. The cell count is preserved on purpose so the mutation trips R4 and not R1. **R4's only evidence**: no row of the shipped file has an empty status cell | docs/chores/C34.md |
| C34-M9 | Two edits run separately: `C26`'s Status set to `wontfix`, then to `unknown` |  | closed | run | `registerValidatorCatchesAnUnknownStatusOutsideTheExceptionSet` | caught on BOTH — `wontfix` is refused against the printed vocabulary, and `unknown` is refused *naming `C26` as outside the legacy set* and listing the nine ids that may carry it. The second half is what proves the exception is keyed to an id rather than a blanket allowance | docs/chores/C34.md |
| C34-M10 | Swap the two adjacent `H1` and `H2` rows in `## Hooks (H)` |  | closed | run | `registerValidatorCatchesAnUnsortedSection` | caught — R6: *H2 then H1 is out of ID order*. The paired positive matters as much: the unmutated file reports **0** out-of-order pairs under the widened key | docs/chores/C34.md |
| C34-M11 | Rename the `(RR)` section's `Status` header cell to `State` |  | closed | run | `registerValidatorCatchesARenamedStatusHeader` | caught — R8 names the section and prints the header it actually found. The reason R8 exists is the second half: once regenerated, the page renders Risks as `4 rows, 0 open, 4 unknown` and **`make check-status` passes** (exit 0) — a confident wrong answer for a whole section | docs/chores/C34.md |
| C34-M12 | Remove `"ratified"` from `gen_status.CLOSED` |  | closed | run | `ratifiedDecisionsAreNotCountedOpen`; `make check-status` | caught — the page's Decisions line moves from `43 / 1 / 0 / 42` to `43 / 41 / 0 / 2` and forty owner-owned rows appear in the open table; the fixture test fails at `3 / 2 / 0 / 1`. Pins the ruling that makes carrying all 43 rows affordable | docs/chores/C34.md |
| C34-M13 | Add `"held"` to `gen_status.CLOSED` |  | closed | run | `make check-status` | caught — `O37` leaves the open table and Owner items moves from `31 / 25 / 0 / 6` to `31 / 24 / 0 / 7`. Pins the opposite half of the vocabulary ruling: `held` is parked work, not finished work, and the decision not to bucket it is now a thing that can fail | docs/chores/C34.md |
| C34-M14 | Build the page from the on-disk register text instead of the in-memory spliced text |  | closed | run | `statusCheckPassesWhenGenerated` (`C28`'s existing guard) | caught — three script tests go red, `statusCheckPassesWhenGenerated` reporting *--check failed on a page it had just written*. Detectable ONLY because `make_status_repo`'s fixture now carries a deltas file and the marker pair, which is why that fixture change was not optional | docs/chores/C34.md |
| C34-M15 | Point the validator at a register with every table row removed |  | closed | run | `registerValidatorRefusesAnEmptyRead` | caught — *the parser read nothing*, naming all seven empty sections, rather than printing OK over zero rows. The floor `docs/conventions.md`'s harness contract requires | docs/chores/C34.md |
| C34-M16 | Revert `parse_tables` to the escape-blind `split("\|")`, so the generator and the validator disagree about where a cell ends |  | closed | run | `anEscapedPipeDoesNotShiftARow` | caught — the fixture page reports Hooks as `2 / 2 / 0 / 0`, printing a **closed** hook as open, and on the real file `make check-status` goes red with Mutations moving to `75 closed and 1 unknown`. **`check_register_rows.py` stays GREEN throughout**, because by its own tokenizer the row is well formed — which is why this mutation's evidence is a generator test and not the validator | docs/chores/C34.md |
| C37-M1 | Reinstate the first draft's truncation: `open_row` clips the Why at 900 characters again |  | closed | run | `scripts/open_backfill_diff.py`, and `make check-open-backfill` | caught — exit 1, **2 rows unaccounted for**, `O47` and `O33` each reported with the text present before and absent after. This is `C29`'s defect committed a second time, by me, in the generator that replaced the extractor that caused it; `O47` went from 3111 characters to 910. | docs/chores/C37.md |
| C37-M2 | Delete the register's `O17` row, so the backfill silently drops an item |  | closed | run | `scripts/open_backfill_diff.py`, and `make check-open-backfill` | caught — exit 1, **1 row unaccounted for**, `~~O17~~`. The rule the check exists for, stated as a failure: a row present before and absent after is a defect, not a row to accept. | docs/chores/C37.md |
| C37-M3 | Delete OPEN.md's `BEGIN GENERATED: owner` marker |  | closed | run | `gen_status.py`'s marker-pair refusal | caught — exit 1, *0 'BEGIN GENERATED: owner' markers and 1 'END'*, and **nothing was written**. A generator that writes into a file whose markers it cannot find writes over prose. | docs/chores/C37.md |
| C37-M4 | Hand-edit a row inside OPEN.md's generated region — `O23`'s title rewritten |  | closed | run | `gen_status.py --check`, and `make check-status` | caught — exit 1, *OPEN.md is out of date or hand-edited*, with the diff printed. Also how the stale success line was found: `--check` covered three artefacts and named two. | docs/chores/C37.md |
| C37-M5 | `splice_open` assigns `lines = body`, replacing the whole file instead of the span between the markers |  | closed | run | `openRegionsAreWrittenAndProseSurvives`, and every status test through `gen_or_fail` | caught — **16 of 44 tests red**. IT DID NOT REPORT AT FIRST, AND THAT WAS THE MORE IMPORTANT FINDING: the runner sets `set -e`, a bare `python3 gen_status.py` returned 1, and the suite printed its banner, exited 1 and reported NOTHING — 43 tests abandoned, which is the *suite that stopped running* the harness contract names. `gen_or_fail` now turns a non-zero generator into one test's failure. | docs/chores/C37.md |
| C37-M6 | `open_row` stops striking a closed row's id |  | closed | run | `openRegionsAreWrittenAndProseSurvives` | caught — *the closed owner row was not struck*, 43 passed and 1 failed. The strikethrough is the one thing a reader of OPEN.md uses it for. | docs/chores/C37.md |
| C37-M7 | `open_row` emits an empty `From` cell, erasing the provenance of all 67 rows |  | closed | run | `open_backfill_diff.py`, and `make check-open-backfill` | ESCAPED at `RUN = 8`, then caught at `RUN = 3`. A `From` value is one to four words inside a long row, so no eight-word window could ever see it — while `C37`'s whole claim is that `From` came across. Found by the adversarial review. | docs/chores/C37.md |
| C37-M8 | Delete the register's `O46` row — an id on the reworded allowlist |  | closed | run | `open_backfill_diff.py`, and `make check-open-backfill` | ESCAPED while the allowlist was keyed BY ID, then caught once it held exact lost RUNS. Allowlisting an id encodes a ruling about wording on one day as *this row may never go missing*. `C37-M2` caught the identical mutation on a non-allowlisted id, which is what made the hole invisible. Found by the adversarial review. | docs/chores/C37.md |
| C37-M9 | Delete the hand-maintained `C-6` row from OPEN.md's `## Closed` table |  | closed | run | `open_backfill_diff.py`, and `make check-open-backfill` | ESCAPED — every gate green — then caught once every before-row is PAIRED with one after-row and an unpaired row is a defect on its own. The row had aligned against a wrong counterpart and every unaligned run landed under the window. Found by the adversarial review. | docs/chores/C37.md |
| C37-M10 | Remove exactly seven words from `O33`'s Why |  | closed | run | `open_backfill_diff.py`, and `make check-open-backfill` | ESCAPED at `RUN = 8` — eight words were caught and seven were not — then caught at `RUN = 3`. The threshold was set by what made the reworded rows quiet, which is choosing a fixture that cannot distinguish the right answer from the wrong one. Found by the adversarial review. | docs/chores/C37.md |
| C37-M11 | `check_register_rows.py` exits 0 unconditionally |  | closed | run | the nine validator tests, through `expect_validator_catches` | ESCAPED as a REPORT: 33 of 44 tests ran, 11 were abandoned and NO summary line printed, because `expect_validator_catches` returned 1 as nine test functions' last statement and `set -e` killed the runner — `C37-M5`'s defect, still live at nine sites the chore claimed were fixed. Now 34 passed, 10 failed, summary printed. Found by the adversarial review. | docs/chores/C37.md |
| C38-M1 | Make every device look like a simulator, so no physical iPhone is found |  | closed | run | `scripts/install-device.sh`'s device discovery | caught — *no PHYSICAL iPhone is paired with this Mac — only 2 simulator(s)*, naming which failure it is and what to do about each. THE DEFECT WAS LIVE AND COST A DEVICE CHECK: the script took the first entry whose marketing name contained `iPhone` and broke, and `xcrun devicectl list devices` lists SIMULATORS TOO — so on this Mac it chose `HN-Test`, built `Debug-iphonesimulator`, and died with *the build succeeded but produced no .app bundle*, which names the symptom and not the cause | docs/chores/C38.md |
| C38-M2 | The no-candidates branch prints three empty fields before its note |  | closed | run | `scripts/install-device.sh`'s empty-udid check | caught on the first run of the fix, and it was MY bug not a planned mutation: `read -r udid name os note` strips leading whitespace, so the note landed in `udid`, the empty check passed, and the build ran with `-destination id=simulators=2`. An explicit `NONE` sentinel cannot collapse | docs/chores/C38.md |
| F7b-M1 | The defect itself: the `do`/`catch` becomes `if let found = try? context.fetch(existing), found.isEmpty == false` |  | closed | run | both refused-read tests, on `Outcome` | caught, 4 issues | docs/plans/F7b.md:259-264 |
| F7b-M2 | The row is written but still reported as dropped |  | closed | run | both refused-read tests, on `Outcome` | caught, 4 issues — and the designed distinction collapsed; the M2/M3 pair no longer separates | docs/plans/F7b.md:259-264 |
| F7b-M3 | The `catch` returns `.recorded` instead of `.unreadable` |  | closed | run | both refused-read tests, on `Outcome` | caught, 2 issues | docs/plans/F7b.md:259-264 |
| F7b-M4 | The adversarial reviewer's own literal trigger: `found = (try? context.fetch(existing)) ?? []` |  | closed | run | both refused-read tests, on `Outcome` | caught, 4 issues | docs/plans/F7b.md:259-264 |
| F7b-M5 | Regression: `if found.isEmpty == false` becomes `if found.isEmpty` |  | closed | run | eight of the suite's nine tests — the `F7` behaviours | caught, 16 issues | docs/plans/F7b.md:259-264 |
| F7b-M6 | Fixture mutation: `CorruptibleStore` overwrites the store file only, not `-wal` and `-shm` |  | closed | run | `aRefusedDedupReadCannotDuplicateAnExistingRow` | caught, 2 issues | docs/plans/F7b.md:259-264 |
| F8-M1 | Make the calculator return a shape whose total exceeds the budget by one minute |  | closed | run | the never-overrun assertions in the `T1` suite — `theOwnersTwoHourExample`, `sixtyMinutesIsTheMinimumAndItIsDerived`, `underTheMinimumStillProducesAShape`, `twentyMinutes` | caught, 4 tests | docs/plans/F8.md:637 · run in `a0dfd09` |
| F8-M2 | Make the override path write one resolved value back to `AppSettings` |  | closed | run | `aShapedSprintWritesNoSetting`, `startingAndStoppingAShapeWritesNoSetting` | caught, plus one bystander | docs/plans/F8.md:638 · run in `66e3e1c` |
| F8-M3 | Skip the clear when the shape's last block finishes, so the shape survives its sprint |  | closed | run | `aShapeDoesNotOutliveItsSprint` | caught, 5 tests | docs/plans/F8.md:639 · run in `3b21e4a` |
| F8-M4 | Make the decoder return the *default* shape instead of no shape when it cannot read the stored value |  | closed | run | `anUnreadableShapeIsNoShape` | caught, 5 tests | docs/plans/F8.md:640 · run in `3b21e4a` |
| F8-M5 | Add one `UserDefaults` read to `TimerEngine.swift`, and delete one from the shape store so the total is unchanged |  | closed | run | the amended `noNewPersistentSurface` in `PolishFenceTests` | caught | docs/plans/F8.md:641 · run in `3b21e4a` |
| F8-M6 | The monotonic twin stops following the shape |  | closed | run | `aShapedBlockSurvivesReconciliation` | caught | `66e3e1c` — **NO FILE IN THE TREE DEFINES THIS ID** — named in `docs/handoffs/blockersfor1_5.md:270` only to record that it has no definition, and defined only in this commit body, which this cell transcribes. See `A19`. |
| F8-M7 | Auto-start ON: `engine.isRunning` -> true, expected false. **AMBIGUOUS — this ID names two mutations** (`O43`): `ZenTomatoTests/ShapeScreenTests.swift:76` uses it for *the nothing-fits case renders an empty shape* |  | closed | run | `aShapeDoesNotOutliveItsSprint`; and, for the second meaning, the empty-shape assertion at `ShapeScreenTests.swift:76` | caught at 628 (`698530b`); 2 tests (`b938f95`) | `698530b` and `b938f95` · collision filed as `O43` |
| F8-M8 | Auto-start OFF: `state.kind` -> `.longBreak`, expected `.work`; `state.completedInSprint` -> 4, expected 0. **AMBIGUOUS — this ID names two mutations** (`O43`): `ZenTomatoTests/ShapeScreenTests.swift:111` uses it for *the under-minimum warning never appears* |  | closed | run | the breakless-shape sibling test; and, for the second meaning, the two-budget assertion at `ShapeScreenTests.swift:111` | caught at 628 (`698530b`); 2 tests (`b938f95`) | `698530b` and `b938f95` · collision filed as `O43` |
| F8-M9 | A control writes `AppSettings` |  | closed | escaped | `ShapeScreenFenceTests` — 2 tests, after the fixture and the source regex were fixed | **ESCAPED on its first application** — the fixture used the settings defaults, which satisfies both the right and the wrong implementation, and the source regex missed an optional chain. Caught after both were fixed | `b938f95` · `ZenTomatoTests/ShapeScreenFenceTests.swift:20` |
| F8-M10 | The quoted minimum stops following the toggle |  | closed | run | `ShapeBudgets`' quoted-number assertion | caught, 1 test | `b938f95` — **NO FILE IN THE TREE DEFINES THIS ID** — cited in `docs/handoffs/blockersfor1_5.md` and defined only in this commit body, which this cell transcribes. See `A19`. |
| F8-M11 | The boundary moved so a full sprint needs 61 minutes |  | closed | run | not named in the source | caught, 10 tests | `b938f95` — **NO FILE IN THE TREE DEFINES THIS ID** — this cell transcribes the commit body, the only source. `docs/handoffs/blockersfor1_5.md:270` names this ID inside the range `F8-M10`–`M13`, which records the absence rather than supplying a definition. See `A19`. |
| F8-M12 | The first of the two control-wiring escapes: `theSheetPassesItsControlsStraightThrough` read a different call further down the file |  | closed | run | not named in the source | caught after the fence was bounded by parenthesis matching | `b938f95` — **NO FILE IN THE TREE DEFINES THIS ID** — this cell transcribes the commit body, the only source. `docs/handoffs/blockersfor1_5.md:270` names this ID inside the range `F8-M10`–`M13`, which records the absence rather than supplying a definition. See `A19`. |
| F8-M13 | The second of the two control-wiring escapes, on the other control |  | closed | run | not named in the source | caught after the fence was bounded by parenthesis matching | `b938f95` — **NO FILE IN THE TREE DEFINES THIS ID** — this cell transcribes the commit body, the only source. `docs/handoffs/blockersfor1_5.md:270` names this ID inside the range `F8-M10`–`M13`, which records the absence rather than supplying a definition. See `A19`. |
| F8-M14 | Change `ShapeScreenModel.defaultBudgetMinutes` from 60 to 90 |  | closed | run | `theScreenOpensOnOneHour`, `theDefaultBudgetMakesFourCycles`, `theDefaultSqueezesPomsBelowTheSettingLength` | caught — 3 of 4 red. THE DEFAULT WAS UNGUARDED UNTIL 2026-09-24: it was `@State private var budgetMinutes = 60` inside a view, a number nothing read and nothing checked, on a screen where every other number is computed and asserted, and the one most likely to be changed by somebody tidying a preview. It is a named constant now, so the screen and its test read the same value rather than agreeing by coincidence | docs/plans/F8.md |
| F10-M1 | Add `@Parameter var taskName: String` to `StartSprintIntent` and use it in `perform()` |  | open | proposed | Fence A — `noParametersOnTheStartIntent` | — | docs/plans/F10.md:470-474 |
| F10-M2 | Make `perform()` call `TimerEngineHolder`/`TimerEngine.start()` directly instead of `BlockLauncher` |  | open | proposed | Fence B — `theStartIntentIsAThinCaller` | — | docs/plans/F10.md:470-474 |
| F10-M3 | Point Fence A's scan at a path that does not exist |  | open | proposed | `noParametersOnTheStartIntent`'s own existence assertion | — | docs/plans/F10.md:470-474 |
| F10-M4 | Make `BlockLauncher` answer `started` instead of `alreadyRunning` and fall through to `TimerEngine.start()` while a block is running |  | open | proposed | `startingTwiceLeavesTheRunningBlockAlone` | — | docs/plans/F10.md:470-474 |
| F10-M5 | Make `BlockLauncher` treat an intent-sourced request as a local one, so `.notDetermined` reaches `requestAuthorization()` |  | open | proposed | `theIntentNeverPromptsForAlarmPermission` | — | docs/plans/F10.md:470-474 |
| F13-M1 | Make `TodoistProjectDTO.color` required rather than `decodeIfPresent` |  | closed | run | `aWorkspaceProjectWithNoColourKeyStillDecodes` | caught — 3 issues across 2 fixtures, every one `.malformedResponse`, i.e. THE WHOLE PAGE refused rather than the row. AND THE ROW-SHAPED TEST STAYED GREEN: `aProjectColourReachesTheMirroredRow`, whose projects all carry a colour, passed throughout. That is the case a row assertion cannot tell from a correct one, and the reason both absent-field fixtures put a populated row AFTER an empty one | docs/reviews/F13.md |
| F13-M2 | Make `TodoistTaskDTO.priority` required |  | closed | run | `aTaskWithNoPriorityKeyStillDecodes` | caught — 1 issue, `.malformedResponse` on the page. Most tasks on most accounts carry no `priority` key, so this is the mutation that would have emptied the picker on a real phone while every fixture that happens to set the field stayed green | docs/reviews/F13.md |
| F13-M3 | Invert the priority mapping — `isMarked` becomes `self == .natural` and `todoistLabel` becomes `P\(rawValue)` |  | closed | run | `priorityIsReadInTodoistsDirection`, asserting on the CAPTURED response from `F13-T5` and never on a hand-built DTO | caught — 8 issues: all four `todoistLabel` pairings, and all four captured tasks. THE FIXTURE IS THE OWNER'S OWN ACCOUNT, task ids and all: *Select 1 to 3 MITs* and *AM Check Inboxes* are wire 4, *Clean the downstairs bathroom* and *fix printer* are wire 1, so the direction is read off what the tasks ARE rather than off documentation | docs/reviews/F13.md |
| F13-M4 | Give two Todoist colour names the same `RGBColor` — `taupe` takes `grey`'s `0xB8B8B8` |  | closed | run | `everyTodoistTintIsDistinct` | caught — 4 issues, the clash reported by name in BOTH appearances and `seen.count == named.count` failing in each. A copy-paste in a twenty-row hex table is the realistic defect, and two projects the same colour is the feature failing silently. Restored: 7 of 7 green | docs/reviews/F13.md |
| F13-M5 | Put `TodoistPalette.berryRed` in `ZenTomato/Views/ProjectPickerView.swift` |  | closed | run | `make lint`, rule `palette_outside_token_layer` | caught — exit 2 with the rule's own widened message, *Screens may only name a ColorRole or a TodoistTint, never a raw palette step*, then exit 0 with the line removed; run in that order. THE REASON `F13-T1` WIDENS THE REGEX IN THE COMMIT THAT CREATES THE SECOND TABLE: the old rule matched the literal `Palette.` and would have let `TodoistPalette.` through a view untouched | docs/reviews/F13.md |
| F13-M6 | Drop the priority clause from `PickerRowView.spokenToggleLabel` |  | closed | run | `spokenTaskLabelNamesPriority` | caught — 1 issue, the spoken label losing *priority P1*. A flag glyph that speaks as nothing is information withheld from exactly the reader who cannot see it, which is why the tint is hidden from VoiceOver and the priority is not | docs/reviews/F13.md |
| F13-M7 | Stop `TodoistCacheStore` copying the colour onto the mirrored row, leaving the DTO field decoded and unused |  | closed | run | `aProjectColourReachesTheMirroredRow` | caught — 4 issues across 2 tests: the mirrored `colorName`s, the tints they resolve to, and the assertion that two projects resolve to DIFFERENT colours, which is the feature's actual claim. The shape `D21`'s `recurrenceReachesTheMirroredRow` already takes: a field that decodes and is then dropped by the writer is invisible to any test that reads the DTO | docs/reviews/F13.md |
| F13-M8 | Build `TodoistTint.byTodoistName` from `rawValue` instead of from `todoistName` |  | closed | run | `everyTintRoundTripsThroughItsTodoistName` | caught — 6 issues, one per multi-word colour, every one of which would have arrived as `unknown` from a real response while a single-word colour still worked. NOT IN THE PLAN'S TABLE: the plan covers the hex values and the lint rule but not the name lookup, which is the seam between Todoist's spelling and ours. Restored: 7 of 7 green | docs/reviews/F13.md |
| F13-M9 | `PickerScreenModel.tint(ofProject:)` returns `projects.first?.tint`, ignoring the id it was given |  | closed | run | `aProjectsTintIsFoundByItsID`, `aProjectWithNoColourStillHasATint`, `anIDTheMirrorDoesNotHoldHasNoTint` | caught — 5 issues across all three. Detectable ONLY because the fixture gives its four projects FOUR DIFFERENT tints, one of them `.unknown`: a corpus where they agreed would satisfy both a correct lookup and one that always returns the first project, which is a tautology with a green tick | docs/reviews/F13.md |
| F13-M10 | Put a backslash-escaped quote back inside an f-string in `scripts/check-todoist-facts.sh`, the defect that shipped there |  | closed | run | `scripts/check_embedded_python.py`, and `make check-embedded-python` | caught — exit 1, the file and embedded line named. THE DEFECT WAS REAL AND PRE-EXISTING, found while adding `CLAIM 4` for `F13-T5`: `CLAIM 2`'s phase-2 verdict block had never compiled, and `json() { python3 -c "$1" 2>/dev/null; }` sent the SyntaxError to a discarded stream — so `--phase2` printed its heading and nothing else, reporting *no evidence* exactly as it would have reported *no finding*. `O12`'s tombstone claim could never have been settled by it | docs/reviews/F13.md |
| F13-M11 | Put the stray backslash back in the facts script's `body()` — `sed '\|$d'`, the defect the owner hit |  | closed | run | `factsScriptSplitsBodyFromStatus` | caught — 44 passed, 1 failed. AND THE FIRST VERSION OF THAT TEST KILLED THE RUNNER instead of failing: under `set -e` a failing command substitution in an assignment aborts the shell, which is the THIRD time today after `gen_or_fail` and `expect_validator_catches`. A test about a broken command must survive the command being broken | docs/reviews/F13.md |
| F13-M12 | The same stray backslash, against the claim run end to end |  | closed | run | `factsClaim4ReportsFromAFixture` | caught — 44 passed, 2 failed. THIS IS THE ONE THAT WOULD HAVE SPARED THE OWNER: `check_embedded_python.py` reported OK throughout, because the embedded Python was never the problem — the shell feeding it was. The claim's real plumbing now runs against a fixture with no token, which is `docs/conventions.md`'s *validate every artefact, not just the primary one* applied to a script a human is told to run | docs/reviews/F13.md |
| F13-M13 | Leave `@MainActor` off `spokenTaskLabelNamesPriority` |  | closed | run | the test process itself | caught, and NOT as a failure — AS A CRASH. `PickerRowView` is a SwiftUI view and every member of one, including a static function that touches nothing, belongs to the main thread. The process died, xcodebuild restarted it eight times, and the summary added totals across launches so the suite's THREE PASSING TESTS were reported as failures. `PickerScreenModelTests.aRowSpeaksItsSecondLine` already carried the annotation and the reason for it; it was not read first. Recorded because the failure mode is a green-looking count over a run that never completed | docs/reviews/F13.md |
| F13-M14 | Wrap a palette reference across a line break in a view: the type name on one line and `.berryRed` on the next |  | closed | run | `make lint`, rule `palette_outside_token_layer` | ESCAPED, then caught once the regex gained `\s*` before the dot. The rule matched `TodoistPalette.` and a line break defeated it, which is the form a line-length fix produces in a view — so the guard had precisely the hole `TodoistPalette.swift`'s own header claimed it had never had. THE OVERCLAIMING SENTENCE IS THE SECOND DEFECT and is corrected: an alias (`typealias TPal = TodoistPalette`) still passes, because a text rule cannot resolve a name, and that is now written down as a limit rather than left as a guarantee. Found by F13's adversarial review | docs/reviews/F13.md |
| F13-M15 | Restore `2>/dev/null` on the facts script's `json()` |  | closed | run | `factsScriptDoesNotSwallowErrors` | ESCAPED — all 46 script tests stayed green — then caught. `probe_facts_claim4.py` DEFINED ITS OWN `json()` and so shadowed the very thing a third of the `CLAIM 4` fix consisted of; the probe now lifts the shipped definition, and a test drives it with a deliberately broken program and asserts the SyntaxError is visible. Found by F13's adversarial review | docs/reviews/F13.md |
| F13-M16 | Three broken embedded programs, one in each form `check_embedded_python.py` did not understand: a heredoc, a double-quoted `-c`, and an unrecognised terminator |  | closed | run | `scripts/check_embedded_python.py`, and `make check-embedded-python` | ESCAPED — *OK, 11 embedded programs in 12 script(s)*, with a script added and the count unmoved — then caught, 3 of 16. The scanner knew two openers and `break`-ed silently on an ending it could not parse, so a program it had never read was indistinguishable from one that compiled. IT NOW READS TWO MORE REAL PROGRAMS THAN BEFORE: `check-watch-provisioning.sh`'s two double-quoted blocks were never checked, and they discard stderr. Found by F13's adversarial review | docs/reviews/F13.md |
| F13-M17 | Delete an embedded program so the count falls below the floor |  | closed | run | `scripts/check_embedded_python.py`'s floor | caught — *found 12 embedded programs, floor is 13*. The check had a count and no floor, in a project whose harness contract requires one: *an exit code cannot show a suite that stopped running*. Found by F13's adversarial review | docs/reviews/F13.md |
| F14-M1 | Delete the freshness check — honour a request of any age |  | open | proposed | `aStaleLaunchIsRefused` | — | docs/plans/F14.md:666-675 |
| F14-M2 | Honour a request id that has already been honoured |  | open | proposed | `oneRequestStartsOneBlock` | — | docs/plans/F14.md:666-675 |
| F14-M3 | Let a remote launch through while authorization is `.notDetermined` |  | open | proposed | `aRemoteLaunchNeverPromptsForPermission` | — | docs/plans/F14.md:666-675 |
| F14-M4 | Attach a wrist-launched pom from the previous block instead of `takeNextAttachment()` |  | open | proposed | `aWristLaunchAttachesLikeAPhoneLaunch` | — | docs/plans/F14.md:666-675 |
| F14-M5 | Add a second call site of `engine.start()` in a view |  | open | proposed | `startHasOneCaller` | — | docs/plans/F14.md:666-675 |
| F14-M6 | Point `startHasOneCaller`'s scan at a directory that does not exist |  | open | proposed | `startHasOneCaller`'s own existence assertion | — | docs/plans/F14.md:666-675 |
| F14-M7 | Add `@Parameter var taskName: String` to any file under `ZenTomato/` |  | open | proposed | `noCaptureAtAnyDoor` | — | docs/plans/F14.md:666-675 |
| F14-M8 | Give `Source` a `mayPromptForPermission` that returns `true` for `.wrist` |  | open | proposed | `aRemoteLaunchNeverPromptsForPermission`, from the other side | — | docs/plans/F14.md:666-675 |
| F14-M9 | Leave the queued block's length out of `WatchStatePublisher.Signature` |  | open | proposed | `anIdleWristSeesASettingsChange` | — | docs/plans/F14.md:666-675 |
| F14-M10 | Send the launch by `transferUserInfo` instead of the fail-fast channel |  | open | proposed | `theLaunchIsNeverQueued` | — | docs/plans/F14.md:666-675 |
| F15-M1 | Add `.font(.system(size: 12))` to `ZenTomato/Views/TimerScreen.swift` |  | open | proposed | `swiftlint --strict`, rule `typography_outside_token_layer` | — | docs/plans/F15.md:450-455 |
| F15-M2 | Add a 21st case to `ColorRole` with both halves supplied |  | open | proposed | `PolishFenceTests.theTokenLayerDoesNotGrow` | — | docs/plans/F15.md:450-455 |
| F15-M3 | Name `import SwiftData` in a `ZenTomato/DesignSystem/` file |  | open | proposed | `WatchLinkTests.noStoreOnWatch` | — | docs/plans/F15.md:450-455 |
| F15-M4 | Restore one `Font.system(size: 11)` in `ZenTomatoWatch/WatchTimerScreen.swift` |  | open | proposed | the watch count assertion from `F15-T1`, expecting 0 | — | docs/plans/F15.md:450-455 |
| F15-M5 | Change one hex in `Design/icon/AppIcon-dark.svg` and leave the PNG alone |  | open | proposed | the icon derivation check (`F15-T5`) | — | docs/plans/F15.md:450-455 |
| F15-M6 | *(only if Ruling A admits `F15-T4`)* make the running segment's fill fraction return 1.0 for every elapsed value |  | open | proposed | the geometry assertion at 40%, read off the drawn path | — | docs/plans/F15.md:450-455 |
| F16-M1 | Add a `consecutiveDays` value derived from the day rows and let it scale the garden |  | open | proposed | `noSurfaceCountsConsecutiveDays` and `theGardenIsAFunctionOfCountAlone` | — | docs/plans/F16.md:285-288 |
| F16-M2 | Change the growth function to take `[StatsDayRow]` instead of `Int`, keeping every name identical |  | open | proposed | `theGardenSeesNoDays` | — | docs/plans/F16.md:285-288 |
| F16-M3 | Make the garden shrink by one plant when the most recent pom is more than seven days old |  | open | proposed | `theGardenSeesNoDays` and `F16-T5`'s assembled-screen assertion | — | docs/plans/F16.md:285-288 |
| F16-M4 | Feed the garden from a stored count written at each block boundary instead of from `StatsQuery` |  | open | proposed | `noNewStoredShape` in `PolishFenceTests` and `theGardenReadsOnlyTheCountingPath` | — | docs/plans/F16.md:285-288 |
| F17-M1 | Change the App Group identifier in one target's entitlement and not the other's |  | open | proposed | `ComplicationBridgeTests.theTwoTargetsNameTheSameContainer` | — | docs/plans/F17.md:507 |
| F17-M2 | Delete the `dependencies:` embed entry that puts this extension inside the watch app |  | open | proposed | `ComplicationTargetTests.theComplicationIsEmbeddedInTheWatchApp` | — | docs/plans/F17.md:536 |
| F17-M3 | Add `ZenTomato/Timer/TimerEngine.swift` to the complication target's sources |  | open | proposed | `ComplicationTargetTests.theFaceOwnsNoTimerAndNoStore` | — | docs/plans/F17.md:554 |
| F17-M4 | Replace the entry's absolute `endsAt` with a remaining duration computed when the timeline is built |  | open | proposed | `ComplicationEntryTests.theFaceCountsFromAnInstantNotADuration` | — | docs/plans/F17.md:584 |
| F17-M5 | Make the entry at `endsAt` render *completed* |  | open | proposed | `ComplicationEntryTests.aPastEndIsNotACompletedPom` | — | docs/plans/F17.md:589 |
| F17-M6 | Make the idle entry render a count of poms completed today |  | open | proposed | `ComplicationEntryTests.theFaceNeverDrawsATally` | — | docs/plans/F17.md:593 |
| F17-M7 | Make the rectangular view read a task title directly and fall back to nothing |  | open | proposed | `ComplicationAttachmentTests.aProjectAttachedBlockStillSaysWhatItIsFor` | — | docs/plans/F17.md:612 |
| F17-M8 | Declare a start intent of the face's own inside `ZenTomatoComplication/` |  | open | proposed | `ComplicationStartFenceTests.theFaceStartsThroughTheOneEntryPoint` | — | docs/plans/F17.md:636 |
| F17-M9 | Remove `ZenTomatoComplication` from the `ZenTomato` scheme's build list, leaving the embed entry untouched |  | open | proposed | `ComplicationTargetTests.theComplicationIsInTheScheme` | — | docs/plans/F17.md:558 |
| F18-M1 | Give this intent an `@Parameter` naming a task to start on |  | open | proposed | `noCaptureAtAnyDoor` | — | docs/plans/F18.md:376-379 |
| F18-M2 | Return a bare `.result()` on the unreachable path |  | open | proposed | `everyWristLaunchOutcomeSaysSomething` | — | docs/plans/F18.md:376-379 |
| F18-M3 | Add a second `sendMessage` call site in the watch target |  | open | proposed | `theWristHasOneSender` | — | docs/plans/F18.md:376-379 |
| F18-M4 | Build the intent's request with no `requestedAt`, or a zeroed one |  | open | proposed | `aStaleWristIntentLaunchIsRefused` | — | docs/plans/F18.md:376-379 |
| F19-M1 | Widen the window predicate to always return `true` |  | open | proposed | `undoIsRefusedOutsideTheWindow` | — | docs/plans/F19.md:353 |
| F19-M2 | Make the project filter return every task in the mirror rather than the project's |  | closed | run | `searchIsScopedToTheOpenProject`, `searchFiltersTasksAndNeverDropsASection` | caught — 3 issues across 2 tests, the load-bearing one being `fromP1.isEmpty`. DETECTABLE ONLY BECAUSE THE FIXTURE HOLDS A MATCHING TASK IN ANOTHER PROJECT: `p2` has *Plan deep work for Thursday* and `p1` has nothing matching *deep*, so an unscoped search leaks a visible row across the boundary. A fixture whose query matched only inside the open project would satisfy the right implementation and the wrong one alike — the tautology `docs/conventions.md` names, which `F19`'s own plan called out in advance and built the fixture to avoid | docs/plans/F19.md:389 |
| F19-M3 | Delete the allowlist line and leave the constant — then the reverse |  | open | proposed | `endpointTableMirrorsTheAllowlist` | — | docs/plans/F19.md:427 |
| F19-M4 | Make the reopen path write its record but leave the completion counted |  | open | proposed | the golden export assertion | — | docs/plans/F19.md:453 |
| M1 | Settings presented with `.notAsked` instead of the live value |  | closed | run | `settingsIsGivenTheRealState` | caught at 468 | docs/plans/F4c.md:126-137 |
| M2 | The picker reaches past the shared function to its own constants |  | closed | run | `thePickerAndSettingsCannotDisagree` | caught at 468 | docs/plans/F4c.md:126-137 |
| M3 | Two states given the same word |  | closed | run | `theSixStatusesAreDistinct` | caught at 468 | docs/plans/F4c.md:126-137 |
| M4 | A status reading `Error!` |  | closed | run | `nothingHereSoundsLikeAnError` | caught at 468 | docs/plans/F4c.md:126-137 |
| M5 | The section removed from the `Form` body, leaving it orphaned |  | closed | run | `settingsShowsTheState` | caught at 468 | docs/plans/F4c.md:126-137 |
| M6 | Music placed below Todoist instead of above |  | closed | run | `settingsShowsTheState` | caught at 468 | docs/plans/F4c.md:126-137 |
| M7 | The refresh removed from the section |  | closed | run | `settingsAsksAgainWhenItOpens` | caught at 468 | docs/plans/F4c.md:126-137 |
| M8 | Settings handed the do-nothing preview refresh |  | closed | run | `settingsAsksAgainWhenItOpens` | caught at 468 | docs/plans/F4c.md:126-137 |
| M9 | `.notAsked` reverted to the "ready" sentence |  | closed | run | `theSixStatusesAreDistinct` | caught at 468 | docs/plans/F4c.md:126-137 |
| M10 | The middle link dropped — `SettingsView` stops passing either value on |  | closed | run | `theMiddleLinkIsWired` | caught at 468 | docs/plans/F4c.md:126-137 |
| M11 | The section rendered conditionally, `if musicAvailability != .notAsked` |  | closed | run | `settingsShowsTheState` | caught at 468 | docs/plans/F4c.md:126-137 |
| M12 | The refresh demoted to a comment |  | closed | run | `settingsAsksAgainWhenItOpens` | caught at 468 | docs/plans/F4c.md:126-137 |
| M13 | `.ready` reverts to the invented `Ready` |  | closed | run | `theVocabularyIsTodoistS` | caught at 470 | docs/plans/F4d.md:60-62 |
| M14 | Todoist renamed to `Signed in`, music left behind |  | closed | run | `theVocabularyIsTodoistS` | caught at 470 | docs/plans/F4d.md:60-62 |
| M15 | The way out removed from the connected footer |  | closed | run | `aConnectedServiceSaysHowToDisconnect` | caught at 470 | docs/plans/F4d.md:60-62 |
| M16 | An empty query returns nothing instead of the whole library |  | closed | run | `anEmptyQueryGivesTheLibraryBack` | caught at 483 | docs/plans/F4e.md:126-129 |
| M17 | The paging reset removed from the view |  | closed | run | `theViewResetsThePagingWhenTheQueryChanges` | caught at 483 | docs/plans/F4e.md:126-129 |
| M18 | Matching narrowed to prefix-only |  | closed | run | `ignoresCapitalsAndAccents` | caught at 483 | docs/plans/F4e.md:126-129 |
| M19 | The empty state becomes a `ContentUnavailableView` — a slot for an action |  | closed | run | `theEmptyStateHasNoSlotForAnAction` | caught at 483 | docs/plans/F4e.md:126-129 |
| M20 | The original data-race warning reintroduced into the Release build |  | closed | run | `scripts/check-release-build.sh` — the gate failed with the file and line | — | docs/chores/C12.md:76 |
| M22 | A break plays whenever music is switched on |  | closed | run | `aBreakIsSilentEvenWithMusicSwitchedOn` | — | docs/plans/F4f.md:129 |
| M23 | The request is never cleared at a boundary |  | closed | run | `theRequestDiesWithTheBreakItWasAbout`, `aSecondBreakStartsSilent` | — | docs/plans/F4f.md:130 |
| O38-M1 | Send the reader to the Settings menu instead of naming the `Save to settings` button that is already on the screen |  | closed | run | `theCopysClaimsAreTrueOfTheAppItDescribes`, and the two pinned copy assertions | caught — 3 issues. This is not a hypothetical: it is the OWNER'S FIRST DRAFT of this sentence, and it would have shipped a true-sounding instruction that walks past the control doing exactly what it asks for. The check is that the copy names `saveLabel`, so the sentence and the button cannot drift apart | docs/plans/00-register.md |

## Hooks (H)

**Opened 2026-09-23 by `C33`, implementing `D38`, with full backfill.** Traced *spec invariant →
mechanism → owning unit*, which are the `Invariant`, `Mechanism` and `Owning unit` columns.
`Title` is the hook's short name and exists because `scripts/gen_status.py` prints `h.get('title')`
under a column it labels `Hook`.

**THE COUNT IS THIRTY-ONE IN-TREE MECHANISMS, AND IT IS NOT FIFTEEN.** `D38` refused to inherit
`docs/handoffs/blockersfor1_5.md:71`'s *"15 real enforcement mechanisms"* because no command had been
shown to reproduce it, and made establishing the real count the first work of this chore. **A
mechanism is a check that (a) is invoked by a named command, (b) can FAIL and thereby stop a commit, a
CI run or a test run, and (c) defends a stated spec invariant or a ratified decision rather than a
formatting preference. It is counted ONCE AT ITS DEFINITION SITE, never once per surface that invokes
it** — `scripts/check-secrets.sh` runs at pre-commit and in CI and is one row with two values in
`Runs at`. Counting invocations instead of definitions is how a count inflates without anything being
built. The enumerating command, the twelve/one/seventeen breakdown, and the exclusions — which are the
argument, and the criterion by which a Swift suite is a mechanism — are in `docs/chores/C33.md`.
**It was thirty until this register was reviewed.** The enumeration's Swift half was a hand-written
`ls` of seventeen named files, so it reproduced its own answer instead of deriving it, and it missed
`ZenTomatoTests/TodoistEndpointTests.swift` — which defends the project's loudest non-negotiable and
meets every part of the criterion. That is `H32`, added after review. **The tree was not touched to
reach any number.** Fifteen was
not reproducible under any defensible rule; the near misses are ten `scripts/check-*.sh` excluding
`install-device`, twelve shell/Python checks, sixteen CI steps including setup, and seven hook
invocations, and it matches none of them.

**THE TABLE HAS 32 ROWS AGAINST 31 IN-TREE MECHANISMS.** The extra is `H31`, GitHub branch protection:
real, named in `CLAUDE.md`'s hook table, and outside this repository, so no command here reproduces
it. Leaving it out would make the register disagree with `CLAUDE.md`; writing it `closed` would be the
confident-sentence failure `docs/conventions.md` names.

**ALLOCATION ORDER, so the next allocator does not have to guess and the allocation is reproducible:**
`H1`, `H2` and `H3` keep the meanings `docs/chores/C6.md` fixed for them; then the remaining
shell/Python checks in `ls scripts/check-*.sh` order, then `gen_status.py`, then
`run-script-tests.sh`, then the SwiftLint custom rule, then the Swift contract/fence suites in `ls`
order, then `H31`. **`H32` is out of that order**, allocated after the adversarial review found a
mechanism the `ls` line had missed; an ID is an identifier and not an index, so the table is not
renumbered to close the seam — `docs/conventions.md` Axis 1, not `D17`, which is about session
plans; see the same correction under `## Mutations (M)`.

**`Proved by: never` IS THE HONEST AND COMMON ANSWER — nineteen of thirty-two — AND IT IS THE POINT
OF THE COLUMN.** A mechanism that exists but has never been broken to watch it fail is `open`, not
`closed`: *a hook that has never failed is a hook nobody knows works.* **THIRTEEN ROWS ARE `closed`
AND EVERY ONE OF THEM NAMES THE MUTATION THAT PROVED IT.** The count is derivable from the table and
is not asserted here: `Status: closed` and a `Proved by` cell containing the word `never` cannot both
be true of one row, and no row satisfies both.

**FOUR ROWS SHIPPED VIOLATING THAT RULE AND WERE CORRECTED 2026-09-23 by `C33`'s adversarial
review.** `H5`, `H7`, `H15` and `H30` were written `closed` beside `Proved by: never` — `H5`'s own
`Why` said *"Nothing has ever broken this script to watch it fail"* next to the word `closed` — and
the preamble's own tally said sixteen where the table held eighteen, missing exactly two of the four.
**The miscount and the wrong statuses were the same error twice**, and the generated page carried it:
`docs/plans/00-status.md` published *"17 of 31 built"* where the register's own derivation gives
thirteen. Each of the four carries the correction in its `Why` rather than being quietly reset.

`Owning unit` is read out of
`git log --oneline --diff-filter=A` on the mechanism's own file, so two rows record a scope that is
wrong rather than a scope that should have been right.

**THREE MECHANISMS ARE WIRED TO NOTHING** — `H6`, `H10` and `H12`. They appear in no hook, no CI step
and no `make` target; `grep -rl` for their names hits only documentation and themselves. `Runs at` is
`manual` and `Status` is `open`, because a hook register that reported protection this project does
not have would be worse than no register.

**`Invariant` is sourced, never composed.** It comes from `CLAUDE.md`'s hook table where that names
one, from the script's own header comment otherwise, and from the suite's `///` docblock for the Swift
fences. Where no invariant could be sourced the cell would read `unsourced`; none did.

| ID | Title | P | Status | Invariant | Mechanism | Owning unit | Runs at | Proved by | Why |
|---|---|---|---|---|---|---|---|---|---|
| H1 | the delta-integrity walk |  | closed | Every `D<n>` cited in the tree has a `## D<n>` heading in `00-deltas.md` — the `D14` failure | `ZenTomatoTests/DeltaIntegrityTests.swift` | C6 | make test | mutation: removing `D14`'s heading fails `everyCitedDeltaIsDefined` and names all ten citing files (`docs/chores/C6.md:309`) | `C6.md:197`. Five assertions. |
| H2 | the spec-amendment ratchet |  | closed | Every ratified delta proposing spec text appears in its baseline's *Amendments applied* list | `ZenTomatoTests/AmendmentRatchetTests.swift`, cited by the string ``/// `everyRatifiedSpecAmendmentIsApplied` — H2.`` | C6 | make test | mutation: lowering the baseline to 8 fails the ratchet (`docs/chores/C6.md:311`); re-proved by `C32-M1`–`C32-M12` | **`D38` and `docs/handoffs/blockersfor1_5.md:73` both locate this doc comment at `ZenTomatoTests/DeltaIntegrityTests.swift:195`. IT IS NOT THERE** — `C32` moved it, and `grep -rn 'H2' ZenTomatoTests/` returns exactly one hit, `AmendmentRatchetTests.swift:217`. The row cites the string and never the line, as `D38` instructs. The delta is not edited. |
| H3 | the read-before-overwrite guard |  | closed | A document is not replaced wholesale without a declaration in the commit message — the `docs/plans/F7.md` loss | `scripts/check-wholesale-rewrite.sh` | C6 | commit-msg | mutation: replacing `docs/plans/F7.md` is refused at 99% (`docs/chores/C6.md:312`); four script tests | `.githooks/commit-msg:25` is the only invocation. |
| H4 | the licence-redescription guard |  | closed | One licence, one pledge: GPL-3.0-or-later everywhere plus an App Store exception, never described as alternatives | `scripts/check-licence-wording.sh` | C18 | pre-commit · CI (`make check-licence`) | five script tests, three of them mutations: `licenceCheckCatchesADisjunction`, `licenceCheckCatchesAPerChannelGrant`, `licenceCheckCatchesTheBinaryPhrasing` |  |
| H5 | the lint gate |  | open | `CLAUDE.md`: SwiftLint, `--strict` — every warning is an error | `scripts/check-lint.sh` | F1 | pre-commit · CI (`make lint`) | never | The script is the runner; `H15` is the one rule with its own invariant. Nothing has ever broken this script to watch it fail. **STATUS CORRECTED TO `open` 2026-09-23** by `C33`'s adversarial review: this row shipped `closed` beside `Proved by: never`, against this section's own rule, in the same paragraph that states it. |
| H6 | the MusicKit App ID check |  | open | The app is signed with an explicit App ID that can carry MusicKit, not the team wildcard | `scripts/check-musickit-entitlement.sh` | C11 | manual | never | **WIRED TO NOTHING.** `grep -rl` for its name hits only itself and documentation: no hook, no CI step, no `make` target. |
| H7 | the register renders as tables |  | open | `docs/reviews/OPEN.md` is the one place every outstanding item lives; a malformed row ends the table and hides the rest | `scripts/check-open-register.sh` | C18 | pre-commit · CI (`make check-register`) | never | Built after six items vanished from the owner's reader. The failure has been seen in the wild but the script has never been broken to watch it fail. **STATUS CORRECTED TO `open` 2026-09-23** by `C33`'s adversarial review: a failure seen in the wild is not a failure the script has been seen to catch, and `closed` beside `Proved by: never` is the false assurance `D38` opened this register to prevent. |
| H8 | the shipping configuration compiles clean |  | closed | The Release configuration builds with no warning whose path is inside this checkout | `scripts/check-release-build.sh` | C12 | CI (`make check-release`) | `M20` — the original data-race warning reintroduced; the gate failed with the file and line |  |
| H9 | the secret scan |  | closed | `CLAUDE.md`, non-negotiable: *Secrets never enter the tree* | `scripts/check-secrets.sh` | F1 | pre-commit (`--staged`) · CI (`make check-secrets`) | `secretsFileIsGitIgnored` in `scripts/tests/run-script-tests.sh` |  |
| H10 | three Todoist API facts against a live token |  | open | `F3b` and `D22` rest on three undocumented Todoist behaviours, each flagged NOT TESTED | `scripts/check-todoist-facts.sh` | F3b | manual | never | **WIRED TO NOTHING**, and it needs a live token. Already tracked as `O12`. |
| H11 | the Todoist endpoint allowlist |  | closed | `CLAUDE.md`, non-negotiable: the only write to Todoist is *complete task* — never create, update or comment | `scripts/check-todoist-writes.sh` | F1 | pre-commit · CI (`make check-todoist`) | four script tests, three of them mutations: `noWritesHookCatchesNewEndpoint`, `noWritesHookCatchesBarePath`, `noWritesHookCatchesBuilderPath` |  |
| H12 | the watch app can actually install |  | open | A watch app whose provisioning does not cover the watch is silently declined by iOS | `scripts/check-watch-provisioning.sh` | C8 | manual | never | **WIRED TO NOTHING.** Named in `O13`'s closure as the check, and invoked by no hook, CI step or `make` target. |
| H13 | the status page is generated, not hand-written |  | closed | `O42`: `00-status.md` claimed a generator that existed in no branch and no commit | `scripts/gen_status.py --check` | C28 | pre-commit · CI (`make check-status`) | `C28-M1`, `C28-M2`, `C28-M3`; and `C33-M1` |  |
| H14 | the mechanism that tests the mechanisms |  | closed | A hook that has never failed is a hook nobody knows works — `conventions.md` | `scripts/tests/run-script-tests.sh` | F1 | CI (`make script-tests`) | `C33-M2`, `C33-M3` and `C33-M4` broke `gen_status.py` in both directions and this suite went red | 21 assertions — `bash scripts/tests/run-script-tests.sh` prints `21 passed, 0 failed`. **This cell said 20 until 2026-09-23:** it was written in the same commit that added the 21st, `statusPageNamesTheAgentRegister`, and `C33`'s second review pass caught it. |
| H15 | Palette outside the token layer |  | open | A screen may name a `ColorRole` and may never name a `Palette` step — the two-layer rule | `.swiftlint.yml` custom rule `palette_outside_token_layer` | F1 | pre-commit · CI (via `make lint`) | `F15-M1` proposes the sibling typography rule's mutation; this rule itself: never | Counted apart from `H5` because it defends its own invariant and is separately mutation-testable: `check-lint.sh` is the runner, the rule is the fence. **STATUS CORRECTED TO `open` 2026-09-23** by `C33`'s adversarial review: `F15-M1` is *proposed* against the sibling typography rule, so nothing has broken this rule to watch it fail. |
| H16 | the amendment declaration rule |  | closed | Which baseline a delta is talking about — `D41`'s Hole 2, at the resolution step | `ZenTomatoTests/AmendmentDeclarationTests.swift` | C32 | make test | `C32-M7`, `C32-M8`, `C32-M9`, `C32-M11` |  |
| H17 | the citation walk's own surface |  | open | The exclusion list must not quietly stop the citation walk looking at anything | `ZenTomatoTests/DeltaCitationSurfaceTests.swift` | C1 | make test | never | **The owning unit is what git says and not what it should be:** `cb06b83` is scoped `fix(C1)`, and `C1` is the repo-setup chore. Recorded as found rather than corrected. |
| H18 | the licence fence |  | open | One licence, one pledge, and neither may quietly become something else | `ZenTomatoTests/LicenceFenceTests.swift` | C18 | make test | never |  |
| H19 | the F4 fence |  | open | The fence around `F4`, as tests rather than as promises | `ZenTomatoTests/MusicFenceTests.swift` | F4 | make test | never |  |
| H20 | no capture in the music picker's search |  | open | The no-capture rule over the music picker's search | `ZenTomatoTests/MusicNoCaptureTests.swift` | F4e | make test | never |  |
| H21 | the no-capture surface |  | open | `CLAUDE.md`, standing rule: the app never accepts a new task from the user | `ZenTomatoTests/NoCaptureSurfaceTests.swift` | F3 | make test | never |  |
| H22 | the polish fence |  | closed | What `F6b` may not do, enforced rather than promised — `AppSettings` at six columns, the `@Model` count, no new persistent surface | `ZenTomatoTests/PolishFenceTests.swift` | F6b | make test | `F8-M5` |  |
| H23 | the privacy manifest holds its word |  | open | The manifest declares no tracking, nothing collected and no required-reason API, and keeps saying it | `ZenTomatoTests/PrivacyManifestTests.swift` | O18 | make test | never | **The owning unit is a register ID, which Axis 2 forbids in a commit scope:** the file arrived as `chore(O18)`. Recorded as found; there is no `F`/`C` unit behind it. |
| H24 | the reflection sheet waits for the alarm |  | open | `D26`: the reflection sheet does not cover the Silence button | `ZenTomatoTests/ReflectionWaitsForAlarmFenceTests.swift` | F2d | make test | never |  |
| H25 | the session-plan fence |  | open | The fence around the session plan, as a test rather than as a promise | `ZenTomatoTests/SessionPlanFenceTests.swift` | F3 | make test | never |  |
| H26 | the shape screen writes nothing |  | closed | `F8` Ruling B — a control writes nothing — and the way in is closed while a block runs | `ZenTomatoTests/ShapeScreenFenceTests.swift` | F8 | make test | `F8-M9` (which **escaped** on its first application) |  |
| H27 | running a shape never writes the settings |  | closed | `F8` Ruling B, as a fence: running a shape never writes the settings | `ZenTomatoTests/ShapeSettingsFenceTests.swift` | F8 | make test | `F8-M2` |  |
| H28 | the silence control's shape |  | open | `D26` — the shape of the timer screen while an alarm rings | `ZenTomatoTests/SilenceControlFenceTests.swift` | F2d | make test | never |  |
| H29 | the F6 fence |  | open | The fence around `F6`, executed rather than promised | `ZenTomatoTests/StatsFenceTests.swift` | F6 | make test | never |  |
| H30 | what may live on the wrist |  | open | What may and may not live on the wrist, and what crosses between the two | `ZenTomatoTests/WatchLinkTests.swift` | F7 | make test | `F15-M3` proposes one; today: never | **STATUS CORRECTED TO `open` 2026-09-23** by `C33`'s adversarial review: `F15-M3` is *proposed* against an unbuilt feature, so today this suite has never been seen to fail. |
| H31 | branch protection on `main` |  | open | `CLAUDE.md`: PR required, CI must pass, rebase-and-merge only, linear history, no force pushes | GitHub repository settings — **not a file in this checkout** | C1 | GitHub | never | **No command in this repository can check this**, and `conventions.md` forbids ratifying a claim about another system until something has run. `O16` records it configured on 2026-08-24. Written as `open` rather than `closed` because *configured* is what somebody intended and this register can show no output. **This is the 32nd row against 31 in-tree mechanisms** — `grep -c '^ H' docs/plans/00-register.md` returns 32, and this row is the one that is not in the tree. The cell said *31st against 30* until 2026-09-23, the figures from before `H32` was added, and it then disagreed with this section's own preamble in one read. |
| H32 | the Todoist write is the only Todoist write |  | open | `CLAUDE.md`, non-negotiable: the only write to Todoist is *complete task* — and the allowlist script compares addresses, not methods | `ZenTomatoTests/TodoistEndpointTests.swift` | F3 | make test | never | **ADDED 2026-09-23 by `C33`'s adversarial review, out of the allocation order above, because renumbering is forbidden** (`D17`, and `docs/conventions.md`: an identifier, not an index). It meets all three parts of the criterion and was missed by the hand-listed `ls` line the enumeration used — the defect that line was flagged for. It is not a duplicate of `H11`: its own docblock states the gap it fills, *“the script compares addresses, not what is done at them … creating a task and reading tasks are the same address; only the method differs”*, so `H11` and `H32` defend the same invariant at two different layers and each can fail without the other. |

## Risks (RR)

**Opened 2026-09-23 by `C33`, implementing `D38`. These are SEEDS, not a backfill.** `RR` has no
backlog to recover, and `D38` says in as many words that seeds are honest where a backfill would be
invention. **Each of the four carries a file and a line.** A fifth was looked for and refused:
TestFlight timing, App Store review and the hard stop are real but are evidenced nowhere citable, and
the rule about a claim recorded before it is verified applies to a risk row as much as to anything
else.

**`Likelihood` IS FILLED ON ALL FOUR ROWS, AND THREE OF THE FOUR ARE NOT PROBABILITIES.** The column
shipped as `—` on 4 of 4 rows and was filled 2026-09-23 after `C33`'s adversarial review pointed out
that `docs/conventions.md` defines this register by exactly five contents — *risk, likelihood, impact,
mitigation, owner* — and the one being opened was empty on its defining field, which a validator
cannot tell from *not applicable*.

**It is deliberately not a pinned `low`/`medium`/`high` enum.** Two of these risks have no probability
to state and pinning three words would have forced an invention: `RR1` cannot occur at all while the
v1.5 scope holds, and `RR4` has already occurred. So the cell says which kind of statement it is
making, and every row's `Evidence` carries the sentence behind it. `certain — already happened`
(`RR4`) is an **observation**: the harm is in the data now. `none while v1.5 holds` (`RR1`) is
**structural**: there is no mechanism by which it can occur until a delta admits one. Anything else
(`RR2`, `RR3`) is **an assessment made by the agent on 2026-09-23, not a measurement, and the owner may
overrule it** — the same standing as the `A` register's priorities, and stated rather than dressed as
contemporaneous.

**The vocabulary is written as prose and NOT as a second markdown table, and that is not a style
choice.** `scripts/gen_status.py`'s `parse_tables` takes a section's field names from the **first**
table it meets in that section and never resets them, so a `| Value | Means |` legend placed above the
`RR` table would have made every risk row parse under the headers `value` and `means`, lose its `id`,
and vanish from a page CI keeps current — **four rows silently, on the day the register opens**. The
draft of this paragraph did exactly that and was caught by generating the page. It is `C34`'s row shape
13.

| ID | Title | P | Status | Likelihood | Impact | Mitigation | Owner | Evidence |
|---|---|---|---|---|---|---|---|---|
| RR1 | iCloud/CloudKit sync would put the log where a conflict can lose it | P0 | open | none while v1.5 holds | the crown jewel | none today; the risk is the scope decision itself | owner | `docs/specs/zenpom-v1.5.md:185`, verbatim: *"A platform, and **the highest-risk item on either list.** The log is local-only today with no conflict resolution to get wrong. Sync is where the crown jewel would be lost."* The project's own words for its worst case. **Likelihood is `none while v1.5 holds` and not a probability**: sync is not in the v1.5 scope, so there is no mechanism by which this can occur until a delta admits one. The row exists so that delta meets the risk already written down. |
| RR2 | Changing the bundle identifier orphans the SwiftData store holding the distraction log | P0 | open | low, and conditional | total loss of the data `O1` needs | `C26`'s standing DO NOT | owner | `docs/chores/C26.md:25-33`: *"A bundle identifier is not a name, it is an identity, and changing it makes a different app"* / *"Changing the bundle identifier would destroy the data O1 needs before O1 has ever been run."* The App Store rejected the name `ZenTomato`, which is what makes this live rather than theoretical. **Likelihood assessed 2026-09-23 by `C33`, not measured**: the trigger is a deliberate human edit to one Xcode setting, guarded by `C26`’s standing DO NOT and by `O1`’s dependence on the store — low, but it is the App Store rejection that keeps it non-zero. |
| RR3 | The shape cursor and `TimerState` advance non-atomically, and control flow now derives from the seam | P1 | open | low per kill, unbounded over time | a lost block, or a sprint ending one block early | none | agent | `docs/reviews/F8.md:137-140`: *"the shape cursor (UserDefaults) and TimerState (SwiftData) advance non-atomically, so a process kill between advanceShape() and persist() loses a block. Pre-existing — but 2fab614 now derives control flow from it."* One row, not two: the sibling defect two lines up — `shapeJustSpent` derived from two separate `UserDefaults` reads either side of `advanceShape()`, where a failed decode ends the sprint a block early — is the same seam. **Likelihood assessed 2026-09-23 by `C33`, not measured**: the window is the microseconds between `advanceShape()` and `persist()`, so any single process kill is unlikely to land in it, but `F8` is halted at `T3` with no mitigation, so the exposure accumulates for as long as that holds. No measurement exists and none is claimed. |
| RR4 | `O1` will be judged against a tally the app itself inflated, and nothing can remove the rows | P1 | open | certain — already happened | the one judgement the project turns on | **none exists by design** | owner | `docs/reviews/OPEN.md:155-169`. The owner: *"tapping the app in the watch has a long delay — more than 2 seconds — and it led to an excessive number of I and E clicks."* The lag is fixed; the data is not: *"The taps already made are real rows and are still in the database. There is no capture surface and no editor, by design, so nothing in the app can remove them. Any export covering that block will over-report."* **Likelihood is `certain — already happened`, not a forecast**: the inflating rows are in the database now, by the owner’s own report, and there is no capture surface and no editor to remove them. This is the one RR row whose likelihood is an observation rather than an assessment. |

## Closed in prose, not by a strike

These rows are **not struck through** in `OPEN.md`; the review prose
records them finished. They are held closed here, with the sentence
that says so, so the claim can be checked rather than taken.

- `O4` — O4 closes — all three interruption paths now exercised.
- `O5` — Playlist looping: pass. "the playlists repeats." O5 closes.

## Defect — reused IDs

Each ID below names **two or more different rows** in `OPEN.md`. The
tally beside it is counted from the source, not asserted. All-closed is
a naming collision only; **two open rows is a sync defect**, because
one `td:` id cannot link two rows and the register cannot tell them
apart. `docs/conventions.md` Axis 1 forbids renumbering to close a
*gap* — this sentence cited `D17`, which is about session plans, until
`C34`'s review — and a *collision* is the opposite case, needing the
owner's ruling on which one gets a new number.

- `O23` — 1 closed, 1 open
- `O24` — 2 closed, 0 open
- `O25` — 1 closed, 1 open

*The line that stood here was an extractor's summary — *"Extracted 16 open owner items, 1 proposed
deltas, 8 chores — 25 syncable"* — left behind by a tool that does not exist in this checkout, against
a file that now holds 26 chore rows and four registers that tool has never heard of. **Removed
2026-09-23 by `C33`; see the corrected header.** The current counts are in
`docs/plans/00-status.md`, which is generated from this file by `scripts/gen_status.py` and which CI
keeps current — a number that can go stale does not belong in a hand-maintained source.*
