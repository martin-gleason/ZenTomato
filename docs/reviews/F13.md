# F13 — Todoist, with Todoist's flair · review log

**Gate:** the owner's *"continue on features for v1.5"*, 2026-09-24. The six questions in
`docs/plans/F13.md` §10 were answered with that plan's own stated recommendations rather than sent
back; §11 tables them so each is visible and reversible.

**Status:** `F13-T1`–`T4` built. `T5` step 1 is run and recorded; steps 2 and 3 — the device check —
are the owner's and are what close the feature.

---

## What was built, and the one fact that shaped it

**The v1.5 spec says the colours and flags are *"already fetched and cached, and currently thrown
away."* That is half right, and the wrong half was worth twenty minutes of reading.** They are fetched
and they are thrown away — at the DTO boundary, one layer earlier than the sentence implies. They were
never decoded and never stored, which is the difference between a one-task feature and a five-task
one. `docs/plans/F13.md` §1 has the evidence, file by file.

### `F13-T1` — the tint vocabulary

Three files under `DesignSystem/` and one line of `.swiftlint.yml`. **Nothing imported them**, so the
app on the phone was byte-for-byte unchanged and the design argument could be settled before anything
was fetched or drawn.

**The table is transcribed from one cited source, named in the file header** — `src/utils/colors.ts`
in Doist's own TypeScript SDK at commit `19798a37`, read 2026-09-24. The plan warned that *"no two of
Todoist's published colours share a hex value"* was **expected, not established**, and that writing an
unchecked assumption about an external system into a test is the worst place for one. It was checked
against the source before the test was written: twenty colours, no two sharing a value. Had two
collided, `everyTodoistTintIsDistinct` would have been the wrong artefact and the table the right one.

**Light and dark are the same value for every tint, deliberately.** These colours are chosen by the
user in another app; lightening one for dark mode would be this app editing somebody else's project
colour so that our page looks better. Visibility is bought by the unconditional `borderStrong` ring
instead — measured at 3.12:1 or better on every ground in both appearances — which is what WCAG 1.4.11
is actually asking for.

**The lint rule was widened in the commit that created the second table.** The old regex matched the
literal `Palette.`, so `TodoistPalette.` in a view would have passed it — a guard with a hole, which
that rule's own comment already names as the thing worth avoiding.

### `F13-T2` — the two fields reach the mirror

`TodoistProjectDTO` gained an optional `color`; `TodoistTaskDTO` an optional `priority`.
`CachedProject.colorName` and `CachedTask.priority` are the two new columns, both optional with
defaults, following `CachedTask.isRecurring`'s precedent exactly so an existing store migrates
lightweight. `TodoistCacheStore.replaceEverything` copies both across.

**`colorName` and not `color`, because it is not a colour** — it holds the key from Todoist's palette,
and a column called `color` holding the seven characters `berry_red` is the kind of name that sends
the next reader looking for a hex.

**Neither field is interpreted, and that is what keeps `T2` free of predictions.** The colour stays
the name Todoist sent; the priority stays the number on the wire. Which end of Todoist's range is
urgent is not settled by reading, `F13-T5` step 1 settles it against a real account, and `F13-T4` maps
it for the screen. A column holding an already-interpreted level would have to be rewritten if that
reading turned out backwards. The wire value never needs rewriting.

**Both contract corrections are in `T2`'s own commit, not a later tidy-up.** `F3-contract.md` §3.2
struck both fields, recorded the authority (`zenpom-v1.5.md` lists `F13` as owing no delta — the
ratified authority `D21` had to be written to supply), and kept the old `color` reasoning on the page
with an explanation of why it no longer holds rather than deleting it. `TaskPickerView`'s header said
*"no priority"* and gave a two-part reason; the first half stopped being true, so the second half —
**nothing sorts by priority** — is now stated on its own as the live guarantee.

---

## Tests added

| Suite | Test | What it protects |
|---|---|---|
| `TodoistTintTests` | `theTableIsTheSizeOfTodoistsPalette` | Todoist adding a colour is noticed, not guessed at |
| | `everyTodoistTintIsDistinct` | a copy-paste in a twenty-row hex table, in both appearances |
| | `everyTintRoundTripsThroughItsTodoistName` | the seam between Todoist's spelling and ours |
| | `theNamesAreTheSpellingTodoistUses` | `berry_red`, and a single word gaining no underscore |
| | `anUnrecognisedColourFallsBackRatherThanFailing` | a colour from the future is drawn, not dropped |
| | `camelCaseIsNotATodoistName` | the direction that would otherwise pass by accident |
| | `aTintIsNotReinterpretedForDarkMode` | the light/dark decision is a record, not an omission |
| `TodoistFlairCacheTests` | `aProjectColourReachesTheMirroredRow` | the field is copied, not merely decoded |
| | `aWorkspaceProjectWithNoColourKeyStillDecodes` | **the whole page**, not the row |
| | `aTaskPriorityReachesTheMirroredRow` | the wire value arrives verbatim |
| | `aTaskWithNoPriorityKeyStillDecodes` | **the whole page**, not the row |

**`TodoistFlairCacheTests` is a file of its own and not by preference.** These tests belong beside
`TodoistCacheTests`, where the same boundary is tested for every other field, and they were written
there first. The 400-line cap in `.swiftlint.yml` is what moved them.

**Every absent-field test asserts on the page, and that is the whole design of those two fixtures.** A
required field does not fail one row: the decoder fails the element, `TodoistPage` fails with it, and
the refresh returns nothing at all — an empty picker on somebody's phone and a green suite everywhere
else. So each fixture puts a populated row *after* an empty one, which is a case a row-shaped
assertion cannot tell apart from a correct one.

---

## Mutations

Recorded as `F13-M1`–`F13-M8` in `docs/plans/00-register.md`. **Open Question F is closed and not by
this feature**: the plan said `M` had nowhere to live and that the register had no Mutations section.
`D38` opened one, `C33` backfilled it with 119 rows, and `scripts/extract_zenpom.py` — the script the
question was about — no longer exists. The chore that paragraph said somebody should open was opened
and closed while `F13` waited.

| ID | mutation | result |
|---|---|---|
| `F13-M1` | `TodoistProjectDTO.color` required | caught — 3 issues, all `.malformedResponse`: **the page**, not the row. The row-shaped test stayed green |
| `F13-M2` | `TodoistTaskDTO.priority` required | caught — 1 issue on the page. Most tasks carry no `priority`, so this is the one that empties a real picker |
| `F13-M4` | two colour names share a hex | caught — 4 issues, the clash named in both appearances |
| `F13-M5` | `TodoistPalette.` in a view | caught — `make lint` exit 2 with the widened message, then exit 0 |
| `F13-M7` | the writer drops the colour | caught — 4 issues, including that two projects resolve to **different** colours |
| `F13-M8` | the lookup built from `rawValue` | caught — 6 issues, one per multi-word colour |
| `F13-M9` | `tint(ofProject:)` ignores its id | caught — 5 issues across all three `T3` tests |
| `F13-M10` | the facts script's f-string defect put back | caught — exit 1, file and embedded line named |

| `F13-M3` | the priority mapping inverted | caught — 8 issues: all four label pairings and all four captured tasks |
| `F13-M6` | the spoken priority dropped | caught — 1 issue, the label losing *priority P1* |
| `F13-M13` | `@MainActor` left off the spoken-label test | **caught as a CRASH, not a failure** — see below |

**`F13-M13` is not a mutation I planned; it is one I caused.** `PickerRowView` is a SwiftUI view, and
every member of one — including a static function that touches nothing — belongs to the main thread.
Without the annotation the test process *died*, xcodebuild restarted it eight times, and the summary
added totals across launches, so this suite's three passing tests were reported as failures.
`PickerScreenModelTests.aRowSpeaksItsSecondLine` already carried the annotation and the reason for it. I
did not read it first — which is the rule *read the plan before you write it*, applied to a test.

It is recorded because the failure mode is the dangerous one: a count over a run that never completed.

**`F13-M1` produced the run's most useful result, and it was not the one it was written for.** It was
expected to fail one test. It failed three, across two fixtures — and `aProjectColourReachesTheMirroredRow`,
whose projects all carry a colour, **passed throughout**. That is the row-versus-page distinction
demonstrated rather than argued: a suite made only of rows that happen to set the field would have
called a required `color` correct.

`F13-M8` is not in the plan's table and needed to be. The plan covers the hex values and the lint rule
but not the name lookup, and that seam is where a real response goes wrong: every multi-word colour
arrives as `unknown` while every single-word colour keeps working, so the feature half-fails on an
account and passes on a fixture.

---

### `F13-T3` — the swatch

A rounded 12pt square, filled with the project's tint and **unconditionally ringed in
`borderStrong`**, sitting between the plan toggle and the title. Three placements: the root project
list, a project row in search results, and `TaskPickerView`'s heading.

**The ring is not decoration and it removes an argument rather than adding one.** No contrast floor
can be guaranteed for any pairing involving a colour somebody chose in another app — Todoist's
charcoal on our dark page is genuinely invisible and cannot be fixed from here. `borderStrong` is
measured at 3.12:1 or better on every ground in both appearances, so the ring makes the swatch a
visible object whatever its fill, which is what WCAG 1.4.11 actually asks for. The alternative was a
per-tint contrast test that passes for eighteen colours and fails for two, forcing us either to alter
somebody's project colour or to ship a known-invisible mark.

**The view does not do the lookup.** `PickerScreenModel.Project.tint` is a `TodoistTint`, converted
once where the model is built, so no screen ever calls `TodoistTint(todoistName:)` itself — a screen
doing that would be a screen deciding what a piece of data looks like. `TaskPickerView` knows an id
and a name because that is what its route carries, so the lookup is `PickerScreenModel.tint(ofProject:)`,
a plain function read by a test with no screen behind it. Adding a third payload to the route would
have put presentation into navigation.

**`nil` and `.unknown` mean different things, deliberately.** A project whose colour Todoist never
sent is `.unknown` and draws Todoist's own default. An id the mirror does not hold — a project deleted
while its task list was open — is `nil` and draws nothing, because a swatch there would claim the
project still exists.

**The heading keeps `navigationTitle` as well as the principal toolbar item.** The modifier supplies
the screen's semantic title, which is what VoiceOver's screen announcement and a back button read;
dropping it to draw a swatch would have traded an accessibility fact for a decoration. This placement
is `T3`'s one taste call and the plan argued it both ways — the project is not in question on a screen
you are already inside — and it is one modifier to remove.

`PlanBuilderView` is at exactly the 400-line cap, which is why the tint arrives through the model
rather than through a new parameter, and why an explanatory comment there was cut in favour of the doc
comment on the property.

---

## The fence caught the new columns, which is what it is for

Adding `colorName` and `priority` turned the whole suite red at
`SessionPlanFenceTests.theLocalCopyHasNoInventedColumns`, which pins the exact column set of every
mirrored type. **That is the mechanical version of `F3-contract.md` §3.2** and the reason a new column
cannot be a small commit nobody notices. The test is restated with both names and the authority for
them, not relaxed.

---

## A defect found in the instrument, not in the feature

`F13-T5` step 1 needs a fourth claim on `scripts/check-todoist-facts.sh` (gate answer `D`). Writing it
turned up something worse than the thing it was for.

**`CLAIM 2`'s phase-2 verdict block had never compiled.** It used a backslash-escaped quote inside an
f-string expression, which no Python accepts at any version — PEP 701 permits quote *reuse* inside
f-strings, never a backslash escape. And the helper was
`json() { python3 -c "$1" 2>/dev/null; }`, so the `SyntaxError` went to a discarded stream: running
`--phase2` printed its heading and then nothing at all.

**An instrument that reports *no evidence* in exactly the way it reports *no finding* is the worst
failure mode there is.** This script exists to settle questions that cannot be settled by reading, and
`O12`'s tombstone claim — does incremental sync report a removed project as a tombstone, or by
silence? — could never have been settled by it.

Fixed three ways, because one would have been the same bet again:

1. The block is rewritten without backslash escapes, and so is the new `CLAIM 4` block.
2. `json()` no longer discards stderr.
3. **`scripts/check_embedded_python.py`** compiles every Python program embedded in every
   `scripts/*.sh` — eleven of them — and is in `make checks`, in CI, and proven by `F13-M10`, which
   puts the original defect back and watches the check go red.

---

## What the owner's run settled, and what it found

The owner ran `scripts/check-todoist-facts.sh` on 2026-09-24. **`CLAIM 4` — the one written for
`F13-T5` — failed on their machine**, and two of `O12`'s three long-open facts were settled on the way.

### `CLAIM 4` shipped broken, and the check I had just written could not see it

```
CLAIM 4 — are colour and priority on the responses, and which way does priority run?
sed: 1: "\$d
": unterminated regular expression
...
json.decoder.JSONDecodeError: Expecting value: line 1 column 1 (char 0)
```

Two of the seven `printf | sed '$d'` splits shipped as `sed '\$d'` — a stray backslash that survived
into the file. `sed` rejected it, the body came back **empty**, and Python was handed nothing.

**`scripts/check_embedded_python.py` reported OK throughout, because the embedded Python was never the
problem.** I had written that check an hour earlier, for this exact script, and it validated the
program and not the shell feeding it. That is `docs/conventions.md`'s *"Validate every artefact, not just the primary one"* one layer down: the thing a
human is told to run was still not run by anything.

Three fixes, because the typo alone would have been the same bet again:

1. **The split is one definition.** `body()` and `code()`, used at all seven sites, so there is one
   place to get wrong instead of seven. `factsScriptSplitsBodyFromStatus` drives the real functions out
   of the shipped file against a fixture and asserts both halves — including that the body is still
   parseable, because an empty string is a perfectly good string and a useless one.
2. **`CLAIM 4` is run, not merely compiled.** `scripts/tests/probe_facts_claim4.py` lifts the claim's
   own code out of the script, stubs `api_get` with a fixture, and runs it with no token.
   `factsClaim4ReportsFromAFixture` asserts the colour half, the priority half, and the absence of a
   traceback.
3. **`json()` no longer discards stderr** — which is the only reason the owner saw a traceback instead
   of silence.

`F13-M11` and `F13-M12` put the stray backslash back; both tests go red. **And the first version of
`F13-M11`'s test killed the runner instead of failing** — under `set -e` a failing command substitution
in an assignment aborts the shell. That is the third instance of that pattern in one day, after
`gen_or_fail` and `expect_validator_catches`.

### And the answer `F13-T4` was waiting for

```
GET /projects -> 7 project(s), 7 carrying a color key.  charcoal · grape · green · red
GET /tasks    -> 50 task(s), 50 carrying a priority key.
priority values seen: {1: 9, 2: 11, 3: 22, 4: 8}
priority=4  Select 1 to 3 MITs        priority=1  Clean the downstairs bathroom
priority=4  AM Check Inboxes          priority=1  fix printer
```

**Wire `4` is `P1`.** Read off what the tasks are, not off documentation: the owner's
most-important-task selection and inbox sweep are `4`; cleaning the bathroom and fixing the printer are
`1`. Those four rows, task ids and all, are the fixture `priorityIsReadInTodoistsDirection` asserts on,
which is what `F13-M3` inverts.

**Two expectations the account overturned, both of which would have shipped wrong.**

1. **`priority` is never absent.** All 50 tasks carried the key. The plan said *"nothing at all on a task
   with no priority — which is most tasks on most accounts"*; there is no such state on the wire. The
   unflagged value is `1`.
2. **The gate answer for `B` was wrong.** *Draw the top two* would have marked **30 rows in 50** — wire
   `3` alone is 22 tasks — against the plan's own rule that *"a picker where half the rows are amber has
   spent that budget on somebody else's data."* Only the top level is marked: 8 in 50.
   `onlyTheTopPriorityIsMarked` pins that count so widening it has to be deliberate.

Section 10 deferred `B` to a live account *because* it was a judgement about this account. That deferral
is the reason the wrong answer never reached the screen.

**All four colours on the account exist in `TodoistTint`** — charcoal, grape, green, red — so nothing
draws as `.unknown` today. And all 7 projects carry a colour, so the workspace shape `F13-M1` defends
against does not occur here. The tolerance is still right and still unexercised by live data; the
mutation is what holds it.

### Two of `O12`'s three facts are settled

- **An archived project DOES resolve by id.** 19 archived projects; `GET /projects/{id}` returned
  HTTP 200 with `is_archived=True`. **So the mirror must read both `/projects` and
  `/projects/archived` or it loses the name — and `/projects/archived` is not on the allowlist.**
  Adding it is a decision, not a tidy-up, and it is not `F13`'s.
- **No all-numeric task id is in active use.** 50 active tasks, 50 alphanumeric, 0 numeric. That does
  not prove old ids are dead, only that none is live, and the row says so.
- **Tombstones are still open.** Phase 1 took a token; phase 2 needs the create–token–delete sequence.
  And it could not have been settled before today regardless, because that verdict block had never
  compiled.

### The backfill diff caught me while I recorded it

Writing those results onto `O12` grew the row from 250 characters to 1100, and
`make check-open-backfill` reported it **GONE**. It was not — every original word was still there.

The first report was a true catch: my rewrite had dropped `O12`'s original sentence naming the three
facts, and I restored it. **The second was the check's own defect**: it paired rows by
`SequenceMatcher.ratio()`, which is a *similarity* and therefore falls when a row grows. Appending to a
row cannot mean the row is missing. It pairs on **coverage** now — what fraction of the old row's words
still align — which appending cannot lower and deleting does. All five triggers still fail, including a
new one: deleting the grown row outright.

---

## The adversarial review: four blocking findings, all correct

Every one was mine, and three were in the instruments rather than the feature.

**1. `check_embedded_python.py` had the failure mode it was built to stop.** It knew two ways a
program could be opened, so a heredoc and a double-quoted `python3 -c "` were not checked at all, and
an ending it could not parse made it `break` out of the scan **silently**. The reviewer put three
genuinely broken programs in one script and got `OK — 11 embedded programs in 12 script(s)`: a script
added, the count unmoved, nothing objected. It also had a count and no floor, in a project whose
harness contract requires one.

Now: heredocs and double-quoted blocks are read — a double-quoted `-c` is expanded the way bash
expands it before Python sees it — an unreadable block is a **failure** rather than a gap, and there
is a floor. **It reads 13 programs where it read 11: `check-watch-provisioning.sh`'s two were never
checked, and they discard stderr.** `F13-M16`, `F13-M17`.

**2. The stderr fix was held by nothing.** `probe_facts_claim4.py` defined its own
`json() { python3 -c "$1"; }` instead of lifting the shipped one — so it shadowed a third of the
`CLAIM 4` fix. The reviewer restored `2>/dev/null` in the real script and **all 46 script tests stayed
green**. The probe lifts the shipped definition now, and `factsScriptDoesNotSwallowErrors` drives it
with a deliberately broken program and asserts the `SyntaxError` is visible. `F13-M15`.

**3. The widened lint rule was defeated by a line break, and the comment claimed otherwise.**
`\b(Palette|TodoistPalette)\.` does not match the type name on one line and `.berryRed` on the next —
which is the form a line-length fix produces in a view. `TodoistPalette.swift`'s header said the guard
*"has never had a hole for it to fall through"*, and `.swiftlint.yml` already states why that is the
worse defect: *a comment claiming a guarantee nobody implemented is worse than claiming none.* The
regex takes `\s*` before the dot now, and the header records what the rule still cannot do — an alias
passes, because a text rule cannot resolve a name. `F13-M14`.

**4. The PR body described a different diff from the branch.** Title said `T1–T3` while `T4` was on it;
it told the owner about coloured squares and **not** about a red flag appearing on 8-in-50 task rows,
which is the most visible thing `T4` ships; and it asserted *653 tests pass* where a run says **657**
— the arithmetic was transcribed from a pre-`T4` state rather than read off a command. `CLAUDE.md` step
4 exists for exactly that gap, and the PR now carries the output instead of the claim.

### Two non-blocking findings taken as well

- **The swatch was written twice** — in `PickerRowView` and again in `TaskPickerView`'s heading — and
  the `borderStrong` ring is the whole accessibility mechanism. It is one `ProjectSwatch` view now, and
  it uses `@ScaledMetric` so it grows with the reader's text size instead of staying a 12pt dot beside
  a headline at AX5.
- **The captured fixture was decorative.** `let expected = row.wire == 4` re-derived the expectation
  from the input, so the ids and titles changed no outcome — the re-derived-expected-value failure
  `conventions.md` names. Each row now carries `isMostUrgent`, the reading a human took from the
  account, and the `== 50` assertion is gone because it was arithmetic over a literal three lines above.

### One finding recorded and not fixed

**Optionality buys tolerance for a missing key and a null, not for a wrong type**, and the doc comments
claimed the broader thing. `"color": 4` or `"priority": "high"` still fails the whole page.
`TodoistTaskDTO.Due` already has a hand-written lenient `init(from:)` for exactly this. Todoist v1 sends
both as the documented types today, so this is a latent risk rather than a live one — but the comments
overstate, and a lenient decoder for both fields is the honest fix. **Not taken here**: it is new
tolerance on a shipped decode path, which is a change the owner should see on its own rather than
folded into a review pass. Filed as an owner item.

**And one for the owner's judgement:** the owner's real account data is now in the tree — four live task
ids and titles in `TodoistPriorityTests.swift`, the priority tally and an archived project's name in the
register and in this log. Not a credential, and gitleaks is clean, but it is personal data in a
licensed repository and removing it is one edit.

---

## Still open

- **`F13-T5` is the owner's, and step 1 runs before `T4` can be finished.** It is the read-only fact
  check against the real account: that `color` is on `GET /projects` and `priority` on `GET /tasks`,
  both already allowlisted, and **which direction `priority` runs**. Until it is run, any mapping is a
  prediction and nothing in the tree contains one.
- **`E` in the gate table is the one answer worth a second look**, because it is the only one that
  changes the size of the feature rather than a line inside it.
