# F8 — Adversarial review log

> **Outstanding items from this review are tracked in [`docs/reviews/OPEN.md`](OPEN.md)**, with
> every other review's, so that an item left open here is visible from the next feature.

**Reviewed:** 2026-09-15 · **Feature:** F8 Fit a sprint to the time you have · **Branch:**
`F8/the-breakless-shape-overrun` (PR 42) · **Scope of the read:** commit `2fab614`, the uncommitted
working-tree change alongside it, and the state of the project's standing documents.

## Process

One adversarial reviewer ran at session start, per `CLAUDE.md` step 3 — *"Never resume blind"* — rather
than at a feature's end, because the session resumed onto a branch with uncommitted work on it. It read
the diff against `SPEC.md`, `zenpom-v1.5.md`, `definitions.md`, `CLAUDE.md` and `conventions.md`, and
ran `swiftlint --strict` itself rather than taking the PR's word for it.

## Verdict

| | |
|---|---|
| **Overall** | **DO NOT MERGE** — as the branch stood at review time |
| **Since** | Findings 1, 2, 4 and 5 are closed; 3 closed by PR 41 merging. `O42`, `O43`, `O44` remain open |
| Commit `2fab614` | **clean.** Real fix, real mutation evidence, plan written before code |
| The uncommitted change | **blocking.** Reverted 2026-09-15; see below |
| The standing documents | **blocking, and not this branch's fault** |

**2 blocking findings against this branch, 5 against project state, 7 non-blocking.**

## Blocking — the uncommitted change, and what closed it

1. **The `openShape` guard defended an unreachable harm and installed a reachable lockout.**
   `ZenTomato/Views/TimerView.swift:1036` gained `guard shapes.runningShape() == nil else { return }`,
   with a comment claiming it stopped a saved shape from rewriting a sprint in flight.

   **That harm has no trigger today.** `ShapeStore.start(_:)` has zero shipped callers —
   `ZenTomato/Views/ShapeSheetActions.swift:16` says so in its own words: *"It never calls
   `ShapeStore.start(_:)`. Nothing on this screen runs a sprint — `F8-T4` owns…"*. `F8-T4` is unbuilt,
   so `runningShape()` is always `nil` and the guard is dead code asserting a mechanism that cannot
   fire.

   **What it creates the moment `F8-T4` lands is worse than what it prevents.**
   `ZenTomato/Views/TimerView.swift:530` sets `controls = .start` whenever `!engine.isRunning`, and
   `ZenTomato/Views/TimerScreen.swift:563` renders `shapeControl` on `case .start`. So through every
   idle gap of a shaped sprint with auto-start off — most of its wall-clock life — the button is
   **visible, enabled and VoiceOver-labelled** and silently does nothing. That is the state
   `TimerScreen.swift:543-551` forbids in its own doctrine: *"ABSENT WHILE A BLOCK RUNS, RATHER THAN
   DISABLED… A greyed-out button is still in the accessibility tree."* The guard side was changed; the
   control side was not.

   **And there is no way out.** `TimerEngine.stop(reason:)` (`ZenTomato/Timer/TimerEngine.swift:349`)
   guards on `isRunning`, and `abandonShape()` is reachable only through `stop`. Reachable trigger:
   start a shaped sprint with auto-start off, finish block 1, kill the app, relaunch the next day. The
   shape is still in `UserDefaults`, `isRunning` is false, the button is inert, and the stale-shape
   discard that `docs/plans/F8.md:566-568` assigns to `F8-T4` does not exist yet.

   **Closed by reverting it.** The change belongs in `F8-T4`, alongside the stale-shape discard and an
   *absent* rather than inert control. The reverted diff is preserved as a patch for that task.

2. **The fence assertion was a source-text grep that would pass on a broken implementation.**
   `ZenTomatoTests/ShapeScreenFenceTests.swift:152` asserted
   `view.contains("guard shapes.runningShape() == nil else { return }")`. It stays green if the line is
   moved *below* `showingShape = true`, pasted into any other function in the file, or placed in a
   branch that never runs — *"a fixture that satisfies both the right and the wrong implementation
   distinguishes nothing"* (`conventions.md`). The suite's own preamble at `ShapeScreenFenceTests.swift:10-14`
   says *"The source checks underneath are the second line, not the first"*; here there was no first
   line. `ShapeSheetActions` exists precisely as the testable non-drawing seam and was not used.

   **Closed by reverting it.** When the predicate returns in `F8-T4` it needs behavioural coverage
   through `ShapeSheetActions`, not a second grep.

## Blocking — project state, found by this review

3. **`CLAUDE.md` on `main` stated a scope fence and a hard stop that two ratified documents replaced** — **CLOSED 2026-09-15 by PR 41 merging.**
   `CLAUDE.md:10` reads *"Scope is `SPEC.md` F1–F6 and nothing else"*; `CLAUDE.md:15` and
   `docs/specs/SPEC.md:5` read *"Hard stop September 13, 2026"* — a date two days past at review time.
   `zenpom-v1.5.md` was ratified 2026-09-09 with eleven units including `F8`. The corrections sit
   unmerged in **PR 41** (`C27/standing-docs-after-the-rescope`), open since 2026-09-11.

   **This is not cosmetic.** `CLAUDE.md`'s own tiebreaker is *v0.1 wins until a `D<n>` says otherwise*,
   and until `D35` no delta had struck `SPEC.md:5` — so **the dead date was authoritative by
   construction**, and every agent reading the repo was correctly concluding that `F8` was out of
   scope. The session that produced this review did exactly that before finding `zenpom-v1.5.md`.

4. **PR 41 was red on one test — CLOSED 2026-09-15, merged as `65240a7`.**
   `DeltaIntegrityTests.everyRatifiedSpecAmendmentIsApplied()` fails because `D35`'s replacement text
   has never reached `SPEC.md:5`, while `docs/specs/AMENDMENT-BASELINE.txt` pins the outstanding count
   at `0`. Local run on `C27/standing-docs-after-the-rescope`, 2026-09-15: **627 tests in 93 suites, one
   failure, `make` exit 65** — that test and nothing else. `AMENDMENTS-TO-APPLY.md` and the test's own
   doc comment both say the agent may not apply it: *"THE AGENT MAY NOT FIX THIS, AND THAT IS WHY THE
   TEST EXISTS… It can only refuse to let the gap stay invisible."*

   **How it closed, recorded because the manner matters.** The owner's first attempt landed in
   `CLAUDE.md` and `00-deltas.md`, which that test does not read, so it stayed red — and the malformed
   `00-deltas.md` index row turned the *F8* branch red too, on `everyCitedDeltaIsDefined()`, because
   `D35`'s heading was on another branch. The owner then instructed the agent to apply it directly. It
   was applied, and the waiver is recorded in `65240a7`'s message rather than done quietly: **the
   prohibition stands**, and what was waived was transcription of an already-ratified decision whose
   replacement text was pre-written verbatim. Evidence: CI run `34992863325`, every step green,
   including the release-configuration build that had been skipped since 2026-09-11. Tracked and closed
   as `O41`.

5. **`docs/plans/F8.md:3` asserted that no F8 code existed, six days after the first commit landed.**
   The header read *"plan written, **awaiting the gate.** No code has been written"* with six F8
   commits on `main` (`a0dfd09` … `fe2c243`). Line 4 called `zenpom-v1.5.md` a DRAFT; it was ratified
   2026-09-09 (`e58f971`). **Corrected 2026-09-15** in this branch.

6. **`docs/plans/00-status.md` claims to be generated by a script that has never existed.** Its header
   reads `<!-- GENERATED by scripts/gen_status.py — do not edit by hand. -->` and names a CI check
   `python3 scripts/gen_status.py --check`. **`scripts/gen_status.py` is absent from the working tree
   and from all 350 commits of history in every branch** (`git log --all --diff-filter=A -- '*gen_status*'`
   returns nothing), and no file in `.github/`, `scripts/` or `.githooks/` references it or
   `00-status.md`. So the project's single status artifact is hand-written prose wearing a generated
   file's header, and the misreport in finding 5 was never going to be corrected by anything. **Not
   fixed here** — writing the generator, or stripping the false header, is a chore-sized decision for
   the owner. This is the `conventions.md` pattern exactly: *"a hook that has never failed is a hook
   nobody knows works."*

7. **`docs/chores/C10.md:5` presents a superseded licence ruling in the present tense with no banner.**
   *"**RULED 2026-08-27 by the owner: option 3, dual licence.**"*, repeated as live at
   `docs/plans/parked.md:55`. `C18.md:5` carries a superseded banner; `C10.md` does not, and it is not
   on `scripts/check-licence-wording.sh:63`'s allowlist — it passes the regex rather than being
   exempted, so the committed guard cannot see it. Pre-existing; not caused by this branch. The
   structural fix is the split `O25` already prescribes for `C18`.

## Non-blocking

- **`F8-M7` and `F8-M8` each name two different mutations.** Commit `2fab614` defines `F8-M7` as
  *auto-start ON, `engine.isRunning` → true* and `F8-M8` as *auto-start OFF, `state.kind` → `.longBreak`*.
  But `ZenTomatoTests/ShapeScreenTests.swift:76` already uses `F8-M7` for *rendering `nil` as an empty
  shape*, and `:111` uses `F8-M8` for the two budgets from `T1`'s table. `docs/plans/F8.md:626-630`'s
  table stops at `F8-M5`. The mutations were genuinely run — the commit records observed values, not
  arguments — but two IDs now resolve to two things each, so neither is citable. Same defect class as
  the `O23`/`O24`/`O25` collisions `00-register.md:84-86` already flags.
- **`F8.md:620` declines to open an `M` register**, deliberately and with its reasoning stated, which
  is why the IDs live in test comments. That choice is defensible; the collision above is what it costs.
- **`ZenTomato/Timer/TimerEngine.swift:833-837`** derives `shapeJustSpent` from two separate
  `UserDefaults` reads either side of `advanceShape()`. A failed decode on the second makes it
  spuriously true and ends the sprint a block early. Separately the shape cursor (`UserDefaults`) and
  `TimerState` (SwiftData) advance non-atomically, so a process kill between `advanceShape()` and
  `persist()` loses a block. Pre-existing — but `2fab614` now derives control flow from it.
- **Four of F8's five gate rulings never became a `D<n>`**, against `F8.md:30`'s own instruction that
  *"each is recorded as a `D<n>`"*. Only Ruling E did, as `D32`. A–D exist as `RULED 2026-09-09` prose
  inside the plan the agent wrote.
- **`D33` (ratified 2026-09-09) adds `F18`/`F19`** — *"the milestone becomes thirteen units"* — and the
  amendment was never applied; `zenpom-v1.5.md`'s table still shows eleven.
- **The v0.2 respec gate was never run under that name.** The string `v0.2` appears nowhere in the
  repository outside the untracked handoff. The v1.5 gate of 2026-09-09 produced three of the handoff's
  four required outputs and **explicitly refused the fourth** — `zenpom-v1.5.md:28`: *"Why a line and
  not a cap… A number replacing it would have been arbitrary"* — recording its own caveat that *"the
  line admitted almost everything."* The list has since grown 11 → 13 via `D33`. Recorded honestly
  rather than hidden, and still unreplaced.
- **`F8-T6`'s device check is unrun**, and is owner-owned.

## Clean

No Todoist call of any kind in either diff. No credential, token or client secret — `gitleaks` scanned
350 commits, no leaks. `swiftlint --strict` clean on every modified file. Plan written 2026-09-09 11:24,
first code 2026-09-09 14:11, so **the plan gate held for F8** — the failure `O25` was raised for did not
recur here. PR 42's description is genuinely readable by a reviewer who does not write Swift, and its
evidence block is specific about what was run.

## Evidence

```
make test  ·  C27/standing-docs-after-the-rescope  ·  2026-09-15
627 tests in 93 suites, 1 failure, make exit 65
Failing: DeltaIntegrityTests.everyRatifiedSpecAmendmentIsApplied()

scripts/check-wholesale-rewrite.sh   OK
scripts/check-open-register.sh       OK
scripts/check-licence-wording.sh     OK
scripts/check-secrets.sh             OK — 350 commits scanned, no leaks
scripts/check-todoist-writes.sh      OK — no endpoint outside the allowlist
swiftlint --strict                   clean on the modified files
```

**Not evidence, and named as such:** `F8-T6`'s device check has not been run, and no test in this
repository can run it.
