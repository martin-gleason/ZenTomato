# Open items — every review, one table

**Why this file exists.** Every review log from F1 onward carries a *Still open* section, three carry
*Still not verified*, and F4 carries a *Watch list*. Nothing aggregated them, so an item left open in
F2 was invisible by the time F6 was being reviewed. Six logs, six places to look, and no way to answer
"what is outstanding" without reading all of them.

**Generated from `docs/plans/00-register.md` between the marker pairs below** (`D42`, `C37`); the prose outside them, including this paragraph and the `## Closed` table, is hand-maintained. An item leaves this table only when it is done or explicitly abandoned —
and abandoning is a decision that gets written down, not a thing that happens by the item scrolling
out of view.

**Owner** is who can actually close it. Most of the device checks cannot be closed by the agent under
any circumstances: no test in this repository can hear a sound come out of a phone or answer a
permission prompt.

---

## Needs the owner

<!-- BEGIN GENERATED: owner — scripts/gen_status.py writes every row between these markers from docs/plans/00-register.md (C37). Do not edit by hand: `make check-status` fails, and the next run reverts it. Prose outside the markers is hand-maintained and is never touched. -->
| # | Item | From | Why it is still open |
|---|---|---|---|
| O1 | **One real day's export, read beside the Rhodia** | F6 | `SPEC.md`'s *Done when* for F6, and the only judgement of "readable without translation". The golden file is a format test, not the criterion. |
| ~~O2~~ | ~~**Migration over an existing install**~~ **CLOSED 2026-08-28** | F6, F2c | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F6, F2c. The text below is OPEN.md's own, not a rewrite. Build `202608281026` was installed over the running app, not a deleted one, and it carried F2c's seventh `AppSettings` column. The sprint ran to completion with settings and history intact, so lightweight migration works over a real store. This was the load-bearing one: a unit test builds a pre-F2c row, it cannot find one on a phone. |
| ~~O3~~ | ~~**A full sprint with the screen locked, uninterrupted**~~ | F4 | CLOSED 2026-09-09 on 0.9.0 (202608281553). Run at last as the single uninterrupted sprint the spec describes, with O4 and O5 in the same run. The owner: "O3, O4, and O5 were one and succeeded. each passed the test." This closes SPEC.md's Done when for F4 — the standing objection was that the pieces had been seen individually and never as one run. |
| ~~O4~~ | ~~**Headphones, CarPlay, and an incoming phone call**~~ | F4 | CLOSED 2026-09-09 on 0.9.0 (202608281553), in the same run as O3 and O5. The row was already marked closed with no evidence recorded; this fills it. NOT ITEMISED: the owner reported the run passing as a whole and the three interruption paths were not confirmed individually, so this records one run rather than three per-path results. |
| ~~O5~~ | ~~**A playlist short enough to reach its end, to prove looping**~~ | F4 | CLOSED 2026-09-09 on 0.9.0 (202608281553), in the same run as O3 and O4. A real playlist reached its end, which is the half no test could reach — the loop had only ever been asserted against a stand-in. |
| ~~O6~~ | ~~**The alarm firing through an active Focus, and through silent mode**~~ **CLOSED 2026-08-28** | F2 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F2. The text below is OPEN.md's own, not a rewrite. **Both halves now observed.** Through a Focus: confirmed on three builds. Through silent mode: the owner ran the `C20` check on build `202608281026` — *"in silent and DND, the alarm consistently fired as did the sheet."* That is the half `SPEC.md` F2's whole design rests on: AlarmKit was chosen over a notification precisely so an alert would survive a Focus and the ringer switch, and it does. |
| ~~O7~~ | ~~**Sound off**~~ **CLOSED 2026-08-28 — on the phone** | F2, C20 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F2, C20. The text below is OPEN.md's own, not a rewrite. AlarmKit has no silent case, so this setting rested entirely on a bundled silent file, and **nothing had ever observed a phone staying quiet**. It has now: *"bell sound OFF: no sound from phone."* `Silence.caf` does what the setting rests on. The watch is a separate question — `O33`. |
| O8 | **VoiceOver on hardware** | F2, F5 | The countdown was coarsened to whole minutes to cut announcement spam; whether iOS re-announces at all is runtime behaviour nobody has listened to. F5 adds the capture buttons' labels, values and announcements. |
| O9 | **The merged stop sheet with taps in it, at AX5** | F5 | The single surface D14 was written for. Three chained sheets on one view, and the switched-off confirm button, unexercised at the largest text size. |
| ~~O10~~ | ~~**Apply the nine outstanding spec amendments**~~ **CLOSED 2026-08-27 — the count is zero** | C6 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by C6. The text below is OPEN.md's own, not a rewrite. `A1`–`A7` were applied 2026-08-24 and `A8`–`A9` on 2026-08-27; the row's "nine outstanding" had been stale since August. `AMENDMENT-BASELINE.txt` holds the authoritative count and `DeltaIntegrityTests` fails if it grows, so this is enforced rather than tracked. `docs/specs/AMENDMENTS-TO-APPLY.md` is now a record of what each amendment changed, not a to-do list. |
| ~~O11~~ | ~~**F7's gate: D2 into `SPEC.md`**~~ **CLOSED — D2 was applied 2026-08-24** | F7 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F7. The text below is OPEN.md's own, not a rewrite. `docs/plans/F7.md` said *"Until D2 is merged into `SPEC.md`, this feature does not exist and no line of it may be written."* It was merged as part of `A2` on 2026-08-24, which is what unblocked F7 — the row simply outlived the fact, alongside `O10`. |
| O12 | **Three Todoist API facts, against a live token** | F3b | `scripts/check-todoist-facts.sh`. Whether an archived project resolves by id, whether sync reports deletions as tombstones or by silence, and whether old numeric task ids still resolve. TWO OF THE THREE ARE NOW SETTLED — the owner ran it 2026-09-24 against their real account and the output is in `docs/reviews/F13.md`. (1) AN ARCHIVED PROJECT DOES RESOLVE BY ID: 19 archived projects, `GET /projects/{id}` on \"FECP and SDOP\" returned HTTP 200 with `is_archived=True`. So the mirror must read BOTH `/projects` and `/projects/archived` or it loses the name — `/projects/archived` is NOT on the allowlist and reading it is a decision, not a tidy-up. (3) NO ALL-NUMERIC TASK ID IS IN ACTIVE USE: 50 active tasks, 50 alphanumeric, 0 numeric. That does not prove old ids are dead, only that none is live; `--task-id` settles it if a recorded old id turns up. (2) TOMBSTONES ARE STILL OPEN: phase 1 took a token (7 projects, 0 already flagged deleted) and phase 2 needs the create-token-delete sequence the script prints. **AND THE PHASE-2 VERDICT BLOCK HAD NEVER COMPILED** until 2026-09-24, so this fact could not have been settled by running it — see `F13-M10`. |
| ~~O13~~ | ~~**Developer Mode on the watch, then provision it**~~ **CLOSED 2026-08-24** | C8 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by C8. The text below is OPEN.md's own, not a rewrite. Root cause: the new Watch Ultra 3 has never had Developer Mode turned on, so Xcode cannot register it, so no watchOS profile can include it — and the watch app is signed with an iOS profile instead. iOS installs the phone app and silently skips the watch app. Blocks F7's device check. Check with `scripts/check-watch-provisioning.sh` |
| ~~O14~~ | ~~**Does the music row give a real answer now?**~~ **CLOSED 2026-08-27 — yes.** Settings → Music reads **Connected** on the device, in Todoist's vocabulary. Open since the MusicKit-entitlement confusion; the App ID work (`C11`) and `F4c`/`F4d` between them answered it. — *now also readable in Settings → Music, which is the faster check* — *the App ID half is done; see `docs/chores/C11.md`* | C8, C11 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by C8, C11. The text below is OPEN.md's own, not a rewrite. The App ID is explicit and MusicKit is enabled on it under App Services. The premise that a missing **entitlement** caused `ICError -7013` was wrong — there is no MusicKit entitlement for an iOS app. So whether those errors persist, and whether the subscription check now succeeds, is a device question rather than a settled one. |
| O15 | **F7's device check — three wrist taps, phone in another room** | F7 | D2's *Done when*, and the only thing that can close F7. Then the harder half: taps made genuinely out of range, arriving later with their **original** timestamps. |
| ~~O16~~ | ~~**Branch protection on `main` — it has never existed**~~ **CLOSED 2026-08-24** | C1 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by C1. The text below is OPEN.md's own, not a rewrite. PR required, CI required, linear history, rebase-only, no force pushes. |
| ~~O17~~ | ~~**Search in the music picker**~~ **CLOSED 2026-08-27 — built and verified on the owner's own library**, which is the only fixture that counted. `D23` ratified, `F4e` shipped. | F4c | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F4c. The text below is OPEN.md's own, not a rewrite. The other half of the note that produced F4c. A library of a few hundred playlists is unusable without one, but a search field is new functionality rather than a rearrangement, so it wants a delta argued rather than assumed. |
| ~~O18~~ | ~~**No `PrivacyInfo.xcprivacy`**~~ **CLOSED 2026-08-27 — added, and fenced.** Declares no tracking, nothing collected, no required-reason API, and `PrivacyManifestTests` holds it to that: the three arrays must stay empty, and the shipped Swift is searched for the five required-reason API families with comments stripped. Verified present in the built bundle rather than in `project.yml`. Formerly: | C12 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by C12. The text below is OPEN.md's own, not a rewrite. Verified rather than assumed: no required-reason API is used (the only `UserDefaults` in the tree is a word in a comment) and nothing is collected or sent, so Apple does not require a manifest. Worth adding anyway so "collects nothing" is machine-readable rather than a claim in a README. |
| ~~O19~~ | ~~**The App Store name — "ZenTomato" is taken**~~ **CLOSED 2026-08-25 — the name is `ZenPom`** | C9, C10 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by C9, C10. The text below is OPEN.md's own, not a rewrite. App Store Connect returned `409 DUPLICATE.DIFFERENT_ACCOUNT` on the first upload attempt: the name is in use by another account. Costs nothing in code — bundle ID and home-screen name are unaffected — but no build can be uploaded until an app record exists, and no record can be created without a name. Effectively permanent once builds ship under it, and it is the name the public GPL repo carries, so it is `C10`'s decision too. |
| O20 | **No choice of alarm sound (needs a delta — D24 proposed)** | F2 | The owner: *"the default alarm sucks, and will be quite jarring to folks using an app with Zen in the name."* A sound picker is a **seventh** setting, and `PolishFenceTests` pins `AppSettings` at six — a bound that was mutation-tested against an alarm-sound picker specifically. So this cannot be built without ratifying the delta first. |
| ~~O21~~ | ~~**Music cannot be restarted during a break**~~ **CLOSED 2026-08-28 — built as `F4f`, verified on the device** | F4 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F4. The text below is OPEN.md's own, not a rewrite. `D25` ratified and applied to `SPEC.md` line 27. All three of that line's obligations confirmed on hardware: a break starts silent even with music on, the switch turns it on for that break, and it stops at the boundary without being asked. The switch is scoped to the break — during one it shows and sets that block, because the standing intention already read "on" while nothing played, leaving nothing to switch. |
| ~~O22~~ | ~~**A CLA or copyright assignment, before the first outside pull request**~~ **CLOSED 2026-08-27 — `CONTRIBUTING.md` ratified by the owner** | C10, C18 | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by C10, C18. The text below is OPEN.md's own, not a rewrite. Ratified in its pledge-joining form: a contributor licenses their work GPL-3.0-or-later and joins the App Store non-enforcement pledge in `LICENSE-EXCEPTION.md`, keeping their copyright and every other GPL right. In force for every pull request opened after 2026-08-27; the instrument records its own effective date. What remains is enforcement at review, which is in the adversarial reviewer's brief. |
| O23 | **Progress bars appear full rather than filling** | F5 | Owner's note during sprint 2: *"progress bars should fill up, not just appear full."* Cosmetic, and not looked at yet. |
| ~~O24~~ | ~~**the id names two different closed rows: a block heard end-to-end on a chosen bell, and the dual-licensing report fetchable by SHA**~~ | F2c, C13 | Both closed before this row existed — `F2c` on 2026-08-28, `C13` on 2026-08-27 — and one register row cannot hold two items. `C37` recovered both verbatim as `C-8` and `C-9` in `docs/reviews/OPEN.md`'s `## Closed` table; this row is the pointer, not the record. The collision itself is still open in `## Defect — reused IDs`. |
| O25 | **docs/chores/C18.md needs splitting so it can leave the licence allowlist** | C18 | C18 holds both the superseded MIT design and the arrangement that shipped, so it must quote a phrase the licence check forbids, so it is exempt from that check — and the stale phrase then sat in its own title for a day. A banner and corrected task rows fix the misreading today. The structural fix is to move the MIT reasoning to `docs/archive/` and take C18 off the allowlist, which is doc surgery the owner should approve rather than the agent do inside a feature branch. |
| ~~O26~~ | ~~**The alarm cannot be silenced from inside the app**~~ **BUILT 2026-08-28, unseen on a phone** | F2, F2c | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F2, F2c. The text below is OPEN.md's own, not a rewrite. `D26`, built as `F2d`. `AlarmScheduling` gains `alertingAlarmID`, `alertingUpdates()` and `stopAlerting(id:)`; the engine gains `silenceAlarm()`, which stops the noise and then runs the same `handleDismiss()` path `DismissBlockIntent` runs. Twenty-two tests across three suites, including a drift test that the button and the system alert produce the same recorded outcome. **Device check is `O29`.** |
| ~~O27~~ | ~~**Every setting is editable while a block runs**~~ **BUILT 2026-08-28** | F2c, F2e | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F2c, F2e. The text below is OPEN.md's own, not a rewrite. `D27`, built as `F2e`. One `.disabled(isBlockRunning)` on a `Group` holding all three customization sections, with a fence that was shown to fail when a section is moved out of it. Device check is `O30`. |
| ~~O28~~ | ~~**The alert sounds need a preview**~~ **BUILT 2026-08-28** | F2c, F2e | TRANSCRIBED FROM docs/reviews/OPEN.md 2026-09-24 by C37, which could not generate that file while sixteen closed rows lived only in it — O44 measured the loss and this is the half of it the register could absorb. Raised by F2c, F2e. The text below is OPEN.md's own, not a rewrite. `D28`, built as `F2e`. Selecting a sound plays it, which is iOS's own Settings › Sounds idiom rather than a new control. `Default` cannot be played — it is iOS's sound, not a file ZenPom holds — and the footer says so. Device check is `O30`, and the audio session is the half a simulator cannot answer. |
| O29 | **The Silence button, on a phone — HALF ANSWERED 2026-08-28** | F2d | **App in the foreground: passes.** Build `202608281343`, the owner's words: *"with app open, silence button was amazing. all tests fired."* The screen-locked reflection gap found in that run is `D29`, ratified and built. **What remains is the second half**: a block ending with the app closed, dismissed from the system alert, confirming no double advance against the in-app button. The owner has it: *"the back half was tested in a previous build, I'll retest when this version lands."* |
| O30 | **The locked settings screen and the sound preview, on a phone** | F2e | `F2e`'s *Done when*, unrun. Three checks: the customization rows locked during a block with music and Todoist still live; **each bell previewed with the ringer switch off** — `.playback` is what should make that audible — with the app's own music expected to keep playing, and audio from *another* app allowed to be interrupted, which is the stated trade-off of the single non-mixable session policy this app has always had; and **music stopped and started again after a preview**, which is the one that matters, since the reverted `.mixWithOthers` version would have left the session mixable for the life of the process and broken `F4`'s interruption handling invisibly. Plus a preview left mid-play by leaving the screen and by backgrounding, both of which must go quiet. |
| O31 | **The watch alarm's button says Stop, and ours says Done — PARKED 2026-08-28** | F2d, F7 | This app sets that label to `"Done"`; the watch app's own source has no Stop button at all. So watchOS is drawing its own alert rather than honouring `AlarmButton`, and the word is not ours to change. The owner's ruling: *"if its a watchos default, keep it. I'll upload a screen shot next time to ris it."* Reopen if the screenshot shows otherwise. |
| O32 | **A watch-face complication does not exist** | F7 | Requested 2026-08-28: *"make sure zenpom can be installed as a complication on a ultra watch face."* **`SPEC.md` line 58 excludes it by name** — *"widgets beyond the Lock Screen Live Activity"* — and `F7.md` declined it citing that line. The only `WidgetKit` code in the tree is `ZenTomatoActivity`, an `ActivityConfiguration` embedded in the *iOS* app; a complication is a separate watchOS widget extension that does not exist. So this is a build, not a check: `D30`, proposed and not ratified, with the recommendation **not** to take it before the September 13 stop while `D29` — a lost reflection prompt in the ordinary locked-phone case — is outstanding. |
| O33 | **The watch makes a noise when the phone is told not to** | F2, F7 | Reported 2026-08-28 on build `202608281553`: *"focus screen on, bell sound OFF: no sound from phone, but the watch makes a buzz and a slight ringing."* **The phone half closes `O7` positively** — sound off really is silent, which nothing had ever observed — and it retro-explains `C20`: the alarm heard with sound off in an earlier sprint was almost certainly the watch. **Hypothesis with evidence:** the watch app ships no `.caf` at all. `project.yml` gives it `Assets.xcassets` specifically and says why — *"that also holds Silence.caf … which is F2's and has no business on a wrist"* — and `find` over the built `ZenTomatoWatch.app` returns none. If AlarmKit mirrors the alarm and resolves the named sound against the *watch's* bundle, it finds nothing, and a missing named sound is not silence there either. **If so the chosen bell is not honoured on the watch either.** One check, a build not a sprint: add `ZenTomato/Resources` to the watch target, install, run one block with sound off. Silence on both means one line fixes it; a buzzing watch anyway means `sound on/off` cannot be honoured on the wrist from the app bundle, which is spec-level and owes a delta. **Not to be guessed at** — three framework assumptions about this alarm have already been wrong. |
| O34 | **From the lock screen, slide-to-stop worked** | F2d | Same run. The accompanying note — *"but the bell sound was turned on"* — is ambiguous between *the sound setting was on, so it rang* (expected) and *something changed the setting*. Recorded rather than resolved; one word next run says which. |
| O35 | **Documentation accuracy left over from F2d/F2e's eight review passes** | F2d, F2e | **Filed rather than fixed, at the owner's explicit direction:** the merge bar for that branch was *no code-behaviour defects*, with accuracy findings recorded and fixed after. The list, from pass eight: `SilenceAlarmTests.theFlagClearsWhenWatchingEnds`'s doc still describes controls being disabled, two design revisions stale; the `Alarm ringing, largest text` preview omits `.preferredColorScheme(.light)` unlike every other AX5 preview in the file; and `docs/plans/00-deltas.md` says *"24 deltas"* above a table of 32 rows — pre-existing on `main`, and `DeltaIntegrityTests` asserts presence rather than the count, so nothing catches it. |
| O36 | **The zenpom Focus runbook, followed once on the phone** | C21 | C21 shipped written but NOT TRIED, and its own header says so. Step 6 is the verification: run one sprint with the Focus on and compare that sprint's external-distraction count against a typical one. The log already counts internal and external separately, so the only real measure of whether the chore was worth writing is free. Menu labels in it are unconfirmed against iOS 26 and corrections go back into the file. |
| O37 | **The shape screen, on a phone — F8-T3's device check** | D39 | HELD 2026-09-22 by D39, which halted F8 at T3 and re-gated it: this check asks the owner to confirm figures no document justifies. The owner's own worked example — 3 sprints over 120 minutes with a minimum 5 minute break and a 10 minute long break — cannot be produced by what was built, which yields one sprint of four poms with a 17-minute long break. It is released when F8's five BLOCKING notes are answered at its fresh gate. ORIGINAL CHECK, unchanged: Enter 120 and read back four poms of 22, three short breaks of 5 and a 17-minute long break finishing exactly two hours out. Then 45 (says it is under the minimum), then 10 (says no shape is possible rather than drawing an empty one), then change the preset at 180 where the presets actually differ. Close by confirming AppSettings is untouched. |
| O38 | **The two sentences on the shape screen are the agent's words, not the owner's** | F8 | "No shape this short / The shortest shape these controls can make is 15 minutes" and "45 minutes fits 3 pomodoros. A full sprint needs 60 minutes." Drafted by the agent and constants in ShapeScreenCopy.swift, so changing them costs nothing. This is the app's voice at the one moment it tells somebody something mildly disappointing, which is where tone matters most. |
| O39 | **Does the absorption preset control earn its place?** | F8 | Ratified 2026-09-09 that the three presets ship in order to be TESTED. Measured: at 60 minutes all three return an identical shape, at 120 the spread is two focus minutes, at 180 it is 120 against 150 against 102. Arithmetic cannot say whether three hours feels better at 150 focus minutes or 102 — only use can. Without this row the control survives by default rather than by evidence, which is how surface accumulates. |
| O40 | **The App Group capability, in the developer portal** | F17 | F17's complication needs one and F18 refuses one, so it is F17-only and F17 is last. The account and the app's current capabilities are provisioned — TestFlight proves that — but there are NO entitlements files in the tree at all and an App Group has never been added to the App ID or pulled into the profile. C2's territory and the owner's; no task or chore row covered it before this one. |
| ~~O41~~ | ~~**D35's one-line amendment into SPEC.md**~~ | F8 | CLOSED 2026-09-15. SPEC.md line 5 now reads "Ends when: v1.5 is released, approved, and sent via TestFlight (D35)" and D35 is in the Amendments applied list, so AMENDMENT-BASELINE.txt's ratchet is back at zero and everyRatifiedSpecAmendmentIsApplied is green. Merged as 65240a7 in PR 41. APPLIED BY THE AGENT AT THE OWNER'S EXPLICIT INSTRUCTION, which is a waiver of the standing never-edit-SPEC.md rule and is recorded in that commit rather than done quietly; the rule stands. Until this landed, CLAUDE.md's v0.1-wins tiebreaker made the dead September 13 date authoritative by construction, six days after v1.5 replaced it. Found by the F8 review. |
| ~~O42~~ | ~~**00-status.md claims a generator that has never existed**~~ | C28 | The file's header says "GENERATED by scripts/gen_status.py — do not edit by hand" and names a CI check. scripts/gen_status.py is absent from the working tree and from all 350 commits in every branch, and nothing in .github/, scripts/ or .githooks/ references it or 00-status.md. So the project's single status artifact is hand-written prose wearing a generated header — which is why it reported F8 as "no code written" for six days and nothing corrected it. Two honest fixes: write the generator, or strip the false header. The owner picks; the agent should not choose to keep a claim that is not true. CLOSED 2026-09-15 by C28: the generator now exists, and is enforced by make check-status, the pre-commit hook and CI, each proven by breaking it. Found by the F8 review. |
| O43 | **F8-M7 and F8-M8 each name two different mutations** | F8 | Commit 2fab614 defines F8-M7 as auto-start ON / engine.isRunning -> true and F8-M8 as auto-start OFF / state.kind -> .longBreak. ShapeScreenTests.swift:76 already uses F8-M7 for rendering nil as an empty shape and :111 uses F8-M8 for T1's two budgets. F8.md's table stops at F8-M5. The mutations were genuinely run — the commit records observed values — but neither ID is citable. F8.md:620 declines to open an M register deliberately and with its reasoning stated; the collision is what that choice costs. Same class as the O23/O24/O25 collisions this register already flags. |
| ~~O44~~ | ~~**OPEN.md and the register are lossy in OPPOSITE directions, and neither is a superset**~~ | F8 | CLOSED 2026-09-24 by `D42`, which is the ruling this row asked for and did not have. NOT OVERRULED — RIGHT ON BOTH COUNTS: the prerequisite it named, that the register be complete first, was met by `C33` and `C34`, and the decision was the owner's and has now been made and numbered. Its measurement is what made the migration safe: 16 of OPEN.md's 35 O ids and 83 lines of prose existed nowhere else, so `C37` transcribed the 16 and left the prose outside the generated markers. ITS PROHIBITION WAS LIVE WHILE C37 WAS BUILT, and the artefact showed it — OPEN.md printed "SO: DO NOT GENERATE OPEN.md FROM THE REGISTER" inside a region generated from the register, which is how `C37`'s adversarial review found that no delta existed. FILED SAYING "OPEN.md is five items behind the register". THAT PREMISE WAS WRONG and the fix it implied would have destroyed data. Measured 2026-09-15: 16 of OPEN.md's 35 O-ids are absent from the register entirely (O2, O6, O7, O10, O11, O13, O14, O16, O17, O18, O19, O21, O22, O26, O27, O28 - all closed, all carrying the reasoning for how they closed), plus 83 lines of prose the register has no column for. One of those paragraphs records that a laggy button inflated the distraction tally, that the affected rows are still in the database, and that no capture surface exists to remove them - so any export covering that block over-reports. That is a caveat on O1 and it exists nowhere else. In the other direction the register had 15 rows truncated; C29 repaired them. SO: DO NOT GENERATE OPEN.md FROM THE REGISTER. The decision is the owner's: carry the 16 closed rows and the prose into the register and then generate, or keep OPEN.md as the narrative record and stop calling the register canonical for O rows. |
| O45 | **Whether v1.5's amendment ledger belongs inside the baseline** | C32 | OPENED 2026-09-23 by C32. docs/specs/zenpom-v1.5.md is a ratified baseline and the agent may never edit it, so C32 put its amendment ledger and baseline count in two files BESIDE it — docs/specs/V15-AMENDMENTS-APPLIED.md and docs/specs/V15-AMENDMENT-BASELINE.txt — in the same relationship AMENDMENT-BASELINE.txt already has to SPEC.md. SPEC.md can carry its own '## Amendments applied' list inline because the OWNER edits SPEC.md. The owner may prefer the same shape for v1.5; that is an edit to a ratified baseline and needs a D<n> of its own, exactly as D40 was needed for D33. Only the owner can decide it, which is why it is here and not a proposal in the chore. |
| O46 | **The v1.5 ratchet still cannot see D33, the delta it was built for** | C32 | OPENED 2026-09-23 by C32's review. D41's second hole says nothing would ever have gone red for D33; after C32 there is a ledger, a baseline file and a ratchet and D33 STILL does not go red. Two causes. FIXED: D33 declares no document inside its Currently block, so it was judged against SPEC.md - the wrong baseline - until the declaration rule learned to read a delta's preamble, where D33 writes 'Owed because `docs/specs/zenpom-v1.5.md` is a ratified baseline'. NOT FIXABLE BY VERBATIM MATCHING: D33 amends an order TABLE and quotes it as a comma list of ids, every one under the twelve-character fragment floor, so there is no verbatim overlap to find; lowering the floor makes F8 match everywhere. MEASURED, NOT ARGUED: the pre-C31 tree was reconstructed from ac80738 and the finished detector run over it - v1.5 owed set {D31}, 'would this have gone red for D33?' = False. So D33 is in docs/specs/V15-AMENDMENTS-APPLIED.md because a person put it there, and the ledger rather than the instrument is what catches a paraphrasing delta. This is the D18 class docs/specs/AMENDMENT-BASELINE.txt already records. THE OWNER'S TO RULE, AND IT IS A CHOICE OF TWO: accept that a paraphrasing delta is caught by the ledger and not by the instrument, or ask for a different detector. |
| O47 | **A refused dedup read drops the wrist tap, and `Distraction.id` still has no uniqueness constraint** | F7b | OPENED 2026-09-23 by `F7b`. `F7b` fixed the swallowed error: `try? context.fetch` mapped a refused read to "nothing found" and fell through to the insert, so a WatchConnectivity resend could become a second row for one press. The ruling shipped is DROP — nothing is inserted and `Outcome.unreadable` is returned — on the grounds that an over-count is permanent (no capture surface can delete a row, see `O44`'s note on the laggy button) while an under-count is merely absent. The owner may overrule; it is one line. The uniqueness constraint was DECLINED, not forgotten: SwiftData's `.unique` upserts rather than rejects, so a redelivered tap would overwrite an existing row's `note` with `nil` — STATED AS A CLAIM, NOT VERIFIED, because nothing here has run it — and adding a constraint to a shipped model with no `VersionedSchema` risks the store `O1` has never been read from. Taking it needs a `D<n>`, a versioned schema with an explicit de-dup migration, and an install-over-a-real-store check in `O2`'s shape. ALSO UNSETTLED, AND NOT THIS UNIT'S: a lost wrist tap is silent, while a refused tap on the phone gets an amber line — so `O15` cannot tell a tap lost to a refused read from one lost to WatchConnectivity. ADVERSARIAL REVIEW, SECOND PASS: TWO BLOCKING FINDINGS, BOTH REAL, NOTHING REFUTED. (1) The first build reached the refused-read branch through an injected closure `refusableFetch` on the production type, justified on the claim that a real ModelContext cannot be made to throw from fetch on demand. THE CLAIM WAS WRONG AND THE SUITE PAID FOR IT: every refused-read test drove the closure, so `return try context.fetch(descriptor)` - the only line the app runs - was never driven as a throwing call, and the reviewer reinstated the original defect one level deeper (`(try? context.fetch(descriptor)) ?? []`) with all 639 tests still green. THE SEAM IS DELETED. A real ModelContext throws when the open on-disk store's bytes are overwritten - NSCocoaErrorDomain 259, NSSQLiteErrorDomain 26 - which was established by running it, not argued; a container whose Schema omits Distraction was also probed and returns [] rather than throwing. Both tests now point a real context at a corrupted real store, the original bytes are restored and the store reopened through a fresh container so the ROW COUNT is read off the disk rather than recomputed. WatchTapInbox has one stored property again and no mutable production surface, which also closes the non-blocking finding that the seam was internal and injectable module-wide. (2) O47 was never written into docs/reviews/OPEN.md, which conventions-local.md defines as every outstanding item from every review in one table; the justification cited O44, but O44 forbids GENERATING OPEN.md from the register, not adding a row, and C32 wrote O45 and O46 into both files. The row is now in OPEN.md under 'Needs the owner'. ONE NON-BLOCKING FINDING WAS REFUTED, NOT TAKEN: logging the discarded error costs more than nothing - a grep over the app target returns zero hits, so there is no logging facility to add a line to and introducing one is new infrastructure inside a defect fix. The silence is recorded in this row instead. The `.failed` doc comment that claimed it 'is reported rather than swallowed' was corrected - the sole caller drops it. |
| ~~O48~~ | ~~**Whether the decisions register may carry a second `### Decisions` heading, and whether the status vocabulary is contract**~~ | C34 | RULED AND CLOSED 2026-09-24 by the owner: **language in `docs/conventions.md` is contract, not implementation.** It says *"one `##` per register"* and *"every register, one `##` each"*, so the owner-fields overlay could not be a second `## Decisions` heading however carefully that heading avoided the `(D)` suffix. THE IMPLEMENTATION CHANGED, NOT THE CONVENTION: the overlay is now `### Decisions — owner fields` inside the `D` section, after the generated region, and both `gen_status.parse_tables` and `check_register_rows` treat a `###` as a section boundary so its three-column rows are never checked against the `D` table's five-column header. The owner's wider ruling, recorded because it governs more than this row: the reviewing agent should have held to conventions.md rather than proposing a reading of it, and a change to conventions is reviewed carefully rather than arrived at through implementation. OPENED 2026-09-24 by `C34`'s adversarial review. TWO RULINGS `C34` MADE THAT MAY NOT BE THE AGENT'S TO MAKE, recorded as a field rather than as a paragraph in the chore. (1) `docs/conventions.md` Axis 2 says *"one `##` per register"* and `grep -n '^## Decisions' docs/plans/00-register.md` now returns two — `### Decisions — owner fields` (hand-maintained `P` and `TD`) and `### Decisions (D)` (generated). `D37` ruled the table generated and said nothing about where the owner's two fields live; the overlay's heading carries no `(D)` symbol on purpose, so no register is counted twice and no number is wrong, but the SHAPE is a reading of Axis 2 that the agent chose. The alternatives `C34` refused are in `docs/chores/C34.md` under *What was built · 2*. (2) `gen_status.CLOSED` gained `ratified` and `resolved` and deliberately did NOT gain `held`, so `O37` stays counted open — defensible as a no-change, but it is a vocabulary ruling, and Axis 2 pins only `proposed · ratified · rejected · superseded`. If a pinned vocabulary is contract rather than implementation, `resolved` and `held` owe a `D<n>`. Either answer is one line of code. |
| O49 | **A wrong JSON type on a mirrored field still fails the whole page** | F13 | `TodoistProjectDTO.color` and `TodoistTaskDTO.priority` are optional, which tolerates a MISSING key and an explicit null — verified — but NOT a wrong type: `"color": 4` or `"priority": "high"` raises `typeMismatch` and the whole page is refused, which presents as an empty picker. Both fields' doc comments claimed the broader tolerance and are corrected. `TodoistTaskDTO.Due` already has a hand-written lenient `init(from:)` for exactly this reason, so the fix is known and small. NOT TAKEN INSIDE `F13`: it is new tolerance on a shipped decode path, and folding that into a review pass is how a decode change arrives without anybody deciding it. Todoist v1 sends both as the documented types today, so this is latent. Found by `F13`'s adversarial review. |
| O50 | **The owner's real account data is committed to the tree** | F13 | Four live Todoist task ids and their titles in `ZenTomatoTests/TodoistPriorityTests.swift`, the priority tally `{1: 9, 2: 11, 3: 22, 4: 8}`, and an archived project's name in this register and in `docs/reviews/F13.md`. It is there for a good reason — the fixture is the owner's own account, which is what makes the priority direction evidence rather than a reading of documentation — and gitleaks is clean because none of it is a credential. But it is personal data in a licensed repository and it was committed without being asked about. The owner's call; removing it costs one edit and weakens the fixture to a synthetic one. Found by `F13`'s adversarial review. |
<!-- END GENERATED: owner -->
**Owner response**

After f7 drops, we’ll test a 1 hour pomodoro sprint that addresses the following items:
- O1 - O3
- O5 - O8

The following should be applied before this is reviewed:
- O10 — Approved
- O11
- O12 — Approved

O4 will be scheduled sometime today.

### Found in F6b's closing read, and fixed — 2026-08-24

**A project-attached block showed nothing on the wrist.** `SPEC.md` line 16: *"A pomodoro is
attached to exactly one Todoist task (or, if no task is chosen, to a project)."* The wire
field the phone sends the watch was called `taskTitle` and carried only the first of those, so
a block attached to a whole project arrived with a countdown and **nothing to say what it was
counting** — half the cases the spec allows, silently blank.

Nobody flagged it: the reviewers read the counting, the scope and the page, and the watch
came later. It only appears if you plan a project rather than a task and then look at your
wrist. Fixed by sending whatever the block is attached to, in the order the spec states them.

**And a break now says where the buttons went.** They vanish during a break, correctly —
`SPEC.md` makes them *"tappable during a pomodoro"* — but a control that disappears without a
word reads as a fault. The phone answers this by reserving the space (D19); a wrist has none
to reserve, so it answers in a sentence.

### Watch item — stopping a block offered nothing to complete

**Reported 2026-08-24, to be retried.** The owner: *"i stopped the pomodoro and couldn't
select what was completed."*

**The wiring is there.** `StopReasonSheet` carries the completion control, and the stop path
populates its subject before presenting — `TimerView.swift:842`, the same call the
end-of-block path makes. So this is not a missing feature; it is one of four specific
conditions, and knowing which turns a repeat into a diagnosis.

`currentCompletionSubject()` returns nothing unless **all four** hold:

| Condition | If it fails |
|---|---|
| A Todoist token is stored | the section cannot appear at all |
| The block had an attachment | nothing to complete |
| That attachment is a **task** | **a block attached to a *project* has no task to tick off** |
| The task has a title snapshot | nothing to name |

**The third is the likeliest, and it is by design.** `SPEC.md`: *"A pomodoro is attached to
exactly one Todoist task (or, if no task is chosen, to a project)."* Only a task can be
completed, so a project-attached block correctly offers nothing.

**There is a second reading, and it is the more interesting one.** "Select what was
completed" may mean choosing from the session plan rather than ticking off the one attached
task. That is not what the app does — it offers the block's own task and nothing else — and
it is close to the owner's stated use case: *"At the end of a pomodoro, but before the break,
I want to capture what I completed."* If that is what was expected, this is a spec question
for v1.1 rather than a defect, and it needs a delta.

**What to note on the retry:** what the block was attached to — a task, a project, or nothing
— and whether the section was absent entirely or present but disabled. Those two answers
separate all four conditions from the expectation gap.

### Device results — 2026-08-24

**Interruptions, from the owner:** *"phone call paused the timer and the music, when
answered. it seems to be working. headphones and siri activation pause but keep working."*

**The timer did not pause, and could not have.** There is no `pause` function, no
`isPaused` and no `pausedAt` anywhere in `TimerEngine`; a block's end rests on `endsAt`, an
absolute instant, and remaining time is `endsAt.timeIntervalSince(instant)`. The music
interruption handler reaches music only. What paused was the audio, which is correct, while
the call held the screen. The no-pause rule ratified on 2026-08-21 is intact.

**One thing to confirm during the full sprint, cheaply:** that a block interrupted by a call
still *ends at the right wall-clock minute*. The design says it must; nobody has watched it.

**Alarm through an active Focus: pass.** The reason D3 chose AlarmKit over a local
notification, proven on hardware for the first time.

**CarPlay: pass.** The owner: *"the song played through the app and didn't miss a beat."*
O4 closes — all three interruption paths now exercised.

**Playlist looping: pass.** *"the playlists repeats."* O5 closes. The loop had only ever been
asserted against a stand-in; no real playlist had run out until now.

**Wrist taps at over fifty feet: pass, informally.** *"the watch taps were captured a distance
over 50 feet."* D2's *Done when* asks for the phone in another room; fifty feet is past that.
O15 stays open only until the full pass confirms it with the timestamps checked, which is the
half that actually matters — that a queued tap arrives saying **when it happened**, not when
it was delivered.

**A defect the wrist found, and it was costing data.** The owner: *"tapping the app in the
watch has a long delay — more than 2 seconds — and it led to an excessive number of I and E
clicks."*

`transferUserInfo` was being called synchronously on the main actor, with the counters
incremented *after* it returned. So nothing on screen moved for over two seconds and the
button read as dead — and every extra press was a real tap that became a real row. **A laggy
button was quietly inflating the one number this app exists to produce.** Fixed: the transfer
moved off the main actor, the counters raised before it, and a tally now appears the instant a
button is pressed.

**The taps already made are real rows and are still in the database.** There is no capture
surface and no editor, by design, so nothing in the app can remove them. Any export covering
that block will over-report. Worth knowing before O1 is judged.

## Needs the agent

<!-- BEGIN GENERATED: agent — scripts/gen_status.py writes every row between these markers from docs/plans/00-register.md (C37). Do not edit by hand: `make check-status` fails, and the next run reverts it. Prose outside the markers is hand-maintained and is never touched. -->
| # | Item | From | Why it is still open |
|---|---|---|---|
| A1 | **`try?` on the three `StatsQuery` fetches** | F6 | A refused database read renders as a confident "you did nothing", on the one screen whose entire premise is being trusted, and as `No pomodoros in this range.` in a document filed as truth. |
| ~~A2~~ | ~~**`noIdentifiersInOutput` is largely tautological**~~ | F6 | CLOSED 2026-08-24 — routed through a real store, verified by mutation. Four of its five assertions cannot fail: the fixture contains no identifiers. The structural guarantee is real; the executed half is decorative. |
| ~~A3~~ | ~~**Main-thread render and disk I/O on every range change**~~ | F6 | CLOSED 2026-08-24 by measurement. `refreshExport()` builds the whole document and sweeps the temp directory synchronously, eagerly, whether or not anyone taps Export. |
| ~~A4~~ | ~~**The temp sweep deletes every `ZenTomato-*.md`**~~ | F6 | CLOSED 2026-08-24 — per-launch directory. Including one an open share extension may still be reading. |
| ~~A5~~ | ~~**Dead code with load-bearing documentation**~~ | F6 | CLOSED 2026-08-24 — `StatsRange.everything` deleted. `StatsRange.everything(endingOn:in:)` and `StatsPeriod.recordedSpan` have no caller outside tests and describe an all-time export the range control does not offer. |
| ~~A6~~ | ~~**`StatsRange.swift:35` states the opposite of the shipped UI**~~ | F6 | CLOSED 2026-08-24. It says "no free-form date pickers" against a control whose own header says "Two pickers and a reset." |
| ~~A7~~ | ~~**Markdown metacharacters in Todoist titles are not escaped**~~ | F6 | CLOSED 2026-08-24. A task named `**Thesis**` renders as markup in the exported page. |
| A8 | **Time-zone change re-attributes historical days — *researched, ruled, pinned*** | F6 | Correct across DST — every boundary goes through `Calendar` — but a device that changes zone makes two exports of the same fortnight disagree. Unspecified and untested. |
| ~~A9~~ | ~~**The performance test asserts `< 1s` against a 16 ms budget**~~ | F6 | CLOSED 2026-08-24 — tightened to 250 ms. A 60× regression passes green. |
| ~~A10~~ | ~~**The palette lint rule does not cover `Stats/`, `Export/`, `Sprint/`**~~ | F6 | CLOSED 2026-08-24 — and `ZenTomatoWatch/`, `Todoist/`, `Plan/`, `Music/`. Nothing violates it today; the guard has a hole. |
| ~~A11~~ | ~~**The no-writes fence test omits `update` and `comment`**~~ | F6 | CLOSED 2026-08-24. Two of the four words `CLAUDE.md`'s hook names. The real gate — the allowlist — is untouched, so the test's comment overstates it. |
| ~~A12~~ | ~~**A task finished on another device is still offered until the next refresh**~~ | F3 | CLOSED 2026-08-24 — the mirror forgets it on `.alreadyGone`. `.alreadyGone` enters neither the D21b set nor a cache deletion. Pre-existing; widening D21b's trigger would change its meaning from *completed* to *believed gone*. |
| ~~A13~~ | ~~**`docs/reviews/F6.md` does not follow the F1–F5 template**~~ | C6 | CLOSED 2026-08-24. Six logs, two shapes. |
| A14 | **The rewind that could not be reproduced** | F4 | Reported once, never seen again, most likely an artefact of the track name being one behind at the time. `docs/reviews/F4.md` carries the diagnostic order and four ranked fixes; **B** is recommended if it recurs. |
| ~~A15~~ | ~~**`MusicSubscription.current` is deprecated in process**~~ | F4 | CLOSED 2026-08-24 — not actionable on iOS. The device log carries: *"it has recently become deprecated to request the music subscription status in process; the new supported code path fetches it in itunescloudd, but you need to add `com.apple.security.exception.mach-lookup.global-name com.apple.itunescloud.music-subscription-status-service` to your sandbox"*. Works today. Worth doing before it stops. |
| A16 | **The query costs 57 ms for an all-time range** | F6b | Found by measuring for A3 — the cost is the query, not the render. ~0.22 ms per day of range, so the 16 ms budget is crossed at about ten weeks; the default fortnight is 3 ms. **Deliberately not fixed:** `F6-contract.md` pre-decided a `@ModelActor` and warned against taking it pre-emptively, *"since a suspension point is where the last two features found their real bugs"*. Recorded with the number so it can be decided with rather than about. |
| A17 | **Why the alarm is silent when the phone is silent** | F2 | Unknown, and **not to be guessed at** — three framework assumptions have already been wrong this week. The configuration itself is idiomatic: `AlarmManager.AlarmConfiguration.timer(...)` with `sound: .default` when the setting is on and `.named("Silence.caf")` when off, and `Silence.caf` is confirmed present in the shipped bundle. Two candidate causes, and one device check separates them — see `docs/chores/C14.md`. |
| A18 | **Watchdog kill: `playbackState` is a blocking XPC call on the main thread — *diagnosed, fix designed, not built*** | F4 | `docs/crashes/ZenTomato-2026-08-26-134602.ips`: `0x8BADF00D`, ten seconds of wall clock against **0.053s of app CPU** — blocked, not looping. Stack ends in `xpc_connection_send_message_with_reply_sync` under `MPMusicPlayerController.playbackState`. `F4-contract.md` §8 chose to read it live on purpose and did not price it as IPC. **No test could have caught it**: the stand-in returns instantly, and nothing in the suite distinguishes a property read from a cross-process call. See `docs/chores/C16.md`. |
| A19 | **Five `F8` mutation IDs are defined only in commit messages and cannot be validated from the tree** | C33 | OPENED 2026-09-23 by `C33`. **All five** — `F8-M6` and `F8-M10` through `F8-M13` — are **defined in no file in the tree**; the only source for each is a commit body (`66e3e1c` for `F8-M6`, `b938f95` for the other four), and each register cell transcribes that body. CORRECTED 2026-09-23 BEFORE MERGE, and the correction is the instructive part: an earlier draft of this row split the five into *two cited* and *three uncited*, derived by a literal grep. `docs/handoffs/blockersfor1_5.md:270` cites all five in one sentence using RANGE notation — "`F8-M6` and `F8-M10`–`M13` exist **only** in the text of two commits" — which a literal grep for `F8-M11` cannot see. The partition was an artifact of the search, asserted as fact in the row whose entire subject is provenance. It also understated the exposure handed to `C34` as two IDs when it is five, so an exemption list built from the earlier draft would have been short by three. And being named in that handoff is not provenance for a mutation's *definition*: the handoff cites these IDs only to record that they have none (`66e3e1c` and `b938f95`), so they are citations with nothing behind them in the tree: the `D14` failure again. **THE ROWS IN `## Mutations (M)` ARE A TRANSCRIPTION AND NOT A SOURCE, AND THE DISTINCTION IS THE WHOLE ITEM.** This row and those rows state each definition, in a file in the tree, so the earlier wording — *"appear in no file in the tree"*, written inside a file in the tree — said something it did not mean and was corrected 2026-09-23 by `C33`'s second review pass. What is absent is an authored definition a checker can trace to a document; what is present is five cells a person copied out of `git show`, which nothing can verify without leaving the tree. All five have rows in `## Mutations (M)` whose `Source` is a SHA, because an ID in use is in the register or the register is not the register. **This is one item, not five rows of the same complaint.** `C34`'s validator cannot resolve a SHA by reading files and must either accept one as a terminal citation or report these as an enumerated exemption — and an exemption list is how a validator quietly stops validating, so whichever it is should be visible in the output. Git history is NOT rewritten and `docs/plans/F8.md` is not retro-edited: `F8.md:620` declined to open an `M` register deliberately and with its reasoning stated. |
<!-- END GENERATED: agent -->

## Closed

| # | Item | From | How it closed |
|---|---|---|---|
| C-1 | CI had never run; `runs-on: macos-26` unverified | F1 | Green on PR #1, 2026-08-22. F1's largest open risk. |
| C-2 | The timer numeral shipped at ~34pt against a ratified 96pt | F1 | D7, ratified 2026-08-22. |
| C-3 | Cold launch flashed white before the stone page painted | F1 | D8, the launch colour set. |
| C-4 | `.env` held an `ACCESS_TOKEN` | F1 | D6b moved secrets to `.xcconfig`; no `.env` exists and it remains git-ignored. |
| C-5 | `## Projects` collapsed to one "No project" heading on real data | F6 | F3b and D22, 2026-08-24. |
| C-6 | D14 cited in ten files and defined nowhere | C6 | Recorded 2026-08-24; `DeltaIntegrityTests` now checks every citation. |
| C-7 | **The two CC0 bell files, supplied and trimmed** — carried the id `O23` in this file, the first of two | F2c | **CLOSED 2026-08-28 — the record was wrong, not the work.** Filed saying the agent could not fetch the sounds and the owner must supply them. **The owner already had**: both WAVs were sitting untracked at the repository root, downloaded 2026-08-27, and the adversarial reviewer found them. The `curl` that returned a login page was real; the conclusion drawn from it was not checked against the tree. Both are now trimmed, normalised and committed as `SmallBell.caf` and `StruckBell.caf` — `docs/sounds/candidates.md` carries the exact commands. The raw 17 MB of WAV moved to the git-ignored `docs/sounds/sources/`, off the repository root where one `git add -A` would have committed it. |
| C-8 | **A block heard end-to-end on a chosen bell** — carried the id `O24` in this file, the first of two | F2c | **CLOSED 2026-08-28.** Sprint on build `202608281026`. Both bells heard at a block boundary; the owner's verdict: *"both sound good."* The trim points and levels in `docs/sounds/candidates.md` were chosen by measuring a waveform, and the measurement held. |
| C-9 | **The dual-licensing report is fetchable by SHA on GitHub** — carried the id `O24` in this file, the second of two | C13 | **RULED 2026-08-27 — accepted, not purged.** Removed from the tree in `c4358c2` but still reachable by its 40-character object id, as any removed-but-not-rewritten blob is. The owner accepted the exposure rather than rewrite history. **The reasoning, so nobody reopens it:** the object is unreachable from any branch and needs the exact SHA to fetch, while rewriting `main` would change every commit id in the repository — and this project cites commit ids constantly, in plans, reviews and merged PR bodies. The cure alters more than the disease. `docs/*-report.md` and the archive globs stop a recurrence. |
| C-10 | **The plan gate was being skipped** — carried the id `O25` in this file, the second of two | C6 | **RULED 2026-08-27 — rule kept, agent to stop skipping it.** Raised by the adversarial reviewer on `F4c`, where the plan was committed one second after the code it gates, and true of most of 2026-08-27's work. A plan written afterwards is a justification and cannot do the gate's job — giving the owner a moment to say *"don't build that"* while it is still cheap. Defect fixes proceed (the `D22` precedent); everything else waits for a yes. Written into `conventions.md`. |

*`C-7`–`C-10` are the four rows recovered by `C37`. Each shared an `O` id with a different row, so the register — which holds one row per id — could keep only one of each pair. They are closed and ruled, so they are recorded here under this table's own scheme rather than given new `O` numbers: renumbering a collision needs the owner's ruling, and `## Defect — reused IDs` in the register still holds the open question.*
