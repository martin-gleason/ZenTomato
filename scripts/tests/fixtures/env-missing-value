# FIXTURE — a .env with a required key present but blank, used by
# scripts/tests/run-script-tests.sh to prove gen-secrets.sh fails loudly and
# names the offending key rather than producing a half-configured build.
#
# EVERY VALUE HERE IS FAKE. See env-complete.
#
# TODOIST_CLIENT_SECRET is deliberately empty. That is the whole point of this
# fixture: an empty client secret builds an app that works right up until the
# moment somebody tries to sign in, which is the worst place to discover it.

TODOIST_CLIENT_ID=EXAMPLE_NOT_A_REAL_CLIENT_ID
TODOIST_CLIENT_SECRET=
TODOIST_OAUTH_CALLBACK_SCHEME='zentomato-example'
DEVELOPMENT_TEAM=
